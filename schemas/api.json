{
  "openapi": "3.1.0",
  "info": {
    "title": "Wink API",
    "version": "0.0.0-stub",
    "description": "Placeholder schema. The upstream https://api.wink.travel/v3/api-docs endpoint was unreachable when this snapshot was last refreshed. Run `npm run schemas:sync` once upstream recovers to replace this stub with the real spec."
  },
  "servers": [
    {
      "url": "https://api.wink.travel",
      "description": "Production"
    }
  ],
  "paths": {
    "/health": {
      "get": {
        "summary": "Placeholder",
        "description": "This path exists only so the OpenAPI document validates while the real spec is unavailable.",
        "responses": {
          "200": {
            "description": "OK"
          }
        }
      }
    }
  }
}
