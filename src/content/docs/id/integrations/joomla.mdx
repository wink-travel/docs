---
title: Joomla
description: Sematkan inventaris perjalanan Wink ke situs Joomla 4/5 Anda menggunakan ekstensi Wink.
sidebar:
  order: 12
---

import { Steps, LinkCard } from '@astrojs/starlight/components';

**Wink Joomla Extension** adalah paket yang menginstal plugin sistem (memuat skrip CDN), plugin konten (memproses tag shortcode dalam artikel), dan komponen (halaman pengaturan admin).

<LinkCard
  title="wink-travel/wink-joomla-extension on GitHub"
  href="https://github.com/wink-travel/wink-joomla-extension"
  description="Kode sumber, panduan pengaturan pengembang, dan instruksi kontribusi."
/>

## Persyaratan

- Joomla 4 atau 5
- PHP 8.1+

## Instalasi

<Steps>
1. Unduh `pkg_wink-x.x.x.zip` terbaru dari [halaman rilis GitHub](https://github.com/wink-travel/wink-joomla-extension/releases).
2. Di Joomla Admin, buka **Extensions → Manage → Install**.
3. Unggah file zip dan klik **Upload & Install**.
4. Kedua plugin akan diaktifkan secara otomatis. Buka **Components → Wink Settings**.
5. Masukkan **Client ID** dan **Client Secret** Anda lalu klik **Save**.
   Buka [Applications](/id/portal/settings/applications) untuk mendapatkan kredensial Anda.
</Steps>

## Sematkan komponen dalam artikel

Gunakan tag gaya shortcode di mana saja dalam konten artikel:

```
{wink-content layout="HOTEL" id="YOUR_LAYOUT_ID"}

{wink-lookup}

{wink-search}

{wink-account}

{wink-itinerary}

{wink-cart}
```

Plugin konten akan menggantikan tag ini dengan HTML `<wink-*>` yang benar saat render. Plugin sistem secara otomatis memuat skrip CDN dan menyisipkan `<wink-app-loader>` di footer halaman.

## Shortcode yang tersedia

| Shortcode | Output | Deskripsi |
|---|---|---|
| `{wink-content layout="..." id="..."}` | `<wink-content-loader>` | Kartu hotel, grid, peta |
| `{wink-lookup}` | `<wink-lookup>` | Bar pencarian destinasi |
| `{wink-search}` | `<wink-search-button>` | Tombol pemilih itinerary |
| `{wink-account}` | `<wink-account-button>` | Masuk / akun pengguna |
| `{wink-itinerary}` | `<wink-itinerary-button>` | Tombol status itinerary |
| `{wink-cart}` | `<wink-shopping-cart-button>` | Tombol keranjang |

## Bacaan lanjutan

- [Referensi Web Components](/id/integrations/web-components)
- [Dapatkan Client ID Anda](/id/portal/settings/applications)
- [Wink Joomla Extension — kode sumber & dokumen pengembang](https://github.com/wink-travel/wink-joomla-extension)
