---
title: קטלוג אירועי Webhook
description: כל סוגי אירועי ה-webhook בפלטפורמת Wink, שנוצרים מתוך קטלוג האירועים של הפלטפורמה — מי מקבל אותם, לאן, והיכן מתועד התוכן.
sidebar:
  order: 3.5
---

import webhooks from '../../../../schemas/webhooks.json';
import WebhookEventTypes from '@/components/webhooks/event-types.astro';

דף זה מציג את כל אירועי ה-webhook שניתן להירשם אליהם ב-Wink. להוראות הגדרה, ראה את [מדריך אינטגרציית Webhook](/he/developers/webhook-integration) או את [דף ניהול ה-Webhooks](/he/portal/settings/webhooks).

## איך להירשם

1. עבור אל **Applications > Webhooks** בפורטל שלך.
2. צור webhook ובחר את האירועים שברצונך לקבל — או השאר את הרשימה ריקה כדי לקבל את כל האירועים.
3. Wink שולחת בקשת HTTP POST חתומה לנקודת הקצה שלך בכל פעם שהאירוע מתרחש. ראה [אינטגרציית Webhook](/he/developers/webhook-integration) עבור המעטפה, הכותרות, אימות החתימה וניסיונות חוזרים.

## כל סוגי האירועים

טבלה זו נוצרת מתוך קטלוג האירועים של הפלטפורמה בזמן הבנייה, ולכן תמיד תואמת את ה-API.
סוגי אירועים המסומנים כ-delivered מקשרים לדף ההתייחסות שלהם ב-[Webhooks API](/he/api/webhooks/), שבו מתועד גוף ה-JSON המדויק (`WebhookEnvelope` עם `object` ספציפי לאירוע), כותרות `Wink-*` ומדיניות הניסיון החוזר. סוגי אירועים שאינם נשלחים קיימים פנימית (תשלומים, ספר חשבונות, סנכרון מנהל ערוצים, …) אך אינם נשלחים כרגע למנויי ה-webhook.

**מקבלי האירוע** מציין מי מקבל את האירוע: `SUPPLIER` (המלון), `AFFILIATE` (משווק עם ערוץ מכירות פעיל למלון), `AGENT` (סוכנות הנסיעות שביצעה את ההזמנה) או `SELF` (הישות המנהלתית שהאירוע עוסק בה). כל מקבל מקבל את אותו מבנה תוכן; פרטים ספציפיים לתפקיד נמצאים מאחורי כתובת ה-URL `links.self` שבתוכן.

<WebhookEventTypes
  types={webhooks['x-wink-event-types']}
  referenceBase="/api/webhooks"
  eventHeader="אירוע"
  descriptionHeader="תיאור"
  deliveredHeader="נשלח"
  recipientsHeader="מקבלים"
  deliveredLabel="נשלח"
  notDeliveredLabel="לא נשלח"
  noRecipientsLabel="—"
/>

:::tip
התחל בהירשמות לכל האירועים במהלך הפיתוח. ברגע שתבין אילו תוכן המערכת שלך צריכה, צמצם את ההרשמות כדי להפחית רעש. שלח לעצמך אירוע בדיקה מ-**Applications > Webhooks** (או `POST …/webhook/{id}/test`) כדי לאמת את נקודת הקצה ובדיקת החתימה לפני ההפעלה החיה.
:::

## קריאה נוספת

- [מדריך אינטגרציית Webhook](/he/developers/webhook-integration) — שלבי הגדרה והמלצות.
- [Webhooks](/he/portal/settings/webhooks) — ניהול הרשמות ה-webhook שלך.
- [Applications](/he/account/applications) — ניהול אישורי API.
- [Build on Wink](/he/developers/build-on-wink) — סקירת הפלטפורמה למפתחים.
