---
title: Webhook 事件目录
description: Wink 平台上的每种 webhook 事件类型，来源于平台的事件目录 — 事件的发送对象及其负载文档位置。
sidebar:
  order: 3.5
---

import webhooks from '../../../../schemas/webhooks.json';
import WebhookEventTypes from '@/components/webhooks/event-types.astro';

本页列出了您可以在 Wink 上订阅的所有 webhook 事件。有关设置说明，请参阅[Webhook 集成指南](/zh-CN/developers/webhook-integration)或[Webhook 管理页面](/zh-CN/portal/settings/webhooks)。

## 如何订阅

1. 在您的门户中导航至 **应用 > Webhooks**。
2. 创建一个 webhook 并选择您想接收的事件 — 或留空以接收所有事件。
3. 每当事件发生时，Wink 会向您的端点发送带签名的 HTTP POST。有关信封、头信息、签名验证和重试，请参阅[Webhook 集成](/zh-CN/developers/webhook-integration)。

## 所有事件类型

此表格由平台事件目录在构建时生成，因此始终与 API 保持一致。
标记为已发送的事件类型链接到其在[Webhooks API](/zh-CN/api/webhooks/)中的参考页面，
该页面记录了精确的 JSON 正文（`WebhookEnvelope`，包含事件特定的 `object`）、`Wink-*` 头信息和重试策略。
未发送的事件类型存在于内部（支付、账本、渠道管理同步等），但当前不会发送给 webhook 订阅者。

**接收者** 表示谁会收到事件：`SUPPLIER`（酒店）、`AFFILIATE`（与酒店有活跃销售渠道的转售商）、`AGENT`（完成预订的旅行社）或 `SELF`（事件所涉及的管理实体）。
每个接收者收到的负载格式相同；角色特定的详细信息位于负载的 `links.self` URL 后。

<WebhookEventTypes
  types={webhooks['x-wink-event-types']}
  referenceBase="/api/webhooks"
  eventHeader="事件"
  descriptionHeader="描述"
  deliveredHeader="已发送"
  recipientsHeader="接收者"
  deliveredLabel="已发送"
  notDeliveredLabel="未发送"
  noRecipientsLabel="—"
/>

:::tip
开发阶段建议先订阅所有事件。了解系统所需的负载后，再缩小订阅范围以减少噪音。您可以从 **应用 > Webhooks** 发送测试事件（或 `POST …/webhook/{id}/test`）以确认端点和签名校验无误后再上线。
:::

## 进一步阅读

- [Webhook 集成指南](/zh-CN/developers/webhook-integration) — 设置步骤和最佳实践。
- [Webhooks](/zh-CN/portal/settings/webhooks) — 管理您的 webhook 订阅。
- [应用](/zh-CN/account/applications) — 管理 API 凭证。
- [在 Wink 上构建](/zh-CN/developers/build-on-wink) — 面向开发者的平台概览。
