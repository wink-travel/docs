---
title: Gatsby
description: 使用 gatsby-browser.js 在 Gatsby 站点中添加 Wink Web 组件以实现客户端加载。
sidebar:
  order: 23
---

import { Aside, LinkCard } from '@astrojs/starlight/components';

Gatsby 在构建时生成静态 HTML，并在客户端进行重新激活。CDN 脚本必须仅在浏览器 API 中加载——`gatsby-browser.js` 是正确的位置。

<LinkCard
  title="@wink/elements npm package"
  href="/integrations/npm"
  description="安装并加载带有 TypeScript 类型的 CDN 包。"
/>

## 安装

```bash
npm install @wink/elements
```

## 通过 gatsby-browser.js 加载

`onClientEntry` API 会在 Gatsby 运行时在浏览器中初始化时运行一次：

```js
// gatsby-browser.js
import { load } from '@wink/elements';

export function onClientEntry() {
  load({ clientId: process.env.GATSBY_WINK_CLIENT_ID });
}
```

## 环境变量

Gatsby 只会将以 `GATSBY_` 为前缀的变量暴露给浏览器包：

```
# .env.production
GATSBY_WINK_CLIENT_ID=your-client-id
```

```
# .env.development
GATSBY_WINK_CLIENT_ID=your-client-id
```

## 在页面和组件中使用

```tsx
// src/pages/hotels.tsx
import React from 'react';

export default function HotelsPage() {
  return (
    <main>
      <wink-content-loader layout="HOTEL" id="YOUR_LAYOUT_ID" />
      <wink-lookup />
    </main>
  );
}
```

## TypeScript

Gatsby 使用 React，因此添加与 React 集成相同的 JSX 内置声明：

```ts
// src/wink-elements.d.ts
// 详见 /integrations/react 中的完整声明文件
```

<Aside type="caution" title="SSR 构建错误">
在执行 `gatsby build` 时，Gatsby 会在 Node.js 环境中将页面渲染为静态 HTML，此时 `window` 和 `document` 不可用。如果你在页面组件的顶层（`onClientEntry` 之外）导入 `@wink/elements`，构建将失败。

请始终使用 `gatsby-browser.js` / `onClientEntry` —— 切勿在页面或组件模块作用域中直接导入 `load`。
</Aside>

## gatsby-ssr.js — 无需更改

你无需修改 `gatsby-ssr.js`。`<wink-*>` 标签在静态输出中渲染为普通 HTML 元素，CDN 脚本在浏览器加载页面时会升级它们。

## 进一步阅读

- [Web Components 参考](/zh-CN/integrations/web-components)
- [@wink/elements npm package](/zh-CN/integrations/npm)
- [React 集成](/zh-CN/integrations/react)
