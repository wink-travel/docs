---
title: Lookup
description: A search dropdown that lets users find hotels and destinations, updating any connected ranked grid.
sidebar:
  order: 3
---

import CloudinaryImage from '/src/components/cloudinary-image.astro';
import WebComponentPreview from '/src/components/WebComponentPreview.astro';

Lookup works great together with one of your ranked grids. It lets your users search for hotel and destination and the grid updates in response to their search result.

## Usage

```html {3}
<html>
  <body>
    <wink-lookup></wink-lookup>
  </body>
</html>
```

<figure>
<CloudinaryImage
      width="350"
      height="57"
      resize="scale"
      quality="best"
      alt="Lookup web component"
      src="academy/lookup"
      errorImage="noimage_opaque_nyrtl0.png"
    />
    <figcaption>*Lookup web component*</figcaption>
</figure>

Clicking on the component above opens a modal that lets you type in the destination or hotel you are searching for.

<figure>
<CloudinaryImage
      width="700"
      height="186"
      resize="scale"
      quality="best"
      alt="Lookup modal"
      src="academy/lookup-results"
      errorImage="noimage_opaque_nyrtl0.png"
    />
    <figcaption>*Lookup modal with results*</figcaption>
</figure>

## Live Preview

<WebComponentPreview src="/demos/lookup" minWidth={375} height={400} />
