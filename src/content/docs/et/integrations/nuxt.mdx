---
title: Nuxt 3
description: Lisa Wink veebikomponendid Nuxt 3 rakendustesse, kasutades kliendipoolset pluginat.
sidebar:
  order: 19
---

import { Aside, LinkCard } from '@astrojs/starlight/components';

Nuxt 3 renderdab lehti vaikimisi serveris. CDN skriptid ja kohandatud elemendid tuleb laadida ainult kliendipoolselt. Nuxt kliendipoolne plugin on selleks kõige puhtam koht.

<LinkCard
  title="@wink/elements npm package"
  href="/integrations/npm"
  description="Paigalda ja laadi CDN pakett koos TypeScripti tüüpidega."
/>

## Paigalda

```bash
npm install @wink/elements
```

## Loo kliendipoolne plugin

Lõpp `.client.ts` ütleb Nuxtile, et see plugin käivitatakse ainult brauseris:

```ts
// plugins/wink.client.ts
import { load } from '@wink/elements';

export default defineNuxtPlugin(() => {
  load({ clientId: useRuntimeConfig().public.winkClientId });
});
```

## Konfigureeri runtime muutuja

```ts
// nuxt.config.ts
export default defineNuxtConfig({
  runtimeConfig: {
    public: {
      winkClientId: process.env.NUXT_PUBLIC_WINK_CLIENT_ID,
    },
  },
});
```

Lisa `.env` faili:

```
NUXT_PUBLIC_WINK_CLIENT_ID=your-client-id
```

## Konfigureeri Vue kohandatud elementide jaoks

Lisa `isCustomElement` kompilaatori valik, et Vue ei hoiaks tundmatute `<wink-*>` siltide pärast:

```ts
// nuxt.config.ts
export default defineNuxtConfig({
  vue: {
    compilerOptions: {
      isCustomElement: (tag) => tag.startsWith('wink-'),
    },
  },
  runtimeConfig: {
    public: {
      winkClientId: process.env.NUXT_PUBLIC_WINK_CLIENT_ID,
    },
  },
});
```

## Kasutamine mallides

Paki Wink komponendid `<ClientOnly>` sisse, et vältida SSR hüdreerimise ebakõlasid:

```vue
<template>
  <main>
    <ClientOnly>
      <wink-content-loader layout="HOTEL" :id="layoutId" />
    </ClientOnly>
  </main>
</template>

<script setup lang="ts">
const layoutId = 'YOUR_LAYOUT_ID';
</script>
```

<Aside type="tip">
`<ClientOnly>` renderdab serveris kohatäitja ja paigaldab reaalse elemendi kliendipoolselt — see on soovitatav muster kõigi ainult brauseris töötavate kolmandate osapoolte manustuste jaoks Nuxtis.
</Aside>

## TypeScripti tüübid

Laienda globaali `HTMLElementTagNameMap` (nagu Vue 3-s), et saada atribuutide automaatne täitmine. Täieliku deklaratsioonifaili leiad [Vue 3 integratsiooni lehelt](/et/integrations/vue#typescript--augment-global-element-types).

## Lisalugemine

- [Veebikomponentide viide](/et/integrations/web-components)
- [@wink/elements npm pakett](/et/integrations/npm)
- [Vue 3 integratsioon](/et/integrations/vue)
