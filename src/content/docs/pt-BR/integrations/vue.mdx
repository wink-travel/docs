---
title: Vue 3
description: Use componentes web Wink em projetos Vue 3 e Vite com a opção de compilador isCustomElement.
sidebar:
  order: 18
---

import { Aside, LinkCard } from '@astrojs/starlight/components';

Vue 3 precisa de uma dica para o compilador tratar as tags `<wink-*>` como elementos customizados nativos em vez de componentes Vue não resolvidos. Depois disso, eles funcionam como qualquer outro elemento HTML.

<LinkCard
  title="@wink/elements npm package"
  href="/integrations/npm"
  description="Instale e carregue o bundle CDN com tipos TypeScript."
/>

## Instalar

```bash
npm install @wink/elements
```

## Configurar o compilador

### Vite + Vue (`vite.config.ts`)

```ts
import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';

export default defineConfig({
  plugins: [
    vue({
      template: {
        compilerOptions: {
          // Tratar todas as tags que começam com "wink-" como elementos customizados
          isCustomElement: (tag) => tag.startsWith('wink-'),
        },
      },
    }),
  ],
});
```

### Vue CLI (`vue.config.js`)

```js
module.exports = {
  chainWebpack(config) {
    config.module
      .rule('vue')
      .use('vue-loader')
      .tap((options) => ({
        ...options,
        compilerOptions: {
          isCustomElement: (tag) => tag.startsWith('wink-'),
        },
      }));
  },
};
```

## Carregar CDN uma vez na inicialização do app

```ts
// src/main.ts
import { createApp } from 'vue';
import App from './App.vue';
import { load } from '@wink/elements';

load({ clientId: import.meta.env.VITE_WINK_CLIENT_ID });

createApp(App).mount('#app');
```

## Usar em templates

```vue
<template>
  <main>
    <wink-content-loader layout="HOTEL" :id="layoutId" />
    <wink-lookup />
  </main>
</template>

<script setup lang="ts">
const layoutId = 'YOUR_LAYOUT_ID';
</script>
```

## TypeScript — aumentar tipos globais de elementos

Vue 3 + TypeScript lê os tipos de elementos customizados do `HTMLElementTagNameMap` global. Adicione um arquivo de declaração:

```ts
// src/wink-elements.d.ts
import type {
  WinkContentLoaderAttributes,
  WinkLookupAttributes,
  WinkSearchButtonAttributes,
  WinkAccountButtonAttributes,
  WinkItineraryButtonAttributes,
  WinkShoppingCartButtonAttributes,
  WinkAppLoaderAttributes,
} from '@wink/elements';

declare global {
  interface HTMLElementTagNameMap {
    'wink-content-loader': HTMLElement & WinkContentLoaderAttributes;
    'wink-lookup': HTMLElement & WinkLookupAttributes;
    'wink-search-button': HTMLElement & WinkSearchButtonAttributes;
    'wink-account-button': HTMLElement & WinkAccountButtonAttributes;
    'wink-itinerary-button': HTMLElement & WinkItineraryButtonAttributes;
    'wink-shopping-cart-button': HTMLElement & WinkShoppingCartButtonAttributes;
    'wink-app-loader': HTMLElement & WinkAppLoaderAttributes;
  }
}
```

## Variáveis de ambiente

Adicione em `.env.local`:

```
VITE_WINK_CLIENT_ID=your-client-id
```

O Vite expõe apenas variáveis prefixadas com `VITE_` para o bundle cliente.

<Aside type="tip">
Para Nuxt 3, veja a página dedicada de [integração Nuxt](/pt-BR/integrations/nuxt) que cobre o carregamento seguro para SSR.
</Aside>

## Leitura adicional

- [Referência de Web Components](/pt-BR/integrations/web-components)
- [Pacote npm @wink/elements](/pt-BR/integrations/npm)
- [Integração Nuxt 3](/pt-BR/integrations/nuxt)
