---
title: Catálogo de Eventos Webhook
description: Todos os tipos de eventos webhook na plataforma Wink, gerados a partir do catálogo de eventos da plataforma — quais são entregues, para quem e onde a carga útil está documentada.
sidebar:
  order: 3.5
---

import webhooks from '../../../../../schemas/webhooks.json';
import WebhookEventTypes from '@/components/webhooks/event-types.astro';

Esta página lista todos os eventos webhook aos quais você pode se inscrever na Wink. Para instruções de configuração, veja o [Guia de Integração Webhook](/pt-BR/developers/webhook-integration) ou a [página de gerenciamento de Webhooks](/pt-BR/portal/settings/webhooks).

## Como se inscrever

1. Navegue até **Aplicações > Webhooks** no seu portal.
2. Crie um webhook e selecione os eventos que deseja receber — ou deixe a lista vazia para receber todos os eventos.
3. A Wink envia um HTTP POST assinado para seu endpoint toda vez que o evento ocorre. Veja [Integração Webhook](/pt-BR/developers/webhook-integration) para o envelope, cabeçalhos, verificação de assinatura e tentativas.

## Todos os tipos de evento

Esta tabela é gerada a partir do catálogo de eventos da plataforma no momento da build, então ela sempre corresponde à API.
Tipos de eventos marcados como entregues linkam para sua página de referência na [API de Webhooks](/pt-BR/api/webhooks/), que
documenta o corpo JSON exato (`WebhookEnvelope` com um `object` específico do evento), os cabeçalhos `Wink-*` e
a política de tentativas. Tipos de eventos que não são entregues existem internamente (pagamentos, razão contábil, sincronização de channel-manager, …) mas atualmente não são enviados aos assinantes webhook.

**Destinatários** indica quem recebe um evento: `SUPPLIER` (o hotel), `AFFILIATE` (um revendedor com um canal de vendas ativo para o hotel), `AGENT` (a agência de viagens que fez a reserva) ou `SELF` (a entidade gestora sobre a qual o evento trata). Cada destinatário recebe a mesma estrutura de carga útil; detalhes específicos do papel estão por trás da URL `links.self` da carga útil.

<WebhookEventTypes
  types={webhooks['x-wink-event-types']}
  referenceBase="/api/webhooks"
  eventHeader="Evento"
  descriptionHeader="Descrição"
  deliveredHeader="Entregue"
  recipientsHeader="Destinatários"
  deliveredLabel="entregue"
  notDeliveredLabel="não entregue"
  noRecipientsLabel="—"
/>

:::tip
Comece se inscrevendo em todos os eventos durante o desenvolvimento. Quando entender quais cargas úteis seu sistema precisa, reduza suas inscrições para diminuir o ruído. Envie para si mesmo um evento de teste em **Aplicações > Webhooks** (ou `POST …/webhook/{id}/test`) para confirmar seu endpoint e a verificação de assinatura antes de entrar em produção.
:::

## Leitura adicional

- [Guia de Integração Webhook](/pt-BR/developers/webhook-integration) — Passos de configuração e melhores práticas.
- [Webhooks](/pt-BR/portal/settings/webhooks) — Gerencie suas inscrições de webhook.
- [Aplicações](/pt-BR/account/applications) — Gerencie credenciais de API.
- [Desenvolva na Wink](/pt-BR/developers/build-on-wink) — Visão geral da plataforma para desenvolvedores.
