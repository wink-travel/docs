---
title: Componentes Web
description: Wink mantém uma biblioteca de Componentes Web em JavaScript que você pode incorporar ao seu site.
sidebar:
  order: 6
---


import { Steps } from '@astrojs/starlight/components';
import CloudinaryImage from '/src/components/cloudinary-image.astro';

[Componentes Web](https://developer.mozilla.org/en-US/docs/Web/API/Web_components) é um padrão que permite integrar recursos inteiros em Javascript ao seu site sem precisar saber programar.
Graças a essa tecnologia incrível, você pode incorporar o inventário de viagens Wink com muito pouca complicação. Este artigo guia você pela nossa coleção de componentes web e mostra como usá-los.

## Incorporar Wink

Há três coisas que você deve incluir em qualquer página onde quiser exibir um de nossos componentes web.

1. Incluir nossa folha de estilos.
2. Incluir nosso Javascript.
3. Incluir nosso carregador de aplicação.

### Folha de estilos

Inclua nosso CSS no cabeçalho do seu documento.

```html {3}
<html>
  <head>
    <link rel="stylesheet" href="https://elements.wink.travel/styles.css"></link>
  </head>
</html>
```

### Javascript

Inclua nosso Javascript no final do seu documento. (*Recomendamos logo acima da tag de fechamento do body*).

```html {3}
<html>
  <body>
    <script src="https://elements.wink.travel/elements.js" type="module" defer></script>
  </body>
</html>
```

Seu site agora está pronto e capaz de exibir nossos componentes web.

## Componentes

Leia sobre nossa biblioteca de componentes abaixo.

### Carregador de aplicação

O carregador é responsável por manter o estado e gerenciar a interação entre nossos componentes. (*Inclua-o abaixo do nosso Javascript*).

```html {3-6}
<html>
  <body>
    <wink-app-loader
      client-id="SEU CLIENT ID AQUI"
      configuration-id="SEU ID DE CUSTOMIZAÇÃO AQUI"
    ></wink-app-loader>
  </body>
</html>
```

### Carregador de conteúdo

O carregador de conteúdo está no coração da nossa biblioteca de componentes web. Ele é responsável por exibir seu inventário (cartões, grades, mapas).

Atributos disponíveis:

- layout
- id
- sort

*O atributo `sort` está disponível apenas quando o layout é `RANKED` e você não quer usar uma grade ranqueada existente. Nesses casos, deixe `id` vazio.*

Tipos de layout disponíveis:

- `AD_BANNER`
- `MAP`
- `HOTEL`
- `GUEST_ROOM`
- `MEETING_ROOM`
- `SPA`
- `RESTAURANT`
- `ACTIVITY`
- `ATTRACTION`
- `PLACE`
- `ADD_ON`
- `LIST`
- `SEARCH`
- `RANKED`

Tipos de ordenação disponíveis:

- `MEMBER`
- `PRICE_LOW_TO_HIGH`
- `PRICE_HIGH_TO_LOW`
- `PRICE`
- `POPULARITY`
- `ECO`
- `EXPERIENCE`
- `PERK`
- `LOYALTY`
- `PACKAGE`

```html {3-6}
<html>
  <body>
    <wink-content-loader
       layout="GUEST_ROOM"
       id="2de7ee9c-61c9-11ef-9722-42004e494300"
    ></wink-content-loader>
  </body>
</html>
```

Exemplos podem ser encontrados para [cartões](/studio/cards), [grades](/studio/grids) e [mapas](/studio/maps).

### Busca

O componente de busca funciona muito bem junto com uma de suas grades ranqueadas. Ele permite que seus usuários pesquisem por hotel e destino e a grade se atualiza em resposta ao resultado da busca.

```html {3}
<html>
  <body>
    <wink-lookup></wink-lookup>
  </body>
</html>
```

<figure>
<CloudinaryImage
      width="350"
      height="57"
      resize="scale"
      quality="best"
      alt="Componente web de busca"
      src="academy/lookup"
      errorImage="noimage_opaque_nyrtl0.png"
    />
    <figcaption>*Componente web de busca*</figcaption>
</figure>

Clicar no componente acima abre um modal que permite digitar o destino ou hotel que você está procurando.

<figure>
<CloudinaryImage
      width="700"
      height="186"
      resize="scale"
      quality="best"
      alt="Modal de busca"
      src="academy/lookup-results"
      errorImage="noimage_opaque_nyrtl0.png"
    />
    <figcaption>*Modal de busca com resultados*</figcaption>
</figure>

### Itinerário

O botão de itinerário mostra o itinerário atual no botão. Quando você clica nele, o seletor completo de itinerário é exibido como um modal.

```html {3}
<html>
  <body>
    <wink-itinerary></wink-itinerary>
  </body>
</html>
```

<figure>
<CloudinaryImage
      width="350"
      height="60"
      resize="scale"
      quality="best"
      alt="Componente web de itinerário"
      src="academy/itinerary-button"
      errorImage="noimage_opaque_nyrtl0.png"
    />
    <figcaption>*Componente web de itinerário*</figcaption>
</figure>

Clicar no botão acima abre um modal que permite atualizar seu itinerário.

<figure>
<CloudinaryImage
      width="700"
      height="459"
      resize="scale"
      quality="best"
      alt="Modal de busca"
      src="academy/search-modal"
      errorImage="noimage_opaque_nyrtl0.png"
    />
    <figcaption>*Seletor de itinerário como modal*</figcaption>
</figure>


### Busca (Search)

Busca é um botão apenas com ícone do seletor de itinerário. Quando você clica nele, o seletor completo de itinerário é exibido como um modal.

```html {3}
<html>
  <body>
    <wink-search></wink-search>
  </body>
</html>
```

<figure>
<CloudinaryImage
      width="138"
      height="116"
      resize="scale"
      quality="best"
      alt="Componente web de busca"
      src="academy/search-button"
      errorImage="noimage_opaque_nyrtl0.png"
    />
    <figcaption>*Componente web de busca*</figcaption>
</figure>

Clicar no botão acima abre um modal que permite atualizar seu itinerário.

<figure>
<CloudinaryImage
      width="700"
      height="459"
      resize="scale"
      quality="best"
      alt="Modal de busca"
      src="academy/search-modal"
      errorImage="noimage_opaque_nyrtl0.png"
    />
    <figcaption>*Seletor de itinerário como modal*</figcaption>
</figure>

### Formulário de Itinerário

O componente de itinerário exibe um formulário de itinerário com o qual os usuários podem interagir.

```html {3}
<html>
  <body>
    <wink-itinerary-form></wink-itinerary-form>
  </body>
</html>
```

<figure>
<CloudinaryImage
      width="700"
      height="90"
      resize="scale"
      quality="best"
      alt="Componente web de formulário de itinerário"
      src="academy/itinerary-form"
      errorImage="noimage_opaque_nyrtl0.png"
    />
    <figcaption>*Componente web de formulário de itinerário*</figcaption>
</figure>

Ao alterar o itinerário, em qualquer um dos nossos componentes de itinerário, dispara o evento de atualização do itinerário para qualquer inventário que você tenha exibido na página.

### Conta

O botão de conta permite que você adicione autenticação Wink ao seu site.

```html {3}
<html>
  <body>
    <wink-account></wink-account>
  </body>
</html>
```

<figure>
<CloudinaryImage
      width="92"
      height="104"
      resize="scale"
      quality="best"
      alt="Componente web do botão de conta"
      src="academy/account-button"
      errorImage="noimage_opaque_nyrtl0.png"
    />
    <figcaption>*Componente web do botão de conta*</figcaption>
</figure>

Clicar no botão, quando o usuário não está autenticado, encaminha o usuário para autenticação.
Quando o usuário está autenticado, exibe o ícone do perfil do usuário.

<figure>
<CloudinaryImage
      width="92"
      height="104"
      resize="scale"
      quality="best"
      alt="Botão de conta quando autenticado"
      src="academy/account-button-auth"
      errorImage="noimage_opaque_nyrtl0.png"
    />
    <figcaption>*Botão de conta (autenticado) componente web*</figcaption>
</figure>

Ao clicar no botão, abre o menu suspenso específico do usuário.

<figure>
<CloudinaryImage
      width="350"
      height="404"
      resize="scale"
      quality="best"
      alt="Botão de conta quando autenticado"
      src="academy/account-button-auth-open"
      errorImage="noimage_opaque_nyrtl0.png"
    />
    <figcaption>*Botão de conta (aberto) componente web*</figcaption>
</figure>

## Incorporar TripPay

Há duas coisas que você deve incluir em qualquer página onde quiser usar o componente web de pagamento TripPay.

1. Incluir nossa folha de estilos.
2. Incluir nosso Javascript.

### Folha de estilos

Inclua nosso CSS no cabeçalho do seu documento.

```html {3}
<html>
  <head>
    <link rel="stylesheet" href="https://elements.trippay.io/styles.css"></link>
  </head>
</html>
```

### Javascript

Inclua nosso Javascript no final do seu documento. (*Recomendamos logo acima da tag de fechamento do body*).

```html {3}
<html>
  <body>
    <script src="https://elements.trippay.io/elements.js" type="module" defer></script>
  </body>
</html>
```

Seu site agora está pronto e capaz de exibir nossos componentes web.

## Componentes

### Pagamento

O componente de pagamento permite informar ao TripPay que um viajante deseja comprar algo e as regras e preços desses itens.

Há um atributo obrigatório para o widget:

- `id` O identificador do contrato de reserva que você deseja executar.

```html {3}
<html>
  <body>
    <trip-pay id="<INSERT_BOOKING_CONTRACT_ID>"></trip-pay>
  </body>
</html>
```

:::note
Para aprender como gerar o `id` do contrato de reserva, leia o guia: [Integrando com TripPay](/guides/integrating-with-trip-pay).
:::

O widget prepara o contrato para execução e exibe os detalhes do pagamento *(Figura 1)* para o usuário finalizar a reserva.

<figure>
<CloudinaryImage
      width="700"
      height="1141"
      resize="scale"
      quality="best"
      alt="Detalhes do pagamento"
      src="academy/payment-modal"
      errorImage="noimage_opaque_nyrtl0.png"
    />
    <figcaption>*Figura 1. Exemplo de formulário de pagamento*</figcaption>
</figure>

## Erros

Se você receber uma mensagem de erro ao incorporar um de nossos componentes web, seja no WinkLinks ou no seu próprio site, algumas coisas podem ter dado errado:

### Disponibilidade

O inventário pode não estar mais disponível. Vá para [Wink Studio](/studio/cards) e verifique se o status do "cartão" está disponível.
Se não estiver, aparecerá em vermelho. Nesse caso, você pode esperar e talvez o fornecedor o torne disponível novamente ou removê-lo da sua lista.

:::note
`Disponibilidade` não significa a disponibilidade diária do inventário para reserva. Significa que o inventário está completamente indisponível para você e/ou para qualquer pessoa.
:::

### Removido

O inventário pode ter sido removido. Vá para [Wink Studio](/studio/cards) e verifique se o status do "cartão" está disponível.
Se não estiver, aparecerá em vermelho. Nesse caso, você pode esperar e talvez o fornecedor o torne disponível novamente ou removê-lo da sua lista.

### Customização

Você pode ter removido acidentalmente a customização que definiu para acompanhar seu inventário. Certifique-se de que a customização ainda está disponível e defina uma nova se alguma estiver faltando.

### Aplicação

Isso é apenas para desenvolvedores web. Se você removeu acidentalmente a [Aplicação](/developer/applications), fazendo com que o client ID não esteja mais disponível, por favor crie uma nova aplicação e use o novo client ID para incorporar nossos Componentes Web.
