---
title: Webhookovi
description: Pretplatite se na događaje iz Wink i TripPay.
sidebar:
  order: 11
---

import { Steps } from '@astrojs/starlight/components';
import CloudinaryImage from '/src/components/cloudinary-image.astro';

Webhookovi su način da primate i reagujete na događaje u realnom vremenu iz Wink za vaš nalog.

<Steps>
 1. Izaberite nalog sa kojim želite da radite sa [Accounts](https://app.wink.travel).
 2. Kliknite na `Actions` na dnu kartice naloga i izaberite `Manage account`.
 3. Kliknite na karticu `Webhooks` na sledećoj stranici.
 4. Ovaj odeljak će prikazati sve vaše webhookove.
 5. Izaberite da izmenite postojeći ili kreirate novi.
 </Steps>

## Primer

<Steps>
1. Desi se rezervacija na Wink.
1. Događaj iz Wink `booking.created` se šalje.
2. Vaš sistem aktivno sluša ovaj događaj.
3. Vaš sistem prati sopstveni skup analitika i koristi ovaj događaj za ažuriranje svoje vremenske baze podataka.
</Steps>

<figure>
<CloudinaryImage
      width="350"
      height="215"
      resize="scale"
      quality="best"
      alt="Primer webhooka"
      src="academy/webhook"
      errorImage="noimage_opaque_nyrtl0.png"
    />
    <figcaption>*Primer unosa webhooka*</figcaption>
</figure>

## Kreiranje webhooka

:::caution
Pre nego što kreirate webhook, potrebno je da napravite affiliate nalog na Wink ili nalog na TripPay jer ćete povezivati webhook sa određenim nalogom.
:::

Evo koraka za kreiranje webhooka:

<Steps>
1. Na stranici webhookova kliknite na dugme `Create new webhook`.
2. ***Ime*** Dajte webhooku ime. *npr. Wink Stalker*
3. ***Omogući*** Ostavite kako jeste da bude omogućen.
4. **Ime entiteta** Povežite webhook sa jednim od vaših postojećih naloga. *npr. Cool Account*
5. **Događaji** Slušajte događaje koji vas interesuju. *npr. `booking.created`*
6. **Webhook URL** Unesite URL na kojem ćete slušati događaje od nas. *npr. `https://my.system.com/webhook/wink`*
7. Kliknite na dugme `Save` da nastavite.
</Steps>

Bićete preusmereni nazad na listu vaših webhookova i vaš novi webhook je spreman za korišćenje.

## Ažuriranje webhooka

Potrebno je da ažurirate vašu aplikaciju ako želite da:

- Onemogućite webhook.
- Slušate više / manje događaja.
- Promenite URL na kojem slušate webhook.

Da biste ažurirali postojeći webhook, uradite sledeće:

<Steps>
1. Kliknite na link `Actions` za webhook koji želite da ažurirate.
2. Kliknite na dugme `Update` ispod Actions.
3. Napravite izmene na webhooku.
4. Kliknite na dugme `Save` da nastavite.
</Steps>

Bićete preusmereni nazad na listu vaših webhookova.

## Uklanjanje webhooka

Ako vam webhook više nije potreban, možete ga ukloniti.

Da uklonite webhook, uradite sledeće:

<Steps>
1. Kliknite na link `Actions` za webhook koji želite da uklonite.
2. Kliknite na dugme `Remove` ispod Actions.
3. Kliknite na dugme `OK` da potvrdite brisanje.
</Steps>

Bićete preusmereni nazad na listu vaših webhookova.
