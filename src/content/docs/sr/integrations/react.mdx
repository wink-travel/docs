---
title: React
description: Koristite Wink web komponente u bilo kojoj React aplikaciji sa punom podrškom za TypeScript preko @wink/elements.
sidebar:
  order: 15
---

import { Aside, LinkCard } from '@astrojs/starlight/components';

React 19+ ima punu podršku za custom elemente. React 18 i ranije verzije prosleđuju samo string atribute — koristite donji tipizovani pomoćni obrazac da biste ostali sigurni kroz verzije.

<LinkCard
  title="@wink/elements npm paket"
  href="/integrations/npm"
  description="Instalirajte i učitajte CDN paket sa TypeScript tipovima."
/>

## Instalacija

```bash
npm install @wink/elements
```

## Učitajte jednom u korenu aplikacije

Pozovite `load()` u vašoj komponenti najvišeg nivoa. Funkcija je idempotentna — bezbedno je pozivati je više puta.

```tsx
// src/App.tsx
import { useEffect } from 'react';
import { load } from '@wink/elements';

export default function App() {
  useEffect(() => {
    load({ clientId: import.meta.env.VITE_WINK_CLIENT_ID });
  }, []);

  return <YourRoutes />;
}
```

## Koristite komponente u JSX-u

```tsx
export default function HotelPage() {
  return (
    <main>
      <wink-content-loader layout="HOTEL" id="YOUR_LAYOUT_ID" />
      <wink-lookup />
    </main>
  );
}
```

## TypeScript — deklarisanje JSX intrinzičnih elemenata

React po defaultu ne prepoznaje nazive custom elemenata. Dodajte deklaracioni fajl jednom da biste uklonili greške tipa:

```ts
// src/wink-elements.d.ts
import type {
  WinkContentLoaderAttributes,
  WinkLookupAttributes,
  WinkSearchButtonAttributes,
  WinkAccountButtonAttributes,
  WinkItineraryButtonAttributes,
  WinkShoppingCartButtonAttributes,
  WinkAppLoaderAttributes,
} from '@wink/elements';

declare module 'react' {
  namespace JSX {
    interface IntrinsicElements {
      'wink-content-loader': WinkContentLoaderAttributes & React.HTMLAttributes<HTMLElement>;
      'wink-lookup': WinkLookupAttributes & React.HTMLAttributes<HTMLElement>;
      'wink-search-button': WinkSearchButtonAttributes & React.HTMLAttributes<HTMLElement>;
      'wink-account-button': WinkAccountButtonAttributes & React.HTMLAttributes<HTMLElement>;
      'wink-itinerary-button': WinkItineraryButtonAttributes & React.HTMLAttributes<HTMLElement>;
      'wink-shopping-cart-button': WinkShoppingCartButtonAttributes & React.HTMLAttributes<HTMLElement>;
      'wink-app-loader': WinkAppLoaderAttributes & React.HTMLAttributes<HTMLElement>;
    }
  }
}
```

<Aside type="caution" title="Ograničenje atributa u React 18">
React 18 i ranije verzije serijalizuju sve JSX propse u stringove pre nego što ih proslede DOM-u. Boolean i objekat propse morate ručno konvertovati. React 19 prosleđuje ne-string propse direktno custom elementima — nije potrebna nikakva zaobilaznica.
</Aside>

## Prosleđivanje tipizovanih atributa (sigurno za React 18)

```tsx
import type { WinkContentLoaderAttributes } from '@wink/elements';

const attrs: WinkContentLoaderAttributes = {
  layout: 'HOTEL',
  id: 'abc123',
  sort: 'POPULARITY',
};

// Spread radi u React 19; za React 18 su sve vrednosti ionako stringovi
export default function HotelCard() {
  return <wink-content-loader layout={attrs.layout} id={attrs.id} sort={attrs.sort} />;
}
```

## Dalje čitanje

- [Reference za Web Components](/sr/integrations/web-components)
- [@wink/elements npm paket](/sr/integrations/npm)
- [Next.js integracija](/sr/integrations/nextjs)
