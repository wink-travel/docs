---
title: Authentifizierung
description: Erfahren Sie, wie Sie sich mit dem OAuth2-Autorisierungsserver von Wink authentifizieren.
sidebar:
  order: 2
---

:::tip
Der Wink IAM-Authentifizierungsserver unterstützt die gängigen OAuth2-Endpunkte:

- /oauth2/authorize
- /oauth2/token
- /oauth2/introspect
- /oauth2/revoke
- /oauth2/userinfo
:::

Eine Anwendung stellt Ihnen eine `Client ID` und einen `Secret Key` zur Verfügung, die Sie benötigen, um eine authentifizierte OAuth2-Sitzung zu erstellen, mit der Sie mit den Wink- und TripPay-APIs kommunizieren können.

Hier sind die Schritte, um eine authentifizierte Sitzung zu erstellen.

**Schritt 1**. Rufen Sie ein Zugriffstoken in der Staging- oder Produktionsumgebung ab:

Wahrscheinlich arbeiten Sie mit einer robusten OAuth2-Bibliothek für Ihre Programmiersprache, die die ganze Arbeit für Sie übernimmt.
Unsere Beispiele zeigen die einfachste Verwendung über die Befehlszeile mit `curl`.

### Staging

```bash

curl -X POST https://staging-iam.wink.travel/oauth2/token \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "grant_type=client_credentials" \
  -d "client_id=<YOUR_CLIENT_ID>" \
  -d "client_secret=<YOUR_SECRET_KEY>"

```

### Produktion

```bash

curl -X POST https://iam.wink.travel/oauth2/token \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "grant_type=client_credentials" \
  -d "client_id=<YOUR_CLIENT_ID>" \
  -d "client_secret=<YOUR_SECRET_KEY>"

```

Dies gibt ein Zugriffstoken zusammen mit weiteren OAuth2-Antwortdaten zurück:

```json
{
  "access_token": "abc123"
}
```

Wenn Sie einen Aufruf an einen unserer Endpunkte machen, fügen Sie im Header Folgendes hinzu:

- `Wink-Version` = `2.0` Neueste - *Siehe unsere API-Dokumentation für weitere verfügbare Versionen.*
- `Authentication` = `Bearer: ${access_token}` *Fügen Sie Ihr Zugriffstoken ein.*

:::tip
Sie können alle unsere APIs testen, ohne eine einzige Zeile Code zu schreiben, bevor Sie starten.

Holen Sie sich eine 'Client ID' und einen 'Secret Key' von einer Ihrer Apps und navigieren Sie zu einer unserer [APIs](/de/developers/apis).
:::
