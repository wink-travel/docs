---
title: Svelte / SvelteKit
description: استخدم مكونات الويب من Wink في تطبيقات Svelte و SvelteKit مع onMount وحماية المتصفح.
sidebar:
  order: 20
---

import { Aside, LinkCard } from '@astrojs/starlight/components';

يمرر Svelte السمات مباشرة إلى DOM بدون طبقة DOM افتراضية، مما يجعل العناصر المخصصة مناسبة بشكل طبيعي. بالنسبة لـ SvelteKit (SSR)، قم بتحميل CDN داخل `onMount` أو احمِ ذلك باستخدام فحص `browser`.

<LinkCard
  title="@wink/elements npm package"
  href="/integrations/npm"
  description="قم بتثبيت وتحميل حزمة CDN مع أنواع TypeScript."
/>

## التثبيت

```bash
npm install @wink/elements
```

## SvelteKit — التخطيط الجذري

```ts
// src/routes/+layout.ts
import { browser } from '$app/environment';
import { load as loadWink } from '@wink/elements';

export function load() {
  if (browser) {
    loadWink({ clientId: import.meta.env.PUBLIC_WINK_CLIENT_ID });
  }
}
```

بدلاً من ذلك، قم بالتحميل داخل `onMount` في `+layout.svelte`:

```svelte
<!-- src/routes/+layout.svelte -->
<script>
  import { onMount } from 'svelte';
  import { load } from '@wink/elements';

  onMount(() => {
    load({ clientId: import.meta.env.PUBLIC_WINK_CLIENT_ID });
  });
</script>

<slot />
```

## الاستخدام في المكونات

يمرر Svelte جميع السمات غير المعروفة مباشرة إلى DOM، لذلك لا حاجة لأي إعداد إضافي:

```svelte
<script>
  export let layoutId = 'YOUR_LAYOUT_ID';
</script>

<main>
  <wink-content-loader layout="HOTEL" id={layoutId} />
  <wink-lookup />
</main>
```

## Svelte 5 (تركيب runes)

```svelte
<script lang="ts">
  import { onMount } from 'svelte';
  import { load } from '@wink/elements';

  let { layoutId }: { layoutId: string } = $props();

  onMount(() => {
    load({ clientId: import.meta.env.PUBLIC_WINK_CLIENT_ID });
  });
</script>

<wink-content-loader layout="HOTEL" id={layoutId} />
```

## متغيرات البيئة

يكشف SvelteKit فقط عن المتغيرات التي تبدأ بـ `PUBLIC_` للمتصفح:

```
# .env
PUBLIC_WINK_CLIENT_ID=your-client-id
```

<Aside type="caution" title="التصيير على الخادم (SSR)">
لا تقم أبدًا بعرض عناصر `<wink-*>` أثناء التصيير على جانب الخادم — حيث يتم تسجيل العناصر المخصصة في سجل `customElements` الخاص بالمتصفح والذي لا يوجد على الخادم. قم بالتغليف داخل `{#if browser}` أو استدعِ `load()` فقط داخل `onMount`.
</Aside>

```svelte
<script>
  import { browser } from '$app/environment';
</script>

{#if browser}
  <wink-content-loader layout="HOTEL" id="YOUR_LAYOUT_ID" />
{/if}
```

## Svelte العادي (Vite، بدون SvelteKit)

```svelte
<!-- src/App.svelte -->
<script>
  import { onMount } from 'svelte';
  import { load } from '@wink/elements';

  onMount(() => {
    load({ clientId: import.meta.env.VITE_WINK_CLIENT_ID });
  });
</script>

<wink-content-loader layout="HOTEL" id="YOUR_LAYOUT_ID" />
```

## قراءة إضافية

- [مرجع مكونات الويب](/ar/integrations/web-components)
- [حزمة npm @wink/elements](/ar/integrations/npm)
