---
title: MCP serveri
description: Savienojiet AI aģentus ar Wink platformu, izmantojot Model Context Protocol. Autentificētie MCP serveri nodrošina jūsu aģentam tiešu piekļuvi inventāram, rezervācijām, kanālu pārvaldnieku integrācijām un citām funkcijām — aizsargāti ar interaktīvu OAuth2 pieteikšanos.
sidebar:
  order: 10
---

import { Steps, LinkButton, Aside, Tabs, TabItem } from '@astrojs/starlight/components';

Wink piedāvā divus autentificētus MCP serverus. Abi izmanto **interaktīvu OAuth2 pieteikšanos** (PKCE autorizācijas koda plūsma) — jūsu AI aģents atver pārlūkprogrammas cilni, lai jūs varētu pieteikties ar savu Wink kontu. Nav jāuztur klienta noslēpumi.

| Serveris | URL | Ko tas dara |
|--------|-----|--------------|
| **Inventory API** | `https://api.wink.travel/mcp` | Inventārs, rezervācijas, cenas, pārdošanas kanāli, analītika un visas platformas operācijas |
| **Integrations** | `https://integrations.wink.travel/mcp` | Kanālu pārvaldnieku integrācijas — sinhronizē īpašumus, cenas un pieejamību ar OTA |

---

## Kā darbojas autentifikācija

Wink MCP serveri izmanto **OAuth2 autorizācijas koda + PKCE** plūsmu un atbalsta **dinamisko klienta reģistrāciju (RFC 7591)**. Šeit ir, kas notiek, kad pieslēdzaties pirmo reizi:

1. Jūsu AI klients atrod autorizācijas serveri no MCP gala punkta `/.well-known/oauth-protected-resource` metadatiem.
2. Klients pats reģistrējas, izmantojot **dinamisko klienta reģistrāciju** — nav nepieciešama manuāla lietotnes iestatīšana.
3. Klients atver **pārlūkprogrammas cilni** uz `https://iam.wink.travel`. Jūs piesakāties ar savu Wink kontu un apstiprināt pieprasītās atļaujas piekrišanas ekrānā.
4. Wink izsniedz **piekļuves tokenu**, kas ir saistīts ar jūsu kontu un identitāti.
5. AI klients pievieno tokenu katram MCP pieprasījumam. Visi rīku rezultāti automātiski tiek ierobežoti jūsu pārvaldības vienībai.
6. Tokeni beidzas pēc 3 stundām. Jūsu AI klients tos klusējot atjauno fonā bez atkārtotas prasības pieteikties.

Katrs rīka izsaukums tiek veikts **jūsu identitātes vārdā** — tās pašas ACL tiesību noteikumi, kas aizsargā tīmekļa portālu, attiecas arī uz AI aģentu.

---

## Savienojiet savu AI klientu

<Tabs>
  <TabItem label="Claude Code">

<Steps>
1. Pārliecinieties, ka Claude Code ir atjaunināts:
   ```bash
   npm install -g @anthropic-ai/claude-code
   ```

2. Pievienojiet Wink Inventory MCP serveri:
   ```bash
   claude mcp add wink-inventory https://api.wink.travel/mcp --transport http
   ```

3. Claude Code sazinās ar serveri, atrod autorizācijas serveri un atver jūsu noklusējuma pārlūkprogrammu.

4. **Piesakieties Wink** un apstipriniet atļauju pieprasījumu piekrišanas ekrānā.

5. Claude Code apstiprina savienojumu. Ierakstiet `/mcp`, lai pārbaudītu, vai serveris ir redzams un tā rīki ir ielādēti.
</Steps>

Lai pievienotu arī Integrations serveri:
```bash
claude mcp add wink-integrations https://integrations.wink.travel/mcp --transport http
```

:::tip[Dalieties ar savu komandu]
Pievienojiet `--scope project` pie `claude mcp add`, un Claude Code ierakstīs servera URL `.mcp.json` failā projekta saknē. Apstipriniet šo failu — katrs komandas loceklis pirmo reizi pieslēdzoties veic savu pārlūkprogrammas pieteikšanos.
:::

  </TabItem>
  <TabItem label="Claude Desktop">

<Steps>
1. Rediģējiet Claude Desktop konfigurācijas failu:
   - **macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`
   - **Windows:** `%APPDATA%\Claude\claude_desktop_config.json`
   - **Linux:** `~/.config/Claude/claude_desktop_config.json`

2. Pievienojiet MCP serveru ierakstus:

   ```json title="claude_desktop_config.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

3. **Pilnībā aizveriet un restartējiet** Claude Desktop (izmantojiet Quit no izvēlnes joslas — loga aizvēršana nav pietiekama).

4. Claude Desktop atver pārlūkprogrammas cilni katram jaunam serverim. Piesakieties Wink un apstipriniet atļaujas.

5. Apakšējā rīkjoslā spraudņa ikona rāda abus serverus kā pieslēgtus.
</Steps>

  </TabItem>
  <TabItem label="OpenAI Codex">

<Steps>
1. Pārliecinieties, ka Codex CLI ir instalēts:
   ```bash
   npm install -g @openai/codex
   ```

2. Atveriet (vai izveidojiet) Codex konfigurācijas failu:
   - **macOS / Linux:** `~/.codex/config.json`
   - **Windows:** `%USERPROFILE%\.codex\config.json`

3. Pievienojiet Wink MCP serveru ierakstus:

   ```json title="~/.codex/config.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

4. Sāciet Codex. Pirmo reizi lietojot, Codex atver pārlūkprogrammas cilni uz `https://iam.wink.travel`.

5. **Piesakieties Wink** un apstipriniet pieprasītās atļaujas.

6. Codex apstiprina savienojumu. Izpildiet `codex` un lūdziet uzskaitīt pieejamos rīkus, lai pārbaudītu, vai Wink serveri ir aktīvi.
</Steps>

:::note
Codex CLI MCP atbalsts prasa versiju **1.0** vai jaunāku. Pārbaudiet ar `codex --version` un atjauniniet ar `npm install -g @openai/codex`.
:::

  </TabItem>
  <TabItem label="ChatGPT">

MCP serveru atbalsts ir pieejams **ChatGPT darbvirsmas lietotnē** Plus, Pro, Team un Enterprise abonentiem.

<Steps>
1. Lejupielādējiet un instalējiet [ChatGPT darbvirsmas lietotni](https://openai.com/chatgpt/download/), ja vēl neesat to izdarījis.

2. Atveriet lietotni un noklikšķiniet uz **savā profila ikonas** augšējā labajā stūrī, pēc tam izvēlieties **Iestatījumi**.

3. Dodieties uz **Savienotāji** (vai **MCP serveri** atkarībā no lietotnes versijas) un noklikšķiniet **Pievienot**.

4. Ievadiet Wink Inventory servera URL:
   ```
   https://api.wink.travel/mcp
   ```
   Pēc tam noklikšķiniet **Savienot**.

5. ChatGPT atver pārlūkprogrammas cilni uz `https://iam.wink.travel`. **Piesakieties Wink** un apstipriniet atļaujas.

6. Atkārtojiet 4.–5. soli Integrations serverim:
   ```
   https://integrations.wink.travel/mcp
   ```

7. Abi serveri parādās jūsu Savienotāju sarakstā. Sāciet jaunu tērzēšanu — Wink rīki ir pieejami automātiski.
</Steps>

:::note[Web lietotnes ierobežojums]
MCP serverus var konfigurēt tikai **darbvirsmas lietotnē**. ChatGPT tīmekļa saskarne chatgpt.com neatbalsta pielāgotus MCP serverus.
:::

  </TabItem>
  <TabItem label="Cursor">

<Steps>
1. Atveriet (vai izveidojiet) Cursor MCP konfigurācijas failu:
   - **macOS / Linux:** `~/.cursor/mcp.json`
   - **Windows:** `%USERPROFILE%\.cursor\mcp.json`

2. Pievienojiet Wink MCP serveru ierakstus:

   ```json title="~/.cursor/mcp.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

3. Atveriet Cursor. Dodieties uz **Iestatījumi → Cursor iestatījumi → MCP** un pārliecinieties, ka abi serveri ir ar zaļu statusa indikatoru. Ja tie ir sarkani, noklikšķiniet **Atjaunot**.

4. Cursor atver pārlūkprogrammas cilni uz `https://iam.wink.travel`. **Piesakieties Wink** un apstipriniet atļaujas.

5. Atveriet tērzēšanu (`Ctrl+L` / `Cmd+L`) un lūdziet Cursor uzskaitīt pieejamos Wink rīkus, lai apstiprinātu savienojumu.
</Steps>

:::tip[Projekta mēroga konfigurācija]
Varat arī ievietot `mcp.json` failu projekta saknē, nevis `~/.cursor/`. Projekta līmeņa konfigurācija ir prioritāra un padara serverus pieejamus katram komandas loceklim, kurš atver projektu Cursor.
:::

  </TabItem>
  <TabItem label="Windsurf">

<Steps>
1. Atveriet (vai izveidojiet) Windsurf MCP konfigurācijas failu:
   - **macOS / Linux:** `~/.codeium/windsurf/mcp_config.json`
   - **Windows:** `%USERPROFILE%\.codeium\windsurf\mcp_config.json`

2. Pievienojiet Wink MCP serveru ierakstus:

   ```json title="~/.codeium/windsurf/mcp_config.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

3. **Pilnībā restartējiet Windsurf** (Fails → Iziet, pēc tam atveriet no jauna).

4. Dodieties uz **Iestatījumi → MCP serveri** un pārliecinieties, ka abi serveri ir pieslēgti. Windsurf pirmo reizi lietojot atver pārlūkprogrammas cilni uz `https://iam.wink.travel`.

5. **Piesakieties Wink** un apstipriniet atļaujas. Serveri iedegas zaļi iestatījumu panelī, kad autentifikācija ir veiksmīga.
</Steps>

:::note
Windsurf MCP atbalsts prasa versiju **1.4** vai jaunāku. Pārbaudiet caur **Palīdzība → Par Windsurf** un atjauniniet no Windsurf vietnes, ja nepieciešams.
:::

  </TabItem>
  <TabItem label="Cline (VS Code)">

Cline ir VS Code paplašinājums ar iebūvētu MCP serveru pārvaldnieku. Nav nepieciešama manuāla JSON rediģēšana.

<Steps>
1. Instalējiet Cline no VS Code Marketplace:
   - Atveriet VS Code un nospiediet `Ctrl+Shift+X` / `Cmd+Shift+X`
   - Meklējiet **Cline** un noklikšķiniet **Instalēt**

2. Noklikšķiniet uz **Cline ikonas** aktivitātes joslā (kreisajā sānjoslā), lai atvērtu Cline paneli.

3. Noklikšķiniet uz **MCP serveru** ikonas (spraudņa ikona) Cline rīkjoslā, pēc tam izvēlieties **Rediģēt MCP iestatījumus**.

4. VS Code atver `cline_mcp_settings.json`. Pievienojiet Wink ierakstus:

   ```json title="cline_mcp_settings.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

5. **Saglabājiet failu.** Cline automātiski pārlādējas un atver pārlūkprogrammas cilni uz `https://iam.wink.travel`.

6. **Piesakieties Wink** un apstipriniet atļaujas. Abi serveri parādās kā aktīvi MCP serveru panelī.
</Steps>

:::tip[Alternatīva: pievienot pēc URL]
MCP serveru panelī noklikšķiniet **Pievienot serveri** un ielīmējiet `https://api.wink.travel/mcp` tieši. Cline automātiski aizpilda konfigurāciju un veic autentifikāciju bez manuālas JSON rediģēšanas.
:::

  </TabItem>
</Tabs>

---

## Piekļuves tiesību atsauce

Apstiprinot piekrišanas ekrānu, redzēsiet, kādas tiesības aģents pieprasa. Wink grupē atļaujas pēc funkcionālās jomas:

| Tiesību grupa | Tiesības | Ko tās atver |
|-------------|--------|-----------------|
| **MCP transports** | `mcp.read` `mcp.write` `mcp.remove` | Nepieciešams, lai pieslēgtos jebkuram MCP serverim |
| **Konts** | `account.read` `account.write` `account.remove` | Pārvaldības vienības, komandas locekļi, iestatījumi, lietotnes |
| **Rezervācija** | `booking.read` `booking.write` `booking.remove` | Meklēšana, skatīšana, izveide, atcelšana un atmaksas |
| **Inventārs** | `inventory.read` `inventory.write` `inventory.remove` | Īpašumi, numuri, cenas, ērtības, pieejamība |
| **Analītika** | `analytics.read` | Rezervāciju analītika, ieņēmumu pārskati, līderu saraksti |
| **Mārketings** | `marketing.read` `marketing.write` `marketing.remove` | Sociālie ieraksti, kampaņas, pārdošanas kanāli, akcijas |
| **Saturs** | `content.read` `content.write` `content.remove` | Multivide, saīsinātās URL, sindicējamie vienumi, kartes |
| **Maksājums** | `payment.read` `payment.write` | Grāmatojums, izmaksas, finanšu sadalījumi, izņemšana |

AI klients pieprasa tikai nepieciešamās tiesības. Jūs varat noraidīt atsevišķas atļaujas piekrišanas ekrānā — rīki, kuriem nepieciešama noraidīta tiesība, izsauks kļūdu.

---

## Problēmu novēršana

**Pārlūkprogrammas cilne neatveras**

Jūsu AI klients var vēl neatbalstīt interaktīvo PKCE plūsmu. Pārbaudiet, vai izmantojat jaunāko versiju:
- **Claude Code:** `npm install -g @anthropic-ai/claude-code`
- **Codex CLI:** `npm install -g @openai/codex`
- **Claude Desktop / ChatGPT darbvirsmas lietotne:** atjauniniet, izmantojot lietotnes iebūvēto atjauninātāju

**`401 Unauthorized` pēc pieteikšanās**

Jūsu kešotais tokens var būt novecojis. Noņemiet un pievienojiet serveri atkārtoti, lai izraisītu jaunu pieteikšanos:

```bash
# Claude Code
claude mcp remove wink-inventory
claude mcp add wink-inventory https://api.wink.travel/mcp --transport http
```

Claude Desktop un ChatGPT noņemiet servera ierakstu iestatījumos, restartējiet lietotni un pievienojiet to no jauna.

**`403 Forbidden` izsaucot konkrētu rīku**

Rīks prasa tiesību, ko neapstiprinājāt piekrišanas ekrānā. Pieslēdziet serveri atkārtoti — piekrišanas ekrāns parādīsies vēlreiz, un varēsiet apstiprināt trūkstošo atļauju.

**Serveris parādās, bet rīka izsaukumi atgriež "account not found"**

Jūs esat pieteicies Wink kontā, kuram nav pārvaldības vienības. Dodieties uz [Wink Portal](https://app.wink.travel) un pārliecinieties, ka jūsu konts ir iestatīts pirms MCP servera lietošanas.

**Tokens beidzas sesijas vidū**

Tokeni ir derīgi 3 stundas. Claude Code un Claude Desktop klusējot atjauno tokenus bez pārtraukuma. Ja pēc ilgas neaktivitātes redzat autentifikācijas kļūdas, ierakstiet jaunu ziņu — klients automātiski pārreģistrēsies.

**Codex CLI rāda "MCP server unreachable"**

Pārbaudiet, vai jūsu `~/.codex/config.json` ir derīgs JSON (bez pēdējām komatiem) un `"type": "http"` ir iestatīts. Codex prasa HTTP transportu attāliem serveriem; stdio transports ir paredzēts tikai lokāliem binārajiem failiem.

**ChatGPT "Savienotāji" izvēlne nav redzama**

MCP atbalsts prasa ChatGPT darbvirsmas lietotnes versiju **1.2024.352** vai jaunāku un aktīvu Plus, Pro, Team vai Enterprise abonementu. Atjauniniet lietotni un pārbaudiet plānu sadaļā Iestatījumi → Konts.

**Cursor serveris rāda sarkanu / "Failed to connect"**

Pārbaudiet, vai jūsu `~/.cursor/mcp.json` ir derīgs JSON un satur `"type": "http"`. Pēc tam dodieties uz **Iestatījumi → Cursor iestatījumi → MCP** un noklikšķiniet **Atjaunot**. Ja kļūda saglabājas, pilnībā restartējiet Cursor — konfigurācijas izmaiņas ne vienmēr tiek uzreiz ņemtas vērā bez restartēšanas.

**Windsurf MCP serveri nav redzami iestatījumos**

Windsurf prasa pilnu restartu (ne tikai loga pārlādi), lai ielādētu izmaiņas `mcp_config.json`. Izmantojiet **Fails → Iziet** un palaidiet no jauna. Ja serveri joprojām nav redzami, pārliecinieties, ka izmantojat Windsurf **1.4+** caur **Palīdzība → Par Windsurf**.

**Cline "Pievienot serveri" opcija nav redzama**

MCP serveru pārvaldnieks prasa Cline versiju **2.0** vai jaunāku. Atveriet VS Code paplašinājumu paneli, atrodiet Cline un noklikšķiniet **Atjaunināt**, ja ir pieejams atjauninājums. Pēc atjaunināšanas pārlādējiet VS Code (`Ctrl+Shift+P` → "Developer: Reload Window").

---

<LinkButton href="https://app.wink.travel" variant="primary" icon="external">Atvērt Wink portālu</LinkButton>
<LinkButton href="https://github.com/wink-travel" variant="secondary" icon="external">Wink GitHub</LinkButton>
