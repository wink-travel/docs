---
title: Booking Engine — Checkout That Sells the Whole Stay
description: Wink Booking Engine turns hotel-approved supply into confirmed bookings across every route — with TripPay payment, booking sync and attribution built in.
template: splash
marketingNav: true
---
import '/src/styles/splash-page.css';
import BreadcrumbJsonLd from "@/components/marketing/BreadcrumbJsonLd.astro";

import DeviceFrame from "@/components/marketing/DeviceFrame.astro";
import ShowcaseRow from "@/components/marketing/ShowcaseRow.astro";
import FaqGrid from "@/components/marketing/FaqGrid.astro";
import ProductJsonLd from "@/components/marketing/ProductJsonLd.astro";

import IconArrowRight from "@tabler/icons/outline/arrow-right.svg";
import IconCheck from "@tabler/icons/outline/check.svg";

export const faqs = [
  { title: "What is Wink Booking Engine?", content: "Wink Booking Engine is the fulfillment, checkout and transaction layer of the Wink platform. When a guest clicks any booking path — a hotel website, link, QR code, Web Component, partner storefront, Traveliko, an Agentic AI answer or a developer app — it loads the right hotel-approved rooms, rates, offers and rules, sells the whole stay, takes payment, and confirms the booking to the guest, hotel and partner." },
  { title: "Can Booking Engine sell more than rooms?", content: "Yes. Where configured, it sells the whole stay around the room: rate plans, packages, add-ons, dining, spa, experiences, transfers, upgrades, early check-in and late checkout — not just the room night." },
  { title: "Which routes does Booking Engine fulfill?", content: "Every Wink route resolves into the same engine: hotel website, Shareable Links, QR codes, Web Components, WinkLinks storefronts, Social posts, partner storefronts, Traveliko, Agentic AI handoffs and developer-powered apps." },
  { title: "How does payment work?", content: "Payment runs through TripPay, Wink's booking payment gateway, powered by Stripe. TripPay handles the checkout UI, cancellation-policy logic, tokenized cards, 3DS, multi-currency and split payouts. It collects the full booking amount on behalf of the hotel; funds are released to the hotel at guest check-in, after Wink's fee. The all-in cost is 5.5% on confirmed direct bookings." },
  { title: "How is Booking Engine connected to Extranet?", content: "Extranet is where hotels manage content, rooms, rates, availability, promotions, policies and channel rules. Booking Engine uses that approved supply to display offers and complete checkout — two connected layers, never the same surface, so the hotel stays in control." },
  { title: "Can developers and AI agents create bookings?", content: "Yes. Booking creation is a REST API operation, every feature is callable externally with OAuth2, and a hosted MCP server lets AI agents read the live contracts. Subscribe to booking.created and 64+ webhook events, or embed bookable inventory with Web Components." },
  { title: "Does Booking Engine sync bookings to my PMS or channel manager?", content: "Yes. When bookings are created, updated or cancelled, they sync to the connected PMS or channel manager with full lifecycle tracking, and the hotel, traveler and (where relevant) partner receive confirmation." },
  { title: "Is Booking Engine the same as an OTA?", content: "No. It's the fulfillment and transaction layer — it never replaces the hotel's brand with an OTA brand. It helps hotel-controlled supply convert across hotel, partner, Traveliko, AI and developer routes, with attribution preserved." },
];

<div class="not-content mkt-page">

<ProductJsonLd
  name="Wink Booking Engine"
  url="https://wink.travel/products/booking-engine/"
  applicationCategory="BusinessApplication"
  description="The fulfillment, checkout and transaction layer of the Wink platform. Turns hotel-approved supply into confirmed bookings across every route, with TripPay payment, booking sync and attribution built in."
  features={["Checkout across website, links, QR codes, Web Components, WinkLinks, partner storefronts, Traveliko, Agentic AI and developer apps", "Sells rooms, rate plans, packages, add-ons, dining, spa, experiences and transfers", "Payment via TripPay (Stripe-powered)", "Booking creation over REST API with OAuth2", "64+ webhook events including booking.created", "Booking sync to PMS / channel manager", "Source and partner attribution preserved", "Guest, hotel and partner confirmation"]}
  faqs={faqs}
/>

<BreadcrumbJsonLd items={[{ name: "Home", url: "https://wink.travel/" }, { name: "Products" }, { name: "Booking Engine" }]} />

{/* Breadcrumb */}
<nav aria-label="Breadcrumb" class="mx-auto w-full max-w-7xl px-4 pt-6 text-sm text-muted-foreground">
  <a href="/" class="hover:text-foreground">Home</a>
  <span class="px-1.5">/</span>
  <span>Products</span>
  <span class="px-1.5">/</span>
  <span class="text-foreground">Booking Engine</span>
</nav>

{/* Hero — dark, flagship (composite desktop + floating phone) */}
<section class="relative w-full overflow-hidden border-b border-white/10 bg-gradient-to-br from-[#2a0f52] via-[#1f0a40] to-[#150629] text-white">
  <div class="pointer-events-none absolute inset-0" aria-hidden="true">
    <div class="absolute -left-24 -top-10 size-96 rounded-full bg-[#ff6b35]/20 blur-[120px]"></div>
    <div class="absolute -bottom-16 -right-16 size-[34rem] rounded-full bg-[#9333ea]/25 blur-[140px]"></div>
  </div>
  <div class="@container relative mx-auto w-full max-w-7xl px-4 py-20 @lg:py-24">
    <div class="grid items-center gap-12 @lg:grid-cols-2 @lg:gap-16">
      <div>
        <span class="inline-flex items-center gap-2 rounded-full bg-white/10 px-4 py-1.5 text-xs font-extrabold uppercase tracking-wider text-[#ff8a5c] ring-1 ring-white/15 backdrop-blur"><span class="size-1.5 rounded-full bg-gradient-to-r from-[#ff6b35] to-[#ec4899]"></span> Booking Engine</span>
        <h1 class="font-heading mt-6 text-4xl font-extrabold leading-[1.05] tracking-tight text-balance @lg:text-5xl @2xl:text-[3.25rem]">
          One checkout behind <span class="bg-gradient-to-r from-[#ff8a5c] via-[#ec4899] to-[#a855f7] bg-clip-text text-transparent">every booking.</span>
        </h1>
        <p class="mt-5 max-w-xl text-pretty text-lg text-white/75 @lg:text-xl">
          Wink Booking Engine turns hotel-approved supply into confirmed bookings — selling the room and the
          whole stay across websites, links, partner storefronts, Traveliko, AI agents and your own apps.
          Payment, confirmation and attribution included.
        </p>
        <div class="mt-8 flex flex-col gap-3 sm:flex-row">
          <a href="https://app.wink.travel" class="inline-flex h-12 items-center justify-center rounded-md bg-[#ff6b35] px-7 text-base font-semibold text-white transition-colors hover:bg-[#e85b29]">Get started</a>
          <a href="https://cal.com/wink-ws36dy" class="inline-flex h-12 items-center justify-center rounded-md bg-white/10 px-7 text-base font-medium text-white ring-1 ring-white/20 backdrop-blur transition-colors hover:bg-white/15">Request a demo</a>
        </div>
        <div class="mt-7 flex flex-wrap items-center gap-x-5 gap-y-2 text-sm text-white/65">
          <span class="inline-flex items-center gap-1.5"><IconCheck class="size-4 text-[#ff8a5c]" /> 5.5% all-in</span>
          <span class="inline-flex items-center gap-1.5"><IconCheck class="size-4 text-[#ff8a5c]" /> Payment via TripPay</span>
          <span class="inline-flex items-center gap-1.5"><IconCheck class="size-4 text-[#ff8a5c]" /> Built for mobile</span>
        </div>
      </div>

      <div class="relative">
        <DeviceFrame
          variant="browser"
          url="book.wink.travel/checkout"
          src="/mockups/booking-engine/checkout-page-booking-engine-desktop.webp"
          alt="Wink Booking Engine checkout — room, rate, add-ons and payment in one flow"
          aspect="16/10"
        />
        <div class="absolute -bottom-8 -right-1 w-[116px] @sm:w-[136px] @lg:w-[148px]">
          <DeviceFrame
            variant="phone"
            src="/mockups/booking-engine/checkout-mobile.webp"
            alt="Booking Engine mobile checkout"
            aspect="9/19.5"
            class="!max-w-none w-full"
          />
        </div>
      </div>
    </div>
  </div>
</section>

{/* What it is — direct answer + every route */}
<section class="@container mx-auto w-full max-w-4xl px-4 pt-24 text-center">
  <p class="mkt-kicker text-primary">What is Wink Booking Engine?</p>
  <p class="mkt-sub text-muted-foreground mx-auto mt-5 max-w-3xl">
    It's the fulfillment, checkout and transaction layer of the Wink platform. When a guest clicks any
    booking path, it loads the right hotel-approved rooms, rates and offers, sells the whole stay, takes
    payment through TripPay, and confirms the booking to the guest, hotel and partner — attribution
    preserved all the way through.
  </p>
  <div class="mx-auto mt-9 flex max-w-3xl flex-wrap justify-center gap-2.5">
    {["Hotel website", "Shareable Links", "QR codes", "Web Components", "WinkLinks", "Social", "Partner storefronts", "Traveliko", "Agentic AI", "Developer apps"].map((r) => (
      <span class="inline-flex items-center gap-2 rounded-full border border-border bg-card px-4 py-2 text-sm font-medium">
        <span class="size-1.5 rounded-full bg-primary" aria-hidden="true"></span>{r}
      </span>
    ))}
  </div>
  <p class="text-muted-foreground mx-auto mt-6 max-w-xl text-pretty text-sm">
    <a href="/products/extranet/" class="text-primary underline underline-offset-4">Extranet</a> sets the supply. Booking Engine converts it — two connected layers, so the hotel stays in control.
  </p>
</section>

{/* Sell the whole stay */}
<section class="@container mx-auto w-full max-w-7xl px-4 pt-24">
  <div class="grid items-center gap-12 @lg:grid-cols-2 @lg:gap-16">
    <div class="@lg:order-2">
      <p class="mkt-kicker text-primary">Sell the whole stay</p>
      <h2 class="mkt-h2 font-heading mt-3">More than a room night.</h2>
      <p class="text-muted-foreground mt-4 text-pretty @lg:text-lg">
        Most booking engines stop at the room. Wink merchandises the whole stay in the same flow — so the
        average booking is worth more and the guest arrives with the trip already built.
      </p>
      <div class="mt-6 flex flex-wrap gap-2.5">
        {["Rooms", "Rate plans", "Packages", "Add-ons", "Dining", "Spa & wellness", "Experiences", "Transfers", "Upgrades", "Early check-in", "Late checkout"].map((c) => (
          <span class="inline-flex items-center gap-2 rounded-full border border-border bg-card px-3.5 py-1.5 text-sm font-medium">
            <span class="size-1.5 rounded-full bg-primary" aria-hidden="true"></span>{c}
          </span>
        ))}
      </div>
      <ul class="mt-7 space-y-2.5">
        {["The correct hotel, room, rate and offer for the route the guest clicked", "Live availability and inventory rules from Extranet", "Cancellation and policy options shown up front"].map((p) => (
          <li class="flex items-start gap-2.5"><IconCheck class="mt-0.5 size-5 shrink-0 text-primary" /><span class="text-sm text-foreground/90">{p}</span></li>
        ))}
      </ul>
    </div>
    <div class="@lg:order-1">
      <DeviceFrame
        variant="browser"
        url="book.wink.travel/rate-plan"
        src="/mockups/booking-engine/rate-plan-add-on-desktop.webp"
        alt="Booking Engine rate plan and add-ons — selling the whole stay"
        aspect="16/10"
      />
    </div>
  </div>
</section>

{/* The booking flow */}
<section class="@container mx-auto w-full max-w-7xl px-4 pt-24">
  <div class="mx-auto mb-12 max-w-2xl text-center">
    <p class="mkt-kicker text-primary">The booking flow</p>
    <h2 class="mkt-h2 font-heading mt-3">From any route to a confirmed booking.</h2>
    <p class="mkt-sub text-muted-foreground mx-auto mt-4 max-w-xl">
      The same flow runs behind every Wink route — clean on desktop, built for mobile.
    </p>
  </div>

  <div class="space-y-20">
    <ShowcaseRow
      step="Step 01 · Choose a room"
      title="Rooms loaded from hotel-approved supply"
      desc="The right rooms, photos and availability load straight from Extranet for the route the guest clicked."
      points={["Live availability and inventory rules", "Hotel-controlled content and imagery", "Dates, guests and occupancy logic"]}
      image="/mockups/booking-engine/room-card-booking-engine-desktop.webp"
      alt="Booking Engine room selection with availability and pricing"
      url="book.wink.travel/rooms"
    />
    <ShowcaseRow
      reverse
      step="Step 02 · Rate plan & extras"
      title="Pick the rate, then add to the stay"
      desc="Rate plans, cancellation options and configured extras — packages, dining, spa, transfers — in the same flow."
      points={["Meal plans, member rates and promotions", "Add-ons and experiences where configured", "Policy and cancellation options up front"]}
      image="/mockups/booking-engine/rate-plan-page-booking-engine-desktop.webp"
      alt="Booking Engine rate plan selection with extras"
      url="book.wink.travel/rate-plan"
    />
    <ShowcaseRow
      step="Step 03 · Checkout & payment"
      title="One checkout, payment handled by TripPay"
      desc="The guest reviews the stay and pays in a single, hotel-branded checkout — payment processed through TripPay, powered by Stripe."
      points={["Hotel-branded, mobile-first checkout", "Payment via TripPay (Stripe-powered)", "Booking record tied to the hotel and route"]}
      image="/mockups/booking-engine/checkout-page-booking-engine-desktop.webp"
      alt="Booking Engine checkout and payment summary"
      url="book.wink.travel/checkout"
    />
    <ShowcaseRow
      reverse
      frame="plain"
      step="Step 04 · Confirmation"
      title="Confirmed to the guest, hotel and partner"
      desc="The booking is confirmed and stays connected to the route, campaign, channel or partner that created the demand — and syncs to the connected PMS or channel manager."
      points={["Instant guest confirmation", "Hotel and partner notified", "Synced to PMS / channel manager, attribution preserved"]}
      image="/mockups/booking-engine/booking-confirmation.webp"
      alt="Booking Engine confirmation screen with booking reference and stay summary"
    />
  </div>
</section>

{/* Value band — gradient (every route resolves here) */}
<section class="@container relative w-full overflow-hidden bg-gradient-to-br from-[#ff6b35] via-[#ec4899] to-[#9333ea] text-white">
  <div class="pointer-events-none absolute inset-0 opacity-20 mix-blend-overlay" aria-hidden="true">
    <div class="absolute -left-20 -top-16 size-96 rounded-full bg-white blur-[120px]"></div>
    <div class="absolute -bottom-20 -right-10 size-96 rounded-full bg-black blur-[130px]"></div>
  </div>
  <div class="relative mx-auto w-full max-w-6xl px-4 py-24">
    <div class="mx-auto mb-12 max-w-2xl text-center">
      <h2 class="mkt-h2 font-heading">Every route. One checkout the hotel controls.</h2>
      <p class="mkt-sub mx-auto mt-4 max-w-2xl text-white/85">Direct, partner, creator, social, consumer and AI demand all resolve in the same flow — payment, confirmation and attribution included.</p>
    </div>
    <div class="grid gap-5 @sm:grid-cols-2 @lg:grid-cols-4">
      {[
        { stat: "5.5%", label: "All-in per confirmed booking" },
        { stat: "Every route", label: "Direct, partner, social, consumer, AI" },
        { stat: "TripPay", label: "Checkout, payment & settlement" },
        { stat: "64+", label: "Webhook events for servicing" },
      ].map((s) => (
        <div class="rounded-2xl border border-white/20 bg-white/10 p-6 text-center backdrop-blur">
          <p class="font-heading text-2xl font-semibold @lg:text-3xl">{s.stat}</p>
          <p class="mt-2 text-sm text-white/80">{s.label}</p>
        </div>
      ))}
    </div>
  </div>
</section>

{/* Built for mobile — uniform phones */}
<section class="@container w-full border-y border-border bg-muted/30">
  <div class="mx-auto w-full max-w-7xl px-4 py-24">
    <div class="mx-auto mb-12 max-w-2xl text-center">
      <p class="mkt-kicker text-primary">Mobile-first</p>
      <h2 class="mkt-h2 font-heading mt-3">Designed for how guests actually book.</h2>
      <p class="mkt-sub text-muted-foreground mx-auto mt-4 max-w-xl">
        Most travel demand starts on a phone. Every step — discover, choose, add, pay — is built for it.
      </p>
    </div>
    <div class="grid grid-cols-2 justify-items-center gap-6 @md:grid-cols-4 @lg:gap-8">
      {[
        { src: "/mockups/booking-engine/hotel-card-mobile.webp", alt: "Mobile hotel card and availability", cap: "Discover" },
        { src: "/mockups/booking-engine/room-details-mobile.webp", alt: "Mobile room details", cap: "Choose" },
        { src: "/mockups/booking-engine/moble-add-on.webp", alt: "Mobile add-ons and extras", cap: "Add to the stay" },
        { src: "/mockups/booking-engine/checkout-mobile.webp", alt: "Mobile checkout and payment", cap: "Pay" },
      ].map((m) => (
        <div class="w-full max-w-[230px]">
          <DeviceFrame variant="phone" src={m.src} alt={m.alt} aspect="9/19.5" />
          <p class="mt-3 text-center text-sm font-medium text-muted-foreground">{m.cap}</p>
        </div>
      ))}
    </div>
  </div>
</section>

{/* How payment works — TripPay money flow */}
<section class="@container mx-auto w-full max-w-7xl px-4 pt-24">
  <div class="grid items-center gap-12 @lg:grid-cols-2 @lg:gap-16">
    <div>
      <p class="mkt-kicker text-primary">How payment works</p>
      <h2 class="mkt-h2 font-heading mt-3">Payment and payout, handled by TripPay.</h2>
      <p class="text-muted-foreground mt-4 text-pretty @lg:text-lg">
        TripPay is Wink's booking payment gateway, powered by Stripe. It runs checkout, policy and
        settlement so the hotel never wires up a processor — and it's included in one all-in rate.
      </p>
      <ul class="mt-6 space-y-3">
        {[
          "Tokenized cards, 3DS and multi-currency, powered by Stripe",
          "Cancellation, deposit and rate-plan policy enforced at checkout",
          "Split payouts and partner attribution settled together",
        ].map((p) => (
          <li class="flex items-start gap-2.5"><IconCheck class="mt-0.5 size-5 shrink-0 text-primary" /><span class="text-sm text-foreground/90">{p}</span></li>
        ))}
      </ul>
      <div class="mt-7 flex items-baseline gap-3 rounded-xl border border-border bg-card px-5 py-4">
        <span class="font-heading text-3xl font-bold text-foreground">5.5%</span>
        <span class="text-muted-foreground text-sm text-pretty">all-in on confirmed direct bookings — processing included, no separate processor contract. <a href="/pricing/" class="text-primary underline underline-offset-4">See pricing</a>.</span>
      </div>
    </div>
    {/* Money flow */}
    <div class="rounded-2xl border border-border bg-card p-6 shadow-sm @lg:p-8">
      <p class="text-xs font-semibold uppercase tracking-wider text-muted-foreground">How the money moves</p>
      <ol class="mt-5 space-y-5">
        {[
          { n: "1", t: "Guest pays in full at checkout", d: "The full booking amount is captured in the hotel-branded TripPay checkout." },
          { n: "2", t: "TripPay holds the funds", d: "Collected on behalf of the hotel, with policy and cancellation logic applied." },
          { n: "3", t: "Released at guest check-in", d: "Funds are paid out to the hotel at check-in, after Wink's all-in fee." },
          { n: "4", t: "Partner split settled", d: "Where a partner route created the booking, attribution and payout settle together." },
        ].map((s, i, arr) => (
          <li class="flex gap-4">
            <div class="flex flex-col items-center">
              <span class="flex size-8 shrink-0 items-center justify-center rounded-full bg-gradient-to-br from-[#ff6b35] to-[#9333ea] text-sm font-bold text-white">{s.n}</span>
              {i < arr.length - 1 && <span class="mt-1 w-px flex-1 bg-border" aria-hidden="true"></span>}
            </div>
            <div class="pb-1">
              <p class="font-heading text-sm font-semibold text-foreground">{s.t}</p>
              <p class="text-muted-foreground mt-1 text-sm text-pretty">{s.d}</p>
            </div>
          </li>
        ))}
      </ol>
    </div>
  </div>
</section>

{/* Developers & AI agents — teaser */}
<section class="@container w-full border-y border-border bg-muted/30">
  <div class="mx-auto w-full max-w-4xl px-4 py-24 text-center">
    <p class="mkt-kicker text-primary">Developers &amp; AI agents</p>
    <h2 class="mkt-h2 font-heading mt-3">The fulfillment layer, on tap.</h2>
    <p class="mkt-sub text-muted-foreground mx-auto mt-4 max-w-2xl">
      Every Agentic AI handoff and developer integration resolves in Booking Engine — booking creation over
      REST with OAuth2, 64+ webhook events, PMS and channel-manager sync, Web Components and a hosted MCP
      server agents can read.
    </p>
    <div class="mx-auto mt-8 flex max-w-2xl flex-wrap justify-center gap-2.5">
      {["Create bookings over REST", "OAuth2 + SDKs", "64+ webhook events", "Booking sync", "Web Components", "Hosted MCP server"].map((r) => (
        <span class="inline-flex items-center gap-2 rounded-full border border-border bg-card px-4 py-2 text-sm font-medium">
          <span class="size-1.5 rounded-full bg-primary" aria-hidden="true"></span>{r}
        </span>
      ))}
    </div>
    <div class="mt-9 flex flex-wrap justify-center gap-3">
      <a href="/developers/build-on-wink/" class="inline-flex items-center gap-1.5 rounded-md bg-primary px-5 py-2.5 text-sm font-semibold text-primary-foreground hover:bg-primary/90">Build on Wink <IconArrowRight class="size-4" /></a>
      <a href="/developers/mcp/" class="inline-flex items-center gap-1.5 rounded-md border border-border bg-card px-5 py-2.5 text-sm font-medium hover:bg-muted">MCP server</a>
      <a href="/agentic-ai/" class="inline-flex items-center gap-1.5 rounded-md border border-border bg-card px-5 py-2.5 text-sm font-medium hover:bg-muted">Agentic AI</a>
    </div>
  </div>
</section>

{/* Use cases — house accent cards (was AudienceCards) */}
<section class="@container mx-auto w-full max-w-7xl px-4 pt-24">
  <div class="mx-auto mb-12 max-w-2xl text-center">
    <p class="mkt-kicker text-primary">Who fulfills here</p>
    <h2 class="mkt-h2 font-heading mt-3">One checkout, three ways to use it.</h2>
  </div>
  <div class="grid gap-5 @md:grid-cols-3">
    {[
      { eyebrow: "For hotels", title: "Convert your direct demand", desc: "Power booking on your website, links, QR codes, Web Components, WinkLinks and Traveliko — sell the whole stay and keep the guest.", href: "/hotels/", cta: "Explore for hotels", accent: "#ff6b35" },
      { eyebrow: "For partners", title: "Fulfill the demand you create", desc: "Send guests from your links, storefronts and campaigns into a real checkout — with attribution attached through confirmation and payout.", href: "/partners/", cta: "Explore for partners", accent: "#ec4899" },
      { eyebrow: "For developers", title: "Build on hotel commerce", desc: "Use Booking Engine as the fulfillment layer behind search, booking, payment and confirmation — without rebuilding hotel checkout.", href: "/dev/", cta: "See documentation", accent: "#22d3ee" },
    ].map((c) => (
      <a href={c.href} class="group flex flex-col rounded-2xl border border-border bg-card p-7 shadow-sm transition-all hover:-translate-y-1 hover:shadow-lg" style={`border-top:3px solid ${c.accent}`}>
        <span class="text-xs font-bold uppercase tracking-wider" style={`color:${c.accent}`}>{c.eyebrow}</span>
        <h3 class="font-heading mt-2 text-lg font-semibold">{c.title}</h3>
        <p class="text-muted-foreground mt-2 flex-1 text-sm text-pretty">{c.desc}</p>
        <span class="mt-4 inline-flex items-center gap-1 text-sm font-semibold" style={`color:${c.accent}`}>{c.cta} <IconArrowRight class="size-4 transition-transform group-hover:translate-x-0.5" /></span>
      </a>
    ))}
  </div>
</section>

{/* FAQ */}
<FaqGrid
  heading="Booking Engine FAQs"
  description="Fulfillment, routes, payment and how to build on it."
  items={faqs}
/>

{/* Final CTA */}
<section class="w-full border-y border-white/10 bg-gradient-to-br from-[#2a0f52] via-[#1f0a40] to-[#150629] text-white">
  <div class="mx-auto w-full max-w-7xl px-4 py-24 text-center">
    <h2 class="mkt-h2 font-heading">Fulfill every booking route from one engine.</h2>
    <p class="mx-auto mt-4 max-w-2xl text-pretty text-white/70 @lg:text-lg">
      Turn hotel-approved supply into confirmed bookings across websites, links, QR codes, Web Components,
      partner storefronts, Traveliko, Agentic AI and developer-powered paths.
    </p>
    <div class="mt-10 flex flex-col items-center justify-center gap-3 sm:flex-row">
      <a href="https://app.wink.travel" class="inline-flex h-12 w-full items-center justify-center rounded-md bg-[#ff6b35] px-8 text-base font-semibold text-white hover:bg-[#e85b29] sm:w-auto">Get started</a>
      <a href="https://cal.com/wink-ws36dy" class="inline-flex h-12 w-full items-center justify-center rounded-md bg-white px-8 text-base font-semibold text-[#1f0a40] hover:bg-white/90 sm:w-auto">Request a demo</a>
    </div>
  </div>
</section>

</div>
