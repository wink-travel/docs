---
title: Preact
description: Използвайте Wink уеб компоненти в Preact приложения — същият модел като React с по-малък обем.
sidebar:
  order: 24
---

import { Aside, LinkCard } from '@astrojs/starlight/components';

API-то на Preact е съвместимо с React. Интеграцията следва същия модел — извикайте `load()` веднъж при стартиране на приложението, след което използвайте `<wink-*>` тагове в JSX.

<LinkCard
  title="@wink/elements npm package"
  href="/integrations/npm"
  description="Инсталирайте и заредете CDN пакета с TypeScript типове."
/>

## Инсталиране

```bash
npm install @wink/elements
```

## Заредете веднъж при стартиране на приложението

```tsx
// src/app.tsx
import { useEffect } from 'preact/hooks';
import { load } from '@wink/elements';

export function App() {
  useEffect(() => {
    load({ clientId: import.meta.env.VITE_WINK_CLIENT_ID });
  }, []);

  return <YourRoutes />;
}
```

## Използване в JSX

```tsx
// src/pages/hotels.tsx
export function HotelsPage() {
  return (
    <main>
      <wink-content-loader layout="HOTEL" id="YOUR_LAYOUT_ID" />
      <wink-lookup />
    </main>
  );
}
```

## TypeScript — деклариране на JSX intrinsics

JSX namespace на Preact е отделен от този на React. Добавете в `preact/src/jsx.d.ts`:

```ts
// src/wink-elements.d.ts
import type {
  WinkContentLoaderAttributes,
  WinkLookupAttributes,
  WinkSearchButtonAttributes,
  WinkAccountButtonAttributes,
  WinkItineraryButtonAttributes,
  WinkShoppingCartButtonAttributes,
  WinkAppLoaderAttributes,
} from '@wink/elements';

declare module 'preact' {
  namespace JSX {
    interface IntrinsicElements {
      'wink-content-loader': WinkContentLoaderAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-lookup': WinkLookupAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-search-button': WinkSearchButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-account-button': WinkAccountButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-itinerary-button': WinkItineraryButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-shopping-cart-button': WinkShoppingCartButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-app-loader': WinkAppLoaderAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
    }
  }
}
```

## Preact с `preact/compat`

Ако използвате alias на React към `preact/compat`, използвайте React JSX декларациите:

```ts
// src/wink-elements.d.ts
// Използвайте React JSX декларацията от /integrations/react
// — preact/compat преекспортира React JSX namespace
```

<Aside type="tip">
По-малкият размер на пакета на Preact е добро решение за вграждане на Wink компоненти в магазини с изисквания за висока производителност. Функцията `load()` от `@wink/elements` добавя около 500 байта компресиран размер — основното тегло идва от CDN пакета, зареждан асинхронно.
</Aside>

## Допълнително четене

- [Референция за Web Components](/bg/integrations/web-components)
- [@wink/elements npm пакет](/bg/integrations/npm)
- [React интеграция](/bg/integrations/react)
