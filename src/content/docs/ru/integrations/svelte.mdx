---
title: Svelte / SvelteKit
description: Используйте веб-компоненты Wink в приложениях Svelte и SvelteKit с onMount и проверками браузера.
sidebar:
  order: 20
---

import { Aside, LinkCard } from '@astrojs/starlight/components';

Svelte передаёт атрибуты напрямую в DOM без слоя виртуального DOM, что делает кастомные элементы естественным выбором. Для SvelteKit (SSR) загружайте CDN внутри `onMount` или используйте проверку `browser`.

<LinkCard
  title="@wink/elements npm package"
  href="/integrations/npm"
  description="Установите и загрузите CDN-бандл с типами TypeScript."
/>

## Установка

```bash
npm install @wink/elements
```

## SvelteKit — корневой layout

```ts
// src/routes/+layout.ts
import { browser } from '$app/environment';
import { load as loadWink } from '@wink/elements';

export function load() {
  if (browser) {
    loadWink({ clientId: import.meta.env.PUBLIC_WINK_CLIENT_ID });
  }
}
```

Или загрузите внутри `onMount` в `+layout.svelte`:

```svelte
<!-- src/routes/+layout.svelte -->
<script>
  import { onMount } from 'svelte';
  import { load } from '@wink/elements';

  onMount(() => {
    load({ clientId: import.meta.env.PUBLIC_WINK_CLIENT_ID });
  });
</script>

<slot />
```

## Использование в компонентах

Svelte передаёт все неизвестные атрибуты напрямую в DOM, поэтому дополнительная настройка не требуется:

```svelte
<script>
  export let layoutId = 'YOUR_LAYOUT_ID';
</script>

<main>
  <wink-content-loader layout="HOTEL" id={layoutId} />
  <wink-lookup />
</main>
```

## Svelte 5 (синтаксис runes)

```svelte
<script lang="ts">
  import { onMount } from 'svelte';
  import { load } from '@wink/elements';

  let { layoutId }: { layoutId: string } = $props();

  onMount(() => {
    load({ clientId: import.meta.env.PUBLIC_WINK_CLIENT_ID });
  });
</script>

<wink-content-loader layout="HOTEL" id={layoutId} />
```

## Переменные окружения

SvelteKit предоставляет в браузер только переменные с префиксом `PUBLIC_`:

```
# .env
PUBLIC_WINK_CLIENT_ID=your-client-id
```

<Aside type="caution" title="SSR рендеринг">
Никогда не рендерьте элементы `<wink-*>` во время серверного рендеринга — кастомные элементы регистрируются в реестре `customElements` браузера, которого нет на сервере. Оборачивайте в `{#if browser}` или вызывайте `load()` только внутри `onMount`.
</Aside>

```svelte
<script>
  import { browser } from '$app/environment';
</script>

{#if browser}
  <wink-content-loader layout="HOTEL" id="YOUR_LAYOUT_ID" />
{/if}
```

## Чистый Svelte (Vite, без SvelteKit)

```svelte
<!-- src/App.svelte -->
<script>
  import { onMount } from 'svelte';
  import { load } from '@wink/elements';

  onMount(() => {
    load({ clientId: import.meta.env.VITE_WINK_CLIENT_ID });
  });
</script>

<wink-content-loader layout="HOTEL" id="YOUR_LAYOUT_ID" />
```

## Дополнительные материалы

- [Справочник по Web Components](/ru/integrations/web-components)
- [@wink/elements npm package](/ru/integrations/npm)
