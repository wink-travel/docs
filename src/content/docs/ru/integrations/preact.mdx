---
title: Preact
description: Используйте веб-компоненты Wink в приложениях Preact — тот же паттерн, что и в React, но с меньшим размером.
sidebar:
  order: 24
---

import { Aside, LinkCard } from '@astrojs/starlight/components';

API Preact совместим с React. Интеграция следует тому же паттерну — вызовите `load()` один раз при запуске приложения, затем используйте теги `<wink-*>` в JSX.

<LinkCard
  title="@wink/elements npm package"
  href="/integrations/npm"
  description="Установите и загрузите CDN-бандл с типами TypeScript."
/>

## Установка

```bash
npm install @wink/elements
```

## Загрузите один раз при запуске приложения

```tsx
// src/app.tsx
import { useEffect } from 'preact/hooks';
import { load } from '@wink/elements';

export function App() {
  useEffect(() => {
    load({ clientId: import.meta.env.VITE_WINK_CLIENT_ID });
  }, []);

  return <YourRoutes />;
}
```

## Использование в JSX

```tsx
// src/pages/hotels.tsx
export function HotelsPage() {
  return (
    <main>
      <wink-content-loader layout="HOTEL" id="YOUR_LAYOUT_ID" />
      <wink-lookup />
    </main>
  );
}
```

## TypeScript — объявление JSX intrinsics

Пространство имён JSX в Preact отделено от React. Расширьте `preact/src/jsx.d.ts`:

```ts
// src/wink-elements.d.ts
import type {
  WinkContentLoaderAttributes,
  WinkLookupAttributes,
  WinkSearchButtonAttributes,
  WinkAccountButtonAttributes,
  WinkItineraryButtonAttributes,
  WinkShoppingCartButtonAttributes,
  WinkAppLoaderAttributes,
} from '@wink/elements';

declare module 'preact' {
  namespace JSX {
    interface IntrinsicElements {
      'wink-content-loader': WinkContentLoaderAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-lookup': WinkLookupAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-search-button': WinkSearchButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-account-button': WinkAccountButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-itinerary-button': WinkItineraryButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-shopping-cart-button': WinkShoppingCartButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-app-loader': WinkAppLoaderAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
    }
  }
}
```

## Preact с `preact/compat`

Если вы используете алиас React на `preact/compat`, используйте декларации JSX React:

```ts
// src/wink-elements.d.ts
// Используйте декларацию JSX React из /integrations/react
// — preact/compat реэкспортирует пространство имён JSX React
```

<Aside type="tip">
Меньший размер бандла Preact отлично подходит для встраивания компонентов Wink в производительные витрины. Функция `load()` из `@wink/elements` добавляет около 500 байт в сжатом виде — основная нагрузка приходится на CDN-бандл, загружаемый асинхронно.
</Aside>

## Дополнительное чтение

- [Справочник по Web Components](/ru/integrations/web-components)
- [@wink/elements npm package](/ru/integrations/npm)
- [Интеграция с React](/ru/integrations/react)
