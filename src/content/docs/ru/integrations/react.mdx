---
title: React
description: Используйте веб-компоненты Wink в любом React-приложении с полной поддержкой TypeScript через @wink/elements.
sidebar:
  order: 15
---

import { Aside, LinkCard } from '@astrojs/starlight/components';

React 19+ имеет полноценную поддержку кастомных элементов. В React 18 и ранее передаются только строковые атрибуты — используйте приведённый ниже типизированный шаблон, чтобы оставаться в безопасности между версиями.

<LinkCard
  title="@wink/elements npm package"
  href="/integrations/npm"
  description="Установите и загрузите CDN-бандл с типами TypeScript."
/>

## Установка

```bash
npm install @wink/elements
```

## Загрузка один раз в корне приложения

Вызовите `load()` в вашем компоненте верхнего уровня. Он идемпотентен — безопасно вызывать несколько раз.

```tsx
// src/App.tsx
import { useEffect } from 'react';
import { load } from '@wink/elements';

export default function App() {
  useEffect(() => {
    load({ clientId: import.meta.env.VITE_WINK_CLIENT_ID });
  }, []);

  return <YourRoutes />;
}
```

## Использование компонентов в JSX

```tsx
export default function HotelPage() {
  return (
    <main>
      <wink-content-loader layout="HOTEL" id="YOUR_LAYOUT_ID" />
      <wink-lookup />
    </main>
  );
}
```

## TypeScript — объявление JSX intrinsics

React по умолчанию не знает имена кастомных элементов. Добавьте файл деклараций один раз, чтобы устранить ошибки типов:

```ts
// src/wink-elements.d.ts
import type {
  WinkContentLoaderAttributes,
  WinkLookupAttributes,
  WinkSearchButtonAttributes,
  WinkAccountButtonAttributes,
  WinkItineraryButtonAttributes,
  WinkShoppingCartButtonAttributes,
  WinkAppLoaderAttributes,
} from '@wink/elements';

declare module 'react' {
  namespace JSX {
    interface IntrinsicElements {
      'wink-content-loader': WinkContentLoaderAttributes & React.HTMLAttributes<HTMLElement>;
      'wink-lookup': WinkLookupAttributes & React.HTMLAttributes<HTMLElement>;
      'wink-search-button': WinkSearchButtonAttributes & React.HTMLAttributes<HTMLElement>;
      'wink-account-button': WinkAccountButtonAttributes & React.HTMLAttributes<HTMLElement>;
      'wink-itinerary-button': WinkItineraryButtonAttributes & React.HTMLAttributes<HTMLElement>;
      'wink-shopping-cart-button': WinkShoppingCartButtonAttributes & React.HTMLAttributes<HTMLElement>;
      'wink-app-loader': WinkAppLoaderAttributes & React.HTMLAttributes<HTMLElement>;
    }
  }
}
```

<Aside type="caution" title="Ограничение атрибутов в React 18">
React 18 и ранее сериализует все пропсы JSX в строки перед передачей в DOM. Булевы и объектные пропсы нужно конвертировать вручную. React 19 передаёт нестроковые пропсы напрямую кастомным элементам — обходные пути не нужны.
</Aside>

## Передача типизированных атрибутов (безопасно для React 18)

```tsx
import type { WinkContentLoaderAttributes } from '@wink/elements';

const attrs: WinkContentLoaderAttributes = {
  layout: 'HOTEL',
  id: 'abc123',
  sort: 'POPULARITY',
};

// Spread работает в React 19; для React 18 все значения всё равно строки
export default function HotelCard() {
  return <wink-content-loader layout={attrs.layout} id={attrs.id} sort={attrs.sort} />;
}
```

## Дополнительное чтение

- [Справочник по Web Components](/ru/integrations/web-components)
- [@wink/elements npm package](/ru/integrations/npm)
- [Интеграция с Next.js](/ru/integrations/nextjs)
