---
title: Servidores MCP
description: Conecta agentes de IA a la plataforma Wink mediante el Protocolo de Contexto de Modelo. Los servidores MCP autenticados brindan a tu agente acceso directo a inventario, reservas, integraciones con gestores de canales y más, todo protegido por inicio de sesión interactivo OAuth2.
sidebar:
  order: 10
---

import { Steps, LinkButton, Aside, Tabs, TabItem } from '@astrojs/starlight/components';

Wink expone dos servidores MCP autenticados. Ambos usan **inicio de sesión interactivo OAuth2** (flujo de código de autorización PKCE): tu agente de IA abre una pestaña del navegador para que inicies sesión con tu cuenta Wink. No hay secretos de cliente que administrar.

| Servidor | URL | Qué hace |
|--------|-----|--------------|
| **API de Inventario** | `https://api.wink.travel/mcp` | Inventario, reservas, tarifas, canales de venta, analíticas y todas las operaciones de la plataforma |
| **Integraciones** | `https://integrations.wink.travel/mcp` | Integraciones con gestores de canales — sincroniza propiedades, tarifas y disponibilidad con OTAs |

---

## Cómo funciona la autenticación

Los servidores MCP de Wink usan el flujo **OAuth2 Authorization Code + PKCE** y soportan **Registro Dinámico de Clientes (RFC 7591)**. Esto es lo que sucede cuando te conectas por primera vez:

1. Tu cliente de IA descubre el servidor de autorización desde los metadatos `/.well-known/oauth-protected-resource` del endpoint MCP.
2. El cliente se auto-registra mediante **Registro Dinámico de Clientes** — no se requiere configuración manual de la aplicación.
3. El cliente abre una **pestaña del navegador** en `https://iam.wink.travel`. Inicias sesión con tu cuenta Wink y apruebas los permisos solicitados en la pantalla de consentimiento.
4. Wink emite un **token de acceso** vinculado a tu cuenta e identidad.
5. El cliente de IA adjunta el token a cada solicitud MCP. Todos los resultados de las herramientas se limitan automáticamente a tu entidad administradora.
6. Los tokens expiran después de 3 horas. Tu cliente de IA los renueva silenciosamente en segundo plano sin pedirte que inicies sesión de nuevo.

Cada llamada a una herramienta se ejecuta **bajo tu identidad** — las mismas reglas ACL que protegen el portal web se aplican al agente de IA.

---

## Conecta tu cliente de IA

<Tabs>
  <TabItem label="Claude Code">

<Steps>
1. Asegúrate de que Claude Code esté actualizado:
   ```bash
   npm install -g @anthropic-ai/claude-code
   ```

2. Agrega el servidor MCP de Inventario de Wink:
   ```bash
   claude mcp add wink-inventory https://api.wink.travel/mcp --transport http
   ```

3. Claude Code contacta al servidor, descubre el servidor de autorización y abre tu navegador predeterminado.

4. **Inicia sesión en Wink** y aprueba la solicitud de permisos en la pantalla de consentimiento.

5. Claude Code confirma la conexión. Escribe `/mcp` para verificar que el servidor aparece y sus herramientas se han cargado.
</Steps>

Para agregar también el servidor de Integraciones:
```bash
claude mcp add wink-integrations https://integrations.wink.travel/mcp --transport http
```

:::tip[Comparte con tu equipo]
Agrega `--scope project` a `claude mcp add` y Claude Code escribirá la URL del servidor en `.mcp.json` en la raíz del repositorio. Haz commit de ese archivo — cada miembro del equipo completará su propio inicio de sesión en el navegador la primera vez que se conecte.
:::

  </TabItem>
  <TabItem label="Claude Desktop">

<Steps>
1. Edita el archivo de configuración de Claude Desktop:
   - **macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`
   - **Windows:** `%APPDATA%\Claude\claude_desktop_config.json`
   - **Linux:** `~/.config/Claude/claude_desktop_config.json`

2. Agrega las entradas de los servidores MCP:

   ```json title="claude_desktop_config.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

3. **Cierra completamente y reinicia** Claude Desktop (usa la opción Salir en la barra de menú — cerrar la ventana no es suficiente).

4. Claude Desktop abre una pestaña del navegador para cada servidor nuevo. Inicia sesión en Wink y aprueba los permisos.

5. El ícono de enchufe en la barra inferior muestra ambos servidores como conectados.
</Steps>

  </TabItem>
  <TabItem label="OpenAI Codex">

<Steps>
1. Asegúrate de que Codex CLI esté instalado:
   ```bash
   npm install -g @openai/codex
   ```

2. Abre (o crea) el archivo de configuración de Codex:
   - **macOS / Linux:** `~/.codex/config.json`
   - **Windows:** `%USERPROFILE%\.codex\config.json`

3. Agrega las entradas de los servidores MCP de Wink:

   ```json title="~/.codex/config.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

4. Inicia Codex. En el primer uso, Codex abre una pestaña del navegador en `https://iam.wink.travel`.

5. **Inicia sesión en Wink** y aprueba los permisos solicitados.

6. Codex confirma la conexión. Ejecuta `codex` y pídele que liste las herramientas disponibles para verificar que los servidores Wink están activos.
</Steps>

:::note
El soporte MCP en Codex CLI requiere la versión **1.0** o superior. Ejecuta `codex --version` para verificar y `npm install -g @openai/codex` para actualizar.
:::

  </TabItem>
  <TabItem label="ChatGPT">

El soporte para servidores MCP está disponible en la **app de escritorio ChatGPT** para suscriptores Plus, Pro, Team y Enterprise.

<Steps>
1. Descarga e instala la [app de escritorio ChatGPT](https://openai.com/chatgpt/download/) si aún no lo has hecho.

2. Abre la app y haz clic en **tu ícono de perfil** en la esquina superior derecha, luego elige **Configuración**.

3. Ve a **Conectores** (o **Servidores MCP** según la versión de tu app) y haz clic en **Agregar**.

4. Ingresa la URL del servidor de Inventario de Wink:
   ```
   https://api.wink.travel/mcp
   ```
   Luego haz clic en **Conectar**.

5. ChatGPT abre una pestaña del navegador en `https://iam.wink.travel`. **Inicia sesión en Wink** y aprueba los permisos.

6. Repite los pasos 4–5 para el servidor de Integraciones:
   ```
   https://integrations.wink.travel/mcp
   ```

7. Ambos servidores aparecen en tu lista de Conectores. Inicia un chat nuevo — las herramientas de Wink estarán disponibles automáticamente.
</Steps>

:::note[Limitación en la app web]
Los servidores MCP solo se pueden configurar en la **app de escritorio**. La interfaz web de ChatGPT en chatgpt.com no soporta servidores MCP personalizados.
:::

  </TabItem>
  <TabItem label="Cursor">

<Steps>
1. Abre (o crea) el archivo de configuración MCP de Cursor:
   - **macOS / Linux:** `~/.cursor/mcp.json`
   - **Windows:** `%USERPROFILE%\.cursor\mcp.json`

2. Agrega las entradas de los servidores MCP de Wink:

   ```json title="~/.cursor/mcp.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

3. Abre Cursor. Ve a **Configuración → Configuración de Cursor → MCP** y confirma que ambos servidores aparecen con un indicador de estado verde. Si muestran rojo, haz clic en **Actualizar**.

4. Cursor abre una pestaña del navegador en `https://iam.wink.travel`. **Inicia sesión en Wink** y aprueba los permisos.

5. Abre un chat (`Ctrl+L` / `Cmd+L`) y pide a Cursor que liste las herramientas Wink disponibles para confirmar la conexión.
</Steps>

:::tip[Configuración con alcance de proyecto]
También puedes colocar un archivo `mcp.json` en la raíz de tu proyecto en lugar de en `~/.cursor/`. La configuración a nivel de proyecto tiene prioridad y hace que los servidores estén disponibles para cada miembro del equipo que abra el proyecto en Cursor.
:::

  </TabItem>
  <TabItem label="Windsurf">

<Steps>
1. Abre (o crea) el archivo de configuración MCP de Windsurf:
   - **macOS / Linux:** `~/.codeium/windsurf/mcp_config.json`
   - **Windows:** `%USERPROFILE%\.codeium\windsurf\mcp_config.json`

2. Agrega las entradas de los servidores MCP de Wink:

   ```json title="~/.codeium/windsurf/mcp_config.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

3. **Reinicia completamente Windsurf** (Archivo → Salir, luego vuelve a abrir).

4. Ve a **Configuración → Servidores MCP** y verifica que ambos servidores aparezcan como conectados. Windsurf abre una pestaña del navegador en `https://iam.wink.travel` en el primer uso.

5. **Inicia sesión en Wink** y aprueba los permisos. Los servidores se pondrán verdes en el panel de configuración una vez autenticados.
</Steps>

:::note
El soporte MCP en Windsurf requiere la versión **1.4** o superior. Verifica en **Ayuda → Acerca de Windsurf** y actualiza desde el sitio web de Windsurf si es necesario.
:::

  </TabItem>
  <TabItem label="Cline (VS Code)">

Cline es una extensión para VS Code con un administrador de servidores MCP integrado. No se requiere editar JSON manualmente.

<Steps>
1. Instala Cline desde el Marketplace de VS Code:
   - Abre VS Code y presiona `Ctrl+Shift+X` / `Cmd+Shift+X`
   - Busca **Cline** y haz clic en **Instalar**

2. Haz clic en el **ícono de Cline** en la Barra de Actividad (barra lateral izquierda) para abrir el panel de Cline.

3. Haz clic en el **ícono de Servidores MCP** (ícono de enchufe) en la barra de herramientas de Cline, luego elige **Editar configuración MCP**.

4. VS Code abre `cline_mcp_settings.json`. Agrega las entradas de Wink:

   ```json title="cline_mcp_settings.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

5. **Guarda el archivo.** Cline se recarga automáticamente y abre una pestaña del navegador en `https://iam.wink.travel`.

6. **Inicia sesión en Wink** y aprueba los permisos. Ambos servidores aparecen activos en el panel de Servidores MCP.
</Steps>

:::tip[Alternativa: agregar vía URL]
En el panel de Servidores MCP, haz clic en **Agregar servidor** y pega directamente `https://api.wink.travel/mcp`. Cline autocompleta la configuración y maneja la autenticación sin necesidad de editar JSON manualmente.
:::

  </TabItem>
</Tabs>

---

## Referencia de scopes

Cuando apruebas la pantalla de consentimiento verás un desglose de lo que el agente está solicitando. Wink agrupa los permisos por área funcional:

| Grupo de scopes | Scopes | Qué desbloquean |
|-------------|--------|-----------------|
| **Transporte MCP** | `mcp.read` `mcp.write` `mcp.remove` | Requerido para conectar a cualquiera de los servidores MCP |
| **Cuenta** | `account.read` `account.write` `account.remove` | Entidades administradoras, miembros del equipo, configuraciones, aplicaciones |
| **Reserva** | `booking.read` `booking.write` `booking.remove` | Buscar, ver, crear, cancelar y reembolsar reservas |
| **Inventario** | `inventory.read` `inventory.write` `inventory.remove` | Propiedades, habitaciones, tarifas, amenidades, disponibilidad |
| **Analíticas** | `analytics.read` | Analíticas de reservas, reportes de ingresos, tablas de líderes |
| **Marketing** | `marketing.read` `marketing.write` `marketing.remove` | Publicaciones sociales, campañas, canales de venta, promociones |
| **Contenido** | `content.read` `content.write` `content.remove` | Multimedia, URLs acortadas, elementos sindicables, mapas |
| **Pago** | `payment.read` `payment.write` | Libro mayor, pagos, desglose financiero, retiros |

El cliente de IA solicita solo los scopes que necesita. Puedes negar permisos individuales en la pantalla de consentimiento — las herramientas que requieran un scope denegado devolverán un error al invocarlas.

---

## Solución de problemas

**No se abre la pestaña del navegador**

Tu cliente de IA puede no soportar aún el flujo interactivo PKCE. Verifica que estés usando una versión reciente:
- **Claude Code:** `npm install -g @anthropic-ai/claude-code`
- **Codex CLI:** `npm install -g @openai/codex`
- **Claude Desktop / ChatGPT escritorio:** actualiza desde el actualizador integrado de la app

**`401 Unauthorized` después de iniciar sesión**

Tu token en caché puede estar obsoleto. Elimina y vuelve a agregar el servidor para forzar un nuevo inicio de sesión:

```bash
# Claude Code
claude mcp remove wink-inventory
claude mcp add wink-inventory https://api.wink.travel/mcp --transport http
```

Para Claude Desktop y ChatGPT, elimina la entrada del servidor en la configuración, reinicia la app y agrégalo de nuevo.

**`403 Forbidden` al llamar a una herramienta específica**

La herramienta requiere un scope que no aprobaste en la pantalla de consentimiento. Reconecta el servidor — la pantalla de consentimiento aparecerá de nuevo y podrás aprobar el permiso faltante.

**El servidor aparece pero las llamadas a herramientas devuelven "cuenta no encontrada"**

Has iniciado sesión con una cuenta Wink que no tiene entidad administradora. Ve a [Wink Portal](https://app.wink.travel) y asegúrate de que tu cuenta esté configurada antes de usar el servidor MCP.

**El token expira a mitad de sesión**

Los tokens son válidos por 3 horas. Claude Code y Claude Desktop renuevan tokens silenciosamente sin interrumpir tu sesión. Si ves errores de autenticación tras un largo periodo inactivo, escribe un nuevo mensaje — el cliente se reautenticará automáticamente.

**Codex CLI muestra "MCP server unreachable"**

Verifica que tu `~/.codex/config.json` sea JSON válido (sin comas finales) y que `"type": "http"` esté configurado. Codex requiere transporte HTTP para servidores remotos; el transporte stdio es solo para binarios locales.

**El menú "Conectores" de ChatGPT no aparece**

El soporte MCP requiere la versión **1.2024.352** o superior de la app de escritorio ChatGPT y una suscripción activa Plus, Pro, Team o Enterprise. Actualiza la app y verifica tu plan en Configuración → Cuenta.

**El servidor Cursor muestra rojo / "Error al conectar"**

Verifica que tu `~/.cursor/mcp.json` sea JSON válido y contenga `"type": "http"`. Luego ve a **Configuración → Configuración de Cursor → MCP** y haz clic en **Actualizar**. Si el error persiste, reinicia completamente Cursor — los cambios en la configuración no siempre se detectan sin reiniciar.

**Los servidores MCP de Windsurf no aparecen en configuración**

Windsurf requiere un reinicio completo (no solo recargar ventana) para detectar cambios en `mcp_config.json`. Usa **Archivo → Salir** y vuelve a abrir. Si los servidores aún no aparecen, confirma que tienes Windsurf **1.4+** en **Ayuda → Acerca de Windsurf**.

**La opción "Agregar servidor" en Cline no es visible**

El administrador de servidores MCP requiere Cline **2.0** o superior. Abre el panel de Extensiones de VS Code, busca Cline y haz clic en **Actualizar** si hay una actualización disponible. Después de actualizar, recarga VS Code (`Ctrl+Shift+P` → "Developer: Reload Window").

---

<LinkButton href="https://app.wink.travel" variant="primary" icon="external">Abrir Wink Portal</LinkButton>
<LinkButton href="https://github.com/wink-travel" variant="secondary" icon="external">Wink en GitHub</LinkButton>
