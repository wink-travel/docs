---
title: Vue 3
description: Usa los web components de Wink en proyectos Vue 3 y Vite con la opción de compilador isCustomElement.
sidebar:
  order: 18
---

import { Aside, LinkCard } from '@astrojs/starlight/components';

Vue 3 necesita una pista para el compilador que trate las etiquetas `<wink-*>` como elementos personalizados nativos en lugar de componentes Vue no resueltos. Después de eso, funcionan como cualquier otro elemento HTML.

<LinkCard
  title="@wink/elements npm package"
  href="/integrations/npm"
  description="Instala y carga el paquete CDN con tipos para TypeScript."
/>

## Instalar

```bash
npm install @wink/elements
```

## Configurar el compilador

### Vite + Vue (`vite.config.ts`)

```ts
import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';

export default defineConfig({
  plugins: [
    vue({
      template: {
        compilerOptions: {
          // Trata todas las etiquetas que empiezan con "wink-" como elementos personalizados
          isCustomElement: (tag) => tag.startsWith('wink-'),
        },
      },
    }),
  ],
});
```

### Vue CLI (`vue.config.js`)

```js
module.exports = {
  chainWebpack(config) {
    config.module
      .rule('vue')
      .use('vue-loader')
      .tap((options) => ({
        ...options,
        compilerOptions: {
          isCustomElement: (tag) => tag.startsWith('wink-'),
        },
      }));
  },
};
```

## Cargar CDN una vez al iniciar la app

```ts
// src/main.ts
import { createApp } from 'vue';
import App from './App.vue';
import { load } from '@wink/elements';

load({ clientId: import.meta.env.VITE_WINK_CLIENT_ID });

createApp(App).mount('#app');
```

## Usar en plantillas

```vue
<template>
  <main>
    <wink-content-loader layout="HOTEL" :id="layoutId" />
    <wink-lookup />
  </main>
</template>

<script setup lang="ts">
const layoutId = 'YOUR_LAYOUT_ID';
</script>
```

## TypeScript — ampliar tipos globales de elementos

Vue 3 + TypeScript lee los tipos de elementos personalizados desde el `HTMLElementTagNameMap` global. Agrega un archivo de declaración:

```ts
// src/wink-elements.d.ts
import type {
  WinkContentLoaderAttributes,
  WinkLookupAttributes,
  WinkSearchButtonAttributes,
  WinkAccountButtonAttributes,
  WinkItineraryButtonAttributes,
  WinkShoppingCartButtonAttributes,
  WinkAppLoaderAttributes,
} from '@wink/elements';

declare global {
  interface HTMLElementTagNameMap {
    'wink-content-loader': HTMLElement & WinkContentLoaderAttributes;
    'wink-lookup': HTMLElement & WinkLookupAttributes;
    'wink-search-button': HTMLElement & WinkSearchButtonAttributes;
    'wink-account-button': HTMLElement & WinkAccountButtonAttributes;
    'wink-itinerary-button': HTMLElement & WinkItineraryButtonAttributes;
    'wink-shopping-cart-button': HTMLElement & WinkShoppingCartButtonAttributes;
    'wink-app-loader': HTMLElement & WinkAppLoaderAttributes;
  }
}
```

## Variables de entorno

Agrega a `.env.local`:

```
VITE_WINK_CLIENT_ID=your-client-id
```

Vite expone solo las variables que empiezan con `VITE_` al paquete cliente.

<Aside type="tip">
Para Nuxt 3, consulta la página dedicada de [integración con Nuxt](/es-MX/integrations/nuxt) que cubre la carga segura para SSR.
</Aside>

## Lectura adicional

- [Referencia de Web Components](/es-MX/integrations/web-components)
- [Paquete npm @wink/elements](/es-MX/integrations/npm)
- [Integración con Nuxt 3](/es-MX/integrations/nuxt)
