---
title: MCP-servere
description: Forbind AI-agenter til Wink-platformen via Model Context Protocol. Wink udgiver sine værktøjer som fokuserede, jobformede endpoints, så din agent kun indlæser det, dit arbejde har brug for — sikret med interaktiv OAuth2-login.
sidebar:
  order: 10
---

import { Steps, LinkButton, Aside, Tabs, TabItem } from '@astrojs/starlight/components';

Winks MCP-værktøjer udgives som **toolsets** — fokuserede endpoints, der hver dækker ét job. Du forbinder din AI-klient til den ene eller to, der matcher dit arbejde, ikke til alle.

Hvert endpoint bruger **interaktiv OAuth2-login** (PKCE autorisationskode-flow): din AI-agent åbner en browsertab, du logger ind med din Wink-konto, og det er det. Ingen klienthemmeligheder at administrere.

```bash title="En channel manager-integrator forbinder til ét endpoint"
claude mcp add wink-channel-manager https://integrations.wink.travel/mcp/channel-manager --transport http
```

```bash title="En hotelier, der administrerer egne priser, forbinder til et andet"
claude mcp add wink-rates https://api.wink.travel/mcp/extranet/rates --transport http
```

---

## Hvorfor værktøjerne er opdelt på endpoints

Wink eksponerer omkring 500 værktøjer. At give en agent alle på én gang er teknisk muligt — og en dårlig idé.

**Værktøjsdefinitioner koster kontekst.** Hvert værktøj, en MCP-server annoncerer, indlæses i modellens kontekstvindue, før den udfører noget arbejde, sammen med dets beskrivelse og fulde parameterskema. Fem hundrede af dem bruger en stor del af budgettet, som din faktiske opgave har brug for, ved hver enkelt forespørgsel.

**Valgkvaliteten falder, når listen vokser.** En agent, der vælger blandt 500 værktøjer med lignende navne, træffer dårligere valg end en, der vælger blandt 40. Hvis du integrerer en channel manager, er cirka tyve værktøjer relevante for dig; de øvrige 480 er støj, der gør det sværere at finde det rigtige værktøj.

**Arbejdet er reelt adskilleligt.** Nogen, der synkroniserer tilgængelighed fra et PMS, og nogen, der kuraterer affiliate-landingpages, deler næsten ingen værktøjer. Opdeling efter job betyder, at hver forbindelse bærer ordforrådet for ét job.

<Aside type="caution" title="Toolsets er ikke tilladelser">
At vælge et snævert toolset begrænser **ikke**, hvad din konto kan gøre, og at vælge et bredt giver ikke noget ekstra. Hvert værktøj håndhæver de samme kontotilladelser som webportalen, under din indloggede identitet. Hvis din konto ikke kan annullere en booking i Wink Portal, kan agenten heller ikke — uanset hvilket endpoint den er forbundet til.

Vælg toolsets for **fokus**, ikke for sikkerhed.
</Aside>

---

## Endpoint-katalog

To hosts leverer toolsets. Antal værktøjer er omtrentlige og vokser over tid.

### `https://api.wink.travel` — platformen

| Endpoint | Værktøjer | Hvad det dækker |
|---|---:|---|
| `/mcp/extranet/facilities` | ~64 | Den bookbare og onsite inventar: gæsteværelser og værelsestyper, spaer, restauranter, mødelokaler, aktiviteter, attraktioner, steder, andre salgbare genstande |
| `/mcp/extranet/property` | ~62 | Ejendomsoplysninger og kommercielle vilkår: profil og indhold, tillæg, afbestillingsregler, fordele, meddelelser |
| `/mcp/settings` | ~60 | Konto-administration: administrationsenhedsprofil og branding, ledere og deres beføjelser, tilpasning, webhooks, notifikationspræferencer |
| `/mcp/studio/embeds` | ~48 | Publicering af kurateret inventar: widgets, grids og rangerede grids, kort, kort, dybe links til inventar og leverandørsider |
| `/mcp/extranet/rates` | ~44 | Priser og tilgængelighed: inventar-prisperiode og kvoter, rateplaner, masterpriser, særlige og kampagnepriser |
| `/mcp/extranet/operations` | ~39 | Drift af en live ejendom: reservationer og deres livscyklus, gæsteanmeldelser og svar, kalenderfeeds, planlagte opgaver |
| `/mcp/extranet/distribution` | ~37 | Hvor inventar sælges: salgskanaler og prisændringer, affiliate- og leverandørforholdsanmodninger, registrering, lead capture |
| `/mcp/studio/curation` | ~35 | Opbygning af salgbare samlinger: statiske og dynamiske lister, rangerede lister, gemte søgninger, ejendomsaggregater |
| `/mcp/link-manager` | ~28 | Syndikerede link-samlinger, deres design, kategorier og tags, plus forkortede URL’er |
| `/mcp/reference` | ~24 | Læse-only opslagdata: lande, valutaer og valutakurser, sprog, tidszoner, geo-opslag, IP-geolokation |
| `/mcp/analytics` | ~18 | Booking- og indtægtsanalyse: oversigter, linjediagrammer, top-lister, affiliate-performance, delingsmålinger |
| `/mcp/payment` | ~18 | Finansielle operationer: regnskab, udbetalinger og gebyrestimater, booking-fondssummer |
| `/mcp/booking-engine` | ~17 | Rejsendes shoppingsti: søgning og opslag, indkøbskurv og checkout, bucket lists, kampagnetilpasning |
| `/mcp/social` | ~17 | Forbundne sociale konti, publicering og planlægning af opslag, læsning af opslagindsigter |
| `/mcp/travel-agent` | ~8 | Agent-faciliterede bookinger på vegne af rejsende og agentrapporter |

### `https://integrations.wink.travel` — channel manager-integrationer

Ét endpoint, fordi denne host eksisterer til ét job.

| Endpoint | Værktøjer | Hvad det dækker |
|---|---:|---|
| `/mcp/channel-manager` | ~9 | Forbindelse og verifikation af en channel manager, PMS eller CRS-integration |

<Aside type="note" title="Integration af channel manager vs. håndtering af egne priser">
Dette er forskellige job for forskellige personer, og de deler ikke et endpoint.

En **channel manager-, PMS- eller CRS-leverandør** integrerer én gang via `integrations.wink.travel/mcp/channel-manager`, og udveksler derefter ejendomme, priser, tilgængelighed og reservationer over den integration. Det ene endpoint er hele overfladen.

En **hotelier, der ændrer egne priser manuelt**, bruger extranet-toolsets på `api.wink.travel` — `/mcp/extranet/rates` og lignende. Disse er ikke en del af en channel manager-integration, og en integrator har ingen grund til at forbinde til dem.

Konto-administration er også på `api.wink.travel`, under `/mcp/settings`. Der findes ikke `/mcp/settings` på integrations-host.
</Aside>

Hvert endpoint har også et `ping`-værktøj, så du kan bekræfte, at en forbindelse er aktiv, før du beder det om at udføre rigtigt arbejde.

### Agentiske betalinger

For den komplette rejsendes bookingflow, forbind **Wink Booking Engine** på `https://api.wink.travel/mcp/booking-engine` og en kompatibel **betalingswallet**. Tilføjelse af Wink MCP åbner browser-login og tilladelsesvalg; din klient håndterer derefter autentificering.

Booking Engine inkluderer destinationsopslag, hotel- og værelsessøgning, `agentic_booking_quote`, `agentic_booking_pay`, `agentic_booking_book`, bookinghentning og kvitteringer, når agentisk booking og betaling er aktiveret for det miljø. Se [Agentic Payments](/da/developers/agentic-payments/) for opsætning af forbindelse, tilladelser at vælge og trin fra søgning til bekræftelse.

---

## Hvilke toolsets har jeg brug for?

| Hvis du er… | Forbind til |
|---|---|
| **Integrerer en channel manager, PMS eller CRS** | `integrations.wink.travel/mcp/channel-manager` — kun denne |
| **Administrerer en ejendoms indhold** | `/mcp/extranet/property` + `/mcp/extranet/facilities` |
| **Administrerer værelser og værelsestyper** | `/mcp/extranet/facilities` |
| **Administrerer priser og tilgængelighed** | `/mcp/extranet/rates` |
| **Driver dag-til-dag reservationer** | `/mcp/extranet/operations` |
| **Udvider distribution** | `/mcp/extranet/distribution` + `/mcp/analytics` |
| **Bygger affiliate-sider** | `/mcp/studio/curation` + `/mcp/studio/embeds` |
| **Driver link-in-bio-sider** | `/mcp/link-manager` + `/mcp/social` |
| **Booker på vegne af rejsende** | `/mcp/travel-agent` |
| **Afstemmer penge** | `/mcp/payment` |
| **Booker et værelse med en AI-agent** | `/mcp/booking-engine` + en kompatibel betalingswallet — se [Agentic Payments](/da/developers/agentic-payments/) |
| **Bygger en bookingoplevelse** | `/mcp/booking-engine`; eventuelt `/mcp/reference` til yderligere opslag |

**Booking Engine indeholder den centrale bookingworkflow.** Den leverer konto- og tilpasningsopdagelse, destinationsopslag, hoteltilgængelighed og værelsesprisdetaljer, indkøbskurve, checkout og rejsendes bookingadministration. Agentiske booking- og betalingsværktøjer er også inkluderet, når de er aktiveret for det miljø.

For en bybaseret hotelsøgning, brug `destination_lookup_search_suggestions` til at løse destinationen, derefter `inventory_search_city` for at finde tilgængelige hoteller og priser. Begge værktøjer er i `/mcp/booking-engine`, så denne flow kræver ikke en Reference-forbindelse.

Tilføj `/mcp/reference`, når du har brug for understøttende opslag som lande, valutaer og valutakurser, understøttede sprog, tidszoner, bredere geografiske søgninger, OTA-klassifikationer eller offentlig ejendomsviden. Disse er valgfrie tilføjelser til bookingflowet.

Start med ét. Tilføj et andet, når du opdager, at du beder om noget, det ikke kan gøre — hver forbindelse er uafhængig, og tilføjelse af én påvirker aldrig de andre.

<Aside type="tip" title="Vil du stadig have alt?">
`https://api.wink.travel/mcp` og `https://integrations.wink.travel/mcp` forbliver tilgængelige og leverer **alle** værktøjer på deres host. De er nyttige til at udforske, hvad der findes, men vi anbefaler dem ikke til dagligt arbejde: værktøjslisten er stor nok til at fortrænge din opgave og forringe agentens værktøjsvalg. Foretræk de ovenstående toolsets.
</Aside>

---

## Sådan fungerer autentificering

Wink MCP-servere bruger **OAuth2 Authorization Code + PKCE** flow og understøtter **Dynamic Client Registration (RFC 7591)**. Her er, hvad der sker, når du forbinder første gang:

1. Din AI-klient opdager autorisationsserveren fra MCP-endpointets `/.well-known/oauth-protected-resource` metadata.
2. Klienten selvregistrerer via **Dynamic Client Registration** — ingen manuel applikationsopsætning nødvendig.
3. Klienten åbner en **browsertab** til `https://iam.wink.travel`. Du logger ind med din Wink-konto og godkender de anmodede tilladelser på samtykkeskærmen.
4. Wink udsteder et **adgangstoken** scoped til din konto og knyttet til din identitet.
5. AI-klienten vedhæfter token til hver MCP-forespørgsel. Alle værktøjsresultater er automatisk scoped til din administrationsenhed.
6. Tokens udløber efter 3 timer. Din AI-klient opdaterer stille i baggrunden uden at bede om login igen.

Hver værktøjskald kører **under din identitet** — de samme ACL-regler, der beskytter webportalen, gælder for AI-agenten.

---

## Forbind din AI-klient

<Tabs>
  <TabItem label="Claude Code">

<Steps>
1. Sørg for, at Claude Code er opdateret:
   ```bash
   npm install -g @anthropic-ai/claude-code
   ```

2. Vælg et toolset fra [kataloget ovenfor](#endpoint-directory) og tilføj det. For eksempel priser og tilgængelighed:
   ```bash
   claude mcp add wink-rates https://api.wink.travel/mcp/extranet/rates --transport http
   ```

3. Claude Code kontakter serveren, opdager autorisationsserveren og åbner din standardbrowser.

4. **Log ind på Wink** og godkend tilladelsesanmodningen på samtykkeskærmen.

5. Claude Code bekræfter forbindelsen. Skriv `/mcp` for at verificere, at serveren vises, og at dens værktøjer er indlæst.
</Steps>

Tilføj så mange toolsets, som dit arbejde kræver — hver er en separat serverpost med sit eget navn:
```bash
claude mcp add wink-operations https://api.wink.travel/mcp/extranet/operations --transport http
claude mcp add wink-analytics  https://api.wink.travel/mcp/analytics           --transport http
```

Du logger kun ind én gang. Efter første browsergodkendelse genbruger Claude Code din Wink-session til yderligere Wink-endpoints.

:::tip[Del med dit team]
Tilføj `--scope project` til `claude mcp add`, og Claude Code skriver server-URL’en til `.mcp.json` i repo-roden. Commit den fil — hver teammedlem får de samme toolsets, og hver gennemfører sin egen browser-login første gang, de forbinder.
:::

  </TabItem>
  <TabItem label="Claude Desktop">

<Steps>
1. Rediger Claude Desktop-konfigurationsfilen:
   - **macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`
   - **Windows:** `%APPDATA%\Claude\claude_desktop_config.json`
   - **Linux:** `~/.config/Claude/claude_desktop_config.json`

2. Tilføj MCP-serverposterne:

   ```json title="claude_desktop_config.json"
   {
     "mcpServers": {
       "wink-extranet-rates": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/rates"
       },
       "wink-extranet-operations": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/operations"
       }
     }
   }
   ```

3. **Afslut helt og genstart** Claude Desktop (brug Quit i menulinjen — det er ikke nok at lukke vinduet).

4. Claude Desktop åbner en browsertab for hver ny server. Log ind på Wink og godkend tilladelser.

5. Stik-ikonet i bundværktøjslinjen viser begge servere som forbundet.
</Steps>

  </TabItem>
  <TabItem label="OpenAI Codex">

<Steps>
1. Sørg for, at Codex CLI er installeret:
   ```bash
   npm install -g @openai/codex
   ```

2. Åbn (eller opret) Codex-konfigurationsfilen:
   - **macOS / Linux:** `~/.codex/config.json`
   - **Windows:** `%USERPROFILE%\.codex\config.json`

3. Tilføj Wink MCP-serverposterne:

   ```json title="~/.codex/config.json"
   {
     "mcpServers": {
       "wink-extranet-rates": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/rates"
       },
       "wink-extranet-operations": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/operations"
       }
     }
   }
   ```

4. Start Codex. Ved første brug åbner Codex en browsertab til `https://iam.wink.travel`.

5. **Log ind på Wink** og godkend de anmodede tilladelser.

6. Codex bekræfter forbindelsen. Kør `codex` og bed den liste tilgængelige værktøjer for at bekræfte, at Wink-serverne er aktive.
</Steps>

:::note
Codex CLI MCP-support kræver version **1.0** eller nyere. Kør `codex --version` for at tjekke, og `npm install -g @openai/codex` for at opgradere.
:::

  </TabItem>
  <TabItem label="ChatGPT">

MCP-serverunderstøttelse findes i **ChatGPT desktop-appen** for Plus, Pro, Team og Enterprise-abonnenter.

<Steps>
1. Download og installer [ChatGPT desktop-appen](https://openai.com/chatgpt/download/), hvis du ikke allerede har den.

2. Åbn appen og klik på **din profilikon** øverst til højre, vælg derefter **Indstillinger**.

3. Gå til **Connectors** (eller **MCP Servers** afhængigt af din app-version) og klik på **Tilføj**.

4. Indtast URL’en til et toolset fra [kataloget ovenfor](#endpoint-directory), for eksempel:
   ```
   https://api.wink.travel/mcp/extranet/rates
   ```
   Klik derefter på **Forbind**.

5. ChatGPT åbner en browsertab til `https://iam.wink.travel`. **Log ind på Wink** og godkend tilladelser.

6. Gentag trin 4–5 for andre toolsets, dit arbejde kræver, for eksempel:
   ```
   https://api.wink.travel/mcp/extranet/operations
   ```

7. Hvert toolset vises i din Connectors-liste. Start en ny chat — Wink-værktøjer er tilgængelige automatisk.
</Steps>

:::note[Begrænsning i webapp]
MCP-servere kan kun konfigureres i **desktop-appen**. ChatGPT-webgrænsefladen på chatgpt.com understøtter ikke brugerdefinerede MCP-servere.
:::

  </TabItem>
  <TabItem label="Cursor">

<Steps>
1. Åbn (eller opret) Cursor MCP-konfigurationsfilen:
   - **macOS / Linux:** `~/.cursor/mcp.json`
   - **Windows:** `%USERPROFILE%\.cursor\mcp.json`

2. Tilføj Wink MCP-serverposterne:

   ```json title="~/.cursor/mcp.json"
   {
     "mcpServers": {
       "wink-extranet-rates": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/rates"
       },
       "wink-extranet-operations": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/operations"
       }
     }
   }
   ```

3. Åbn Cursor. Gå til **Indstillinger → Cursor Settings → MCP** og bekræft, at begge servere vises med grøn statusindikator. Hvis de viser rød, klik på **Opdater**.

4. Cursor åbner en browsertab til `https://iam.wink.travel`. **Log ind på Wink** og godkend tilladelser.

5. Åbn en chat (`Ctrl+L` / `Cmd+L`) og bed Cursor liste tilgængelige Wink-værktøjer for at bekræfte forbindelsen.
</Steps>

:::tip[Projekt-scoped konfiguration]
Du kan også placere en `mcp.json`-fil i roden af dit projekt i stedet for i `~/.cursor/`. Projekt-niveau konfiguration har forrang og gør serverne tilgængelige for alle teammedlemmer, der åbner projektet i Cursor.
:::

  </TabItem>
  <TabItem label="Windsurf">

<Steps>
1. Åbn (eller opret) Windsurf MCP-konfigurationsfilen:
   - **macOS / Linux:** `~/.codeium/windsurf/mcp_config.json`
   - **Windows:** `%USERPROFILE%\.codeium\windsurf\mcp_config.json`

2. Tilføj Wink MCP-serverposterne:

   ```json title="~/.codeium/windsurf/mcp_config.json"
   {
     "mcpServers": {
       "wink-extranet-rates": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/rates"
       },
       "wink-extranet-operations": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/operations"
       }
     }
   }
   ```

3. **Genstart Windsurf helt** (Fil → Afslut, og åbn igen).

4. Gå til **Indstillinger → MCP Servers** og bekræft, at begge servere vises som forbundet. Windsurf åbner en browsertab til `https://iam.wink.travel` ved første brug.

5. **Log ind på Wink** og godkend tilladelser. Serverne bliver grønne i indstillingspanelet, når de er autentificerede.
</Steps>

:::note
Windsurf MCP-support kræver version **1.4** eller nyere. Tjek via **Hjælp → Om Windsurf** og opdater fra Windsurf-websitet, hvis nødvendigt.
:::

  </TabItem>
  <TabItem label="Cline (VS Code)">

Cline er en VS Code-udvidelse med indbygget MCP-servermanager. Ingen manuel JSON-redigering er nødvendig.

<Steps>
1. Installer Cline fra VS Code Marketplace:
   - Åbn VS Code og tryk `Ctrl+Shift+X` / `Cmd+Shift+X`
   - Søg efter **Cline** og klik på **Installer**

2. Klik på **Cline-ikonet** i aktivitetsbjælken (venstre sidepanel) for at åbne Cline-panelet.

3. Klik på **MCP Servers-ikonet** (stik-ikon) i Cline-værktøjslinjen, vælg derefter **Rediger MCP-indstillinger**.

4. VS Code åbner `cline_mcp_settings.json`. Tilføj Wink-posterne:

   ```json title="cline_mcp_settings.json"
   {
     "mcpServers": {
       "wink-extranet-rates": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/rates"
       },
       "wink-extranet-operations": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/operations"
       }
     }
   }
   ```

5. **Gem filen.** Cline genindlæses automatisk og åbner en browsertab til `https://iam.wink.travel`.

6. **Log ind på Wink** og godkend tilladelser. Begge servere vises som aktive i MCP Servers-panelet.
</Steps>

:::tip[Alternativ: tilføj via URL]
I MCP Servers-panelet klik på **Tilføj server** og indsæt en toolset-URL som `https://api.wink.travel/mcp/extranet/rates` direkte. Cline udfylder konfigurationen automatisk og håndterer autentificering uden manuel JSON-redigering.
:::

  </TabItem>
</Tabs>

---

## Scopes

Når du godkender samtykkeskærmen, vil du se en oversigt over, hvad agenten anmoder om. MCP
klienter bruger det samme tilladelsesordforråd som resten af platformen — se den fulde
[scope-reference på Autentificeringssiden](/da/developers/authentication#scopes).

To ting er specifikke for MCP:

- **`mcp.read` `mcp.write` `mcp.remove` scopes er yderligere påkrævet** for at åbne MCP
  transporten. En klient med et `mcp.*` scope får adgang til ethvert Wink MCP-endpoint, men hvert
  værktøj, den kalder, håndhæver stadig sektionens scope for det endpoint, det omslutter (f.eks. `booking.read`).
- **AI-klienten anmoder kun om de scopes, den har brug for.** Du kan nægte individuelle tilladelser på
  samtykkeskærmen — værktøjer, der kræver et nægtet scope, returnerer en fejl ved kald.

**Toolsets ændrer ikke noget af dette.** Det endpoint, du forbinder til, bestemmer, hvilke værktøjer der *vises*,
ikke hvad de *må gøre*. Forbindelse til `/mcp/payment` giver ingen finansiel tilladelse, du ikke allerede havde, og forbindelse til `/mcp/reference` forhindrer ikke et værktøj der i at håndhæve sit eget scope. Autorisation afgøres per værktøjskald, ud fra dit token og din kontos beføjelser.

---

## Fejlfinding

**Browserfanen åbner ikke**

Din AI-klient understøtter muligvis ikke det interaktive PKCE-flow endnu. Bekræft, at du kører en nyere version:
- **Claude Code:** `npm install -g @anthropic-ai/claude-code`
- **Codex CLI:** `npm install -g @openai/codex`
- **Claude Desktop / ChatGPT desktop:** opdater via appens indbyggede opdatering

**`401 Unauthorized` efter login**

Dit cachede token kan være udløbet. Fjern og tilføj serveren igen for at udløse et nyt login:

```bash
# Claude Code
claude mcp remove wink-rates
claude mcp add wink-rates https://api.wink.travel/mcp/extranet/rates --transport http
```

For Claude Desktop og ChatGPT, fjern serverposten i indstillinger, genstart appen og tilføj den igen.

**`403 Forbidden` ved kald af et specifikt værktøj**

Værktøjet kræver et scope, du ikke godkendte på samtykkeskærmen, eller en tilladelse, din konto ikke har. Forbind serveren igen — samtykkeskærmen vises igen, og du kan godkende den manglende tilladelse. Hvis det stadig fejler, kræver værktøjet en kontotilladelse, du ikke har; at forbinde til et andet toolset hjælper ikke, fordi toolsets ikke giver tilladelser.

**Det værktøj, jeg har brug for, er ikke på listen**

Du er sandsynligvis forbundet til et toolset, der ikke dækker det job. Tjek [endpoint-kataloget](#endpoint-directory) og tilføj det toolset, der gør — forbindelser er uafhængige, så tilføjelse af ét ændrer ikke noget ved de andre. Hvis du er usikker på, hvilket der har det, kan du midlertidigt forbinde til `https://api.wink.travel/mcp`, finde værktøjet og derefter se, hvilket toolset der lister det.

**`404 Not Found` på et toolset-endpoint**

Tjek stien nøje — de indlejrede toolsets har to segmenter (`/mcp/extranet/rates`, ikke `/mcp/extranet-rates`), og de flade har ét (`/mcp/link-manager`). Bemærk, at en *unauthentificeret* forespørgsel til en hvilken som helst sti under `/mcp` returnerer `401`, ikke `404`, så en `401` alene fortæller ikke, om endpointet findes.

**Jeg forbandt til `api.wink.travel/mcp/channel-manager` og fik intet**

Det endpoint findes ikke. Channel manager-integration serveres kun fra `https://integrations.wink.travel/mcp/channel-manager`. Hvis du også skal læse eller skrive en ejendoms egne priser manuelt, er det et separat job på et separat endpoint — se `/mcp/extranet/rates`.

**Serveren vises, men værktøjskald returnerer "account not found"**

Du er logget ind på en Wink-konto uden en administrationsenhed. Gå til [Wink Portal](https://app.wink.travel) og sørg for, at din konto er sat op, før du bruger MCP-serveren.

**Token udløber midt i sessionen**

Tokens er gyldige i 3 timer. Claude Code og Claude Desktop opdaterer tokens stille uden at afbryde din session. Hvis du ser autentificeringsfejl efter lang inaktivitet, skriv en ny besked — klienten genautentificerer automatisk.

**Codex CLI viser "MCP server unreachable"**

Tjek, at din `~/.codex/config.json` er gyldig JSON (ingen afsluttende kommaer) og at `"type": "http"` er sat. Codex kræver HTTP-transport for fjernservere; stdio-transport er kun til lokale binærer.

**ChatGPT "Connectors"-menu mangler**

MCP-support kræver ChatGPT desktop-app version **1.2024.352** eller nyere og et aktivt Plus-, Pro-, Team- eller Enterprise-abonnement. Opdater appen og tjek din plan under Indstillinger → Konto.

**Cursor-server viser rød / "Failed to connect"**

Bekræft, at din `~/.cursor/mcp.json` er gyldig JSON og indeholder `"type": "http"`. Gå derefter til **Indstillinger → Cursor Settings → MCP** og klik på **Opdater**. Hvis fejlen fortsætter, genstart Cursor helt — konfigurationsændringer opfanges ikke altid uden genstart.

**Windsurf MCP-servere vises ikke i indstillinger**

Windsurf kræver en fuld genstart (ikke kun genindlæsning af vindue) for at opfange ændringer i `mcp_config.json`. Brug **Fil → Afslut** og genstart. Hvis serverne stadig ikke vises, bekræft, at du kører Windsurf **1.4+** via **Hjælp → Om Windsurf**.

**Cline "Add Server"-mulighed er ikke synlig**

MCP-servermanageren kræver Cline **2.0** eller nyere. Åbn VS Code Extensions-panelet, find Cline, og klik på **Opdater**, hvis en opdatering er tilgængelig. Efter opdatering, genindlæs VS Code (`Ctrl+Shift+P` → "Developer: Reload Window").

---

<LinkButton href="https://app.wink.travel" variant="primary" icon="external">Åbn Wink Portal</LinkButton>
<LinkButton href="https://github.com/wink-travel" variant="secondary" icon="external">Wink på GitHub</LinkButton>
