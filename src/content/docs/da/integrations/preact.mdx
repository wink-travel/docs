---
title: Preact
description: Brug Wink webkomponenter i Preact-applikationer — samme mønster som React med et mindre footprint.
sidebar:
  order: 24
---

import { Aside, LinkCard } from '@astrojs/starlight/components';

Preacts API er kompatibel med React. Integrationen følger samme mønster — kald `load()` én gang ved app-opstart, og brug derefter `<wink-*>` tags i JSX.

<LinkCard
  title="@wink/elements npm package"
  href="/integrations/npm"
  description="Installer og indlæs CDN-bundtet med TypeScript-typer."
/>

## Installér

```bash
npm install @wink/elements
```

## Indlæs én gang ved app-opstart

```tsx
// src/app.tsx
import { useEffect } from 'preact/hooks';
import { load } from '@wink/elements';

export function App() {
  useEffect(() => {
    load({ clientId: import.meta.env.VITE_WINK_CLIENT_ID });
  }, []);

  return <YourRoutes />;
}
```

## Brug i JSX

```tsx
// src/pages/hotels.tsx
export function HotelsPage() {
  return (
    <main>
      <wink-content-loader layout="HOTEL" id="YOUR_LAYOUT_ID" />
      <wink-lookup />
    </main>
  );
}
```

## TypeScript — deklarér JSX intrinsics

Preacts JSX-namespace er adskilt fra Reacts. Udvid `preact/src/jsx.d.ts`:

```ts
// src/wink-elements.d.ts
import type {
  WinkContentLoaderAttributes,
  WinkLookupAttributes,
  WinkSearchButtonAttributes,
  WinkAccountButtonAttributes,
  WinkItineraryButtonAttributes,
  WinkShoppingCartButtonAttributes,
  WinkAppLoaderAttributes,
} from '@wink/elements';

declare module 'preact' {
  namespace JSX {
    interface IntrinsicElements {
      'wink-content-loader': WinkContentLoaderAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-lookup': WinkLookupAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-search-button': WinkSearchButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-account-button': WinkAccountButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-itinerary-button': WinkItineraryButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-shopping-cart-button': WinkShoppingCartButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-app-loader': WinkAppLoaderAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
    }
  }
}
```

## Preact med `preact/compat`

Hvis du aliaser React til `preact/compat`, brug Reacts JSX-deklarationer i stedet:

```ts
// src/wink-elements.d.ts
// Brug React JSX-deklarationen fra /integrations/react
// — preact/compat re-eksporterer Reacts JSX-namespace
```

<Aside type="tip">
Preacts mindre bundle-størrelse er et godt match til at indlejre Wink-komponenter i performance-følsomme webshopper. `@wink/elements` `load()` funktionen tilføjer ~500 bytes gzippet — størstedelen af vægten kommer fra CDN-bundtet, som indlæses asynkront.
</Aside>

## Yderligere læsning

- [Web Components reference](/da/integrations/web-components)
- [@wink/elements npm package](/da/integrations/npm)
- [React integration](/da/integrations/react)
