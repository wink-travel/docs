---
title: Nuxt 3
description: Afegeix components web de Wink a aplicacions Nuxt 3 mitjançant un plugin client.
sidebar:
  order: 19
---

import { Aside, LinkCard } from '@astrojs/starlight/components';

Nuxt 3 renderitza les pàgines al servidor per defecte. Els scripts CDN i els elements personalitzats s’han de carregar només al client. Un plugin client de Nuxt és el lloc més net per fer-ho.

<LinkCard
  title="@wink/elements npm package"
  href="/integrations/npm"
  description="Instal·la i carrega el paquet CDN amb tipus TypeScript."
/>

## Instal·lar

```bash
npm install @wink/elements
```

## Crear un plugin client

El sufix `.client.ts` indica a Nuxt que aquest plugin s’executi només al navegador:

```ts
// plugins/wink.client.ts
import { load } from '@wink/elements';

export default defineNuxtPlugin(() => {
  load({ clientId: useRuntimeConfig().public.winkClientId });
});
```

## Configurar la variable d’execució

```ts
// nuxt.config.ts
export default defineNuxtConfig({
  runtimeConfig: {
    public: {
      winkClientId: process.env.NUXT_PUBLIC_WINK_CLIENT_ID,
    },
  },
});
```

Afegeix a `.env`:

```
NUXT_PUBLIC_WINK_CLIENT_ID=your-client-id
```

## Configurar Vue per a elements personalitzats

Afegeix l’opció `isCustomElement` al compilador perquè Vue no avisi dels tags desconeguts `<wink-*>`:

```ts
// nuxt.config.ts
export default defineNuxtConfig({
  vue: {
    compilerOptions: {
      isCustomElement: (tag) => tag.startsWith('wink-'),
    },
  },
  runtimeConfig: {
    public: {
      winkClientId: process.env.NUXT_PUBLIC_WINK_CLIENT_ID,
    },
  },
});
```

## Ús en plantilles

Envolta els components Wink amb `<ClientOnly>` per evitar desajustos en la hidratació SSR:

```vue
<template>
  <main>
    <ClientOnly>
      <wink-content-loader layout="HOTEL" :id="layoutId" />
    </ClientOnly>
  </main>
</template>

<script setup lang="ts">
const layoutId = 'YOUR_LAYOUT_ID';
</script>
```

<Aside type="tip">
`<ClientOnly>` renderitza un marcador de posició al servidor i munta l’element real al client — aquest és el patró recomanat per a totes les incrustacions de tercers només per navegador a Nuxt.
</Aside>

## Tipus TypeScript

Exten la interfície global `HTMLElementTagNameMap` (igual que Vue 3) per obtenir autocompletat d’atributs. Consulta la [pàgina d’integració Vue 3](/ca/integrations/vue#typescript--augment-global-element-types) per al fitxer de declaració complet.

## Lectures addicionals

- [Referència de Web Components](/ca/integrations/web-components)
- [Paquet npm @wink/elements](/ca/integrations/npm)
- [Integració Vue 3](/ca/integrations/vue)
