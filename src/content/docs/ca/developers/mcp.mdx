---
title: Servidors MCP
description: Connecta agents d'IA a la plataforma Wink mitjançant el Model Context Protocol. Els servidors MCP autenticats donen al teu agent accés directe a l'inventari, reserves, integracions amb gestors de canals i més, tot assegurat amb un inici de sessió interactiu OAuth2.
sidebar:
  order: 10
---

import { Steps, LinkButton, Aside, Tabs, TabItem } from '@astrojs/starlight/components';

Wink exposa dos servidors MCP autenticats. Ambdós utilitzen **inici de sessió interactiu OAuth2** (flux de codi d'autorització PKCE): el teu agent d'IA obre una pestanya del navegador perquè signin amb el teu compte Wink. No cal gestionar secrets de client.

| Servidor | URL | Què fa |
|--------|-----|--------------|
| **Inventory API** | `https://api.wink.travel/mcp` | Inventari, reserves, tarifes, canals de venda, analítiques i totes les operacions de la plataforma |
| **Integrations** | `https://integrations.wink.travel/mcp` | Integracions amb gestors de canals — sincronitza propietats, tarifes i disponibilitat amb OTAs |

---

## Com funciona l'autenticació

Els servidors MCP de Wink utilitzen el flux **OAuth2 Authorization Code + PKCE** i suporten **Dynamic Client Registration (RFC 7591)**. Això és el que passa quan et connectes per primera vegada:

1. El client d'IA descobreix el servidor d'autorització a partir de les metadades `/.well-known/oauth-protected-resource` de l'endpoint MCP.
2. El client es registra automàticament mitjançant **Dynamic Client Registration** — no cal configurar l'aplicació manualment.
3. El client obre una **pestanya del navegador** a `https://iam.wink.travel`. Inicies sessió amb el teu compte Wink i aproves els permisos sol·licitats a la pantalla de consentiment.
4. Wink emet un **token d'accés** vinculat al teu compte i a la teva identitat.
5. El client d'IA adjunta el token a cada petició MCP. Tots els resultats d'eines es limiten automàticament a la teva entitat gestora.
6. Els tokens caduquen després de 3 hores. El client d'IA els renova silenciosament en segon pla sense tornar a demanar inici de sessió.

Cada crida a una eina s'executa **amb la teva identitat** — s'apliquen les mateixes regles ACL que protegeixen el portal web.

---

## Connecta el teu client d'IA

<Tabs>
  <TabItem label="Claude Code">

<Steps>
1. Assegura't que Claude Code està actualitzat:
   ```bash
   npm install -g @anthropic-ai/claude-code
   ```

2. Afegeix el servidor MCP Inventory de Wink:
   ```bash
   claude mcp add wink-inventory https://api.wink.travel/mcp --transport http
   ```

3. Claude Code contacta el servidor, descobreix el servidor d'autorització i obre el navegador per defecte.

4. **Inicia sessió a Wink** i aprova la sol·licitud de permisos a la pantalla de consentiment.

5. Claude Code confirma la connexió. Escriu `/mcp` per verificar que el servidor apareix i que les seves eines s'han carregat.
</Steps>

Per afegir també el servidor Integrations:
```bash
claude mcp add wink-integrations https://integrations.wink.travel/mcp --transport http
```

:::tip[Comparteix amb el teu equip]
Afegeix `--scope project` a `claude mcp add` i Claude Code escriurà la URL del servidor a `.mcp.json` a l'arrel del repositori. Fes commit d'aquest fitxer — cada membre de l'equip completarà el seu propi inici de sessió al navegador la primera vegada que es connecti.
:::

  </TabItem>
  <TabItem label="Claude Desktop">

<Steps>
1. Edita el fitxer de configuració de Claude Desktop:
   - **macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`
   - **Windows:** `%APPDATA%\Claude\claude_desktop_config.json`
   - **Linux:** `~/.config/Claude/claude_desktop_config.json`

2. Afegeix les entrades dels servidors MCP:

   ```json title="claude_desktop_config.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

3. **Tanca completament i reinicia** Claude Desktop (fes servir Quit des de la barra de menú — només tancar la finestra no és suficient).

4. Claude Desktop obre una pestanya del navegador per a cada servidor nou. Inicia sessió a Wink i aprova els permisos.

5. L'icona d'endoll a la barra inferior mostra ambdós servidors com a connectats.
</Steps>

  </TabItem>
  <TabItem label="OpenAI Codex">

<Steps>
1. Assegura't que Codex CLI està instal·lat:
   ```bash
   npm install -g @openai/codex
   ```

2. Obre (o crea) el fitxer de configuració de Codex:
   - **macOS / Linux:** `~/.codex/config.json`
   - **Windows:** `%USERPROFILE%\.codex\config.json`

3. Afegeix les entrades dels servidors MCP de Wink:

   ```json title="~/.codex/config.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

4. Inicia Codex. A la primera utilització, Codex obre una pestanya del navegador a `https://iam.wink.travel`.

5. **Inicia sessió a Wink** i aprova els permisos sol·licitats.

6. Codex confirma la connexió. Executa `codex` i demana que llisti les eines disponibles per verificar que els servidors Wink estan actius.
</Steps>

:::note
El suport MCP de Codex CLI requereix la versió **1.0** o superior. Executa `codex --version` per comprovar-ho i `npm install -g @openai/codex` per actualitzar.
:::

  </TabItem>
  <TabItem label="ChatGPT">

El suport per a servidors MCP està disponible a l'**aplicació d'escriptori ChatGPT** per a subscriptors Plus, Pro, Team i Enterprise.

<Steps>
1. Descarrega i instal·la l'[aplicació d'escriptori ChatGPT](https://openai.com/chatgpt/download/) si encara no ho has fet.

2. Obre l'aplicació i fes clic a la **icona del teu perfil** a la cantonada superior dreta, després tria **Configuració**.

3. Ves a **Connectors** (o **Servidors MCP** segons la versió de l'aplicació) i fes clic a **Afegeix**.

4. Introdueix la URL del servidor Inventory de Wink:
   ```
   https://api.wink.travel/mcp
   ```
   Després fes clic a **Connecta**.

5. ChatGPT obre una pestanya del navegador a `https://iam.wink.travel`. **Inicia sessió a Wink** i aprova els permisos.

6. Repeteix els passos 4–5 per al servidor Integrations:
   ```
   https://integrations.wink.travel/mcp
   ```

7. Ambdós servidors apareixen a la teva llista de Connectors. Comença un nou xat — les eines de Wink estan disponibles automàticament.
</Steps>

:::note[Limitació de l'aplicació web]
Els servidors MCP només es poden configurar a l'**aplicació d'escriptori**. La interfície web de ChatGPT a chatgpt.com no suporta servidors MCP personalitzats.
:::

  </TabItem>
  <TabItem label="Cursor">

<Steps>
1. Obre (o crea) el fitxer de configuració MCP de Cursor:
   - **macOS / Linux:** `~/.cursor/mcp.json`
   - **Windows:** `%USERPROFILE%\.cursor\mcp.json`

2. Afegeix les entrades dels servidors MCP de Wink:

   ```json title="~/.cursor/mcp.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

3. Obre Cursor. Ves a **Configuració → Configuració de Cursor → MCP** i comprova que ambdós servidors apareixen amb un indicador d'estat verd. Si apareixen en vermell, fes clic a **Actualitza**.

4. Cursor obre una pestanya del navegador a `https://iam.wink.travel`. **Inicia sessió a Wink** i aprova els permisos.

5. Obre un xat (`Ctrl+L` / `Cmd+L`) i demana a Cursor que llisti les eines Wink disponibles per confirmar la connexió.
</Steps>

:::tip[Configuració amb àmbit de projecte]
També pots col·locar un fitxer `mcp.json` a l'arrel del teu projecte en lloc de `~/.cursor/`. La configuració a nivell de projecte té prioritat i fa que els servidors estiguin disponibles per a cada membre de l'equip que obri el projecte a Cursor.
:::

  </TabItem>
  <TabItem label="Windsurf">

<Steps>
1. Obre (o crea) el fitxer de configuració MCP de Windsurf:
   - **macOS / Linux:** `~/.codeium/windsurf/mcp_config.json`
   - **Windows:** `%USERPROFILE%\.codeium\windsurf\mcp_config.json`

2. Afegeix les entrades dels servidors MCP de Wink:

   ```json title="~/.codeium/windsurf/mcp_config.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

3. **Reinicia completament Windsurf** (Fitxer → Surt, després torna a obrir).

4. Ves a **Configuració → Servidors MCP** i comprova que ambdós servidors apareixen com a connectats. Windsurf obre una pestanya del navegador a `https://iam.wink.travel` a la primera utilització.

5. **Inicia sessió a Wink** i aprova els permisos. Els servidors es posen verds al panell de configuració un cop autenticats.
</Steps>

:::note
El suport MCP de Windsurf requereix la versió **1.4** o superior. Comprova-ho a **Ajuda → Sobre Windsurf** i actualitza des del lloc web de Windsurf si cal.
:::

  </TabItem>
  <TabItem label="Cline (VS Code)">

Cline és una extensió de VS Code amb un gestor de servidors MCP integrat. No cal editar JSON manualment.

<Steps>
1. Instal·la Cline des del Marketplace de VS Code:
   - Obre VS Code i prem `Ctrl+Shift+X` / `Cmd+Shift+X`
   - Cerca **Cline** i fes clic a **Instal·la**

2. Fes clic a la **icona de Cline** a la barra d'activitats (barra lateral esquerra) per obrir el panell de Cline.

3. Fes clic a la icona de **Servidors MCP** (icona d'endoll) a la barra d'eines de Cline, després tria **Edita la configuració MCP**.

4. VS Code obre `cline_mcp_settings.json`. Afegeix les entrades de Wink:

   ```json title="cline_mcp_settings.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

5. **Desa el fitxer.** Cline es recarrega automàticament i obre una pestanya del navegador a `https://iam.wink.travel`.

6. **Inicia sessió a Wink** i aprova els permisos. Ambdós servidors apareixen actius al panell de Servidors MCP.
</Steps>

:::tip[Alternativa: afegeix via URL]
Al panell de Servidors MCP, fes clic a **Afegeix servidor** i enganxa directament `https://api.wink.travel/mcp`. Cline omple automàticament la configuració i gestiona l'autenticació sense editar JSON manualment.
:::

  </TabItem>
</Tabs>

---

## Referència d'àmbits

Quan aproves la pantalla de consentiment veuràs un desglossament del que l'agent sol·licita. Wink agrupa els permisos per àrea funcional:

| Grup d'àmbits | Àmbits | Què desbloquegen |
|-------------|--------|-----------------|
| **Transport MCP** | `mcp.read` `mcp.write` `mcp.remove` | Requerit per connectar a qualsevol servidor MCP |
| **Compte** | `account.read` `account.write` `account.remove` | Entitats gestores, membres de l'equip, configuracions, aplicacions |
| **Reserva** | `booking.read` `booking.write` `booking.remove` | Cerca, visualització, creació, cancel·lació i reemborsament de reserves |
| **Inventari** | `inventory.read` `inventory.write` `inventory.remove` | Propietats, habitacions, tarifes, serveis, disponibilitat |
| **Analítiques** | `analytics.read` | Analítiques de reserves, informes d'ingressos, classificacions |
| **Màrqueting** | `marketing.read` `marketing.write` `marketing.remove` | Publicacions socials, campanyes, canals de venda, promocions |
| **Contingut** | `content.read` `content.write` `content.remove` | Multimèdia, URLs curtes, elements sindicables, mapes |
| **Pagament** | `payment.read` `payment.write` | Llibre major, pagaments, desglossaments financers, retirades |

El client d'IA sol·licita només els àmbits que necessita. Pots denegar permisos individuals a la pantalla de consentiment — les eines que requereixin un àmbit denegat retornaran un error quan s'invocin.

---

## Resolució de problemes

**No s'obre la pestanya del navegador**

El teu client d'IA potser encara no suporta el flux interactiu PKCE. Verifica que tens una versió recent:
- **Claude Code:** `npm install -g @anthropic-ai/claude-code`
- **Codex CLI:** `npm install -g @openai/codex`
- **Claude Desktop / ChatGPT d'escriptori:** actualitza des de l'actualitzador integrat de l'aplicació

**`401 Unauthorized` després d'iniciar sessió**

El token emmagatzemat pot estar caducat. Elimina i torna a afegir el servidor per forçar un nou inici de sessió:

```bash
# Claude Code
claude mcp remove wink-inventory
claude mcp add wink-inventory https://api.wink.travel/mcp --transport http
```

Per a Claude Desktop i ChatGPT, elimina l'entrada del servidor a la configuració, reinicia l'aplicació i torna a afegir-lo.

**`403 Forbidden` en cridar una eina específica**

L'eina requereix un àmbit que no vas aprovar a la pantalla de consentiment. Torna a connectar el servidor — la pantalla de consentiment apareixerà de nou i podràs aprovar el permís que falta.

**El servidor apareix però les crides a eines retornen "compte no trobat"**

Estàs signat amb un compte Wink que no té una entitat gestora. Ves a [Wink Portal](https://app.wink.travel) i assegura't que el teu compte està configurat abans d'utilitzar el servidor MCP.

**El token caduca a mitja sessió**

Els tokens són vàlids durant 3 hores. Claude Code i Claude Desktop renoven els tokens silenciosament sense interrompre la sessió. Si veus errors d'autenticació després d'un període llarg d'inactivitat, escriu un nou missatge — el client es reautenticarà automàticament.

**Codex CLI mostra "MCP server unreachable"**

Comprova que el teu `~/.codex/config.json` és un JSON vàlid (sense comes finals) i que `"type": "http"` està configurat. Codex requereix transport HTTP per a servidors remots; el transport stdio és només per a binaris locals.

**El menú "Connectors" de ChatGPT no apareix**

El suport MCP requereix la versió **1.2024.352** o superior de l'aplicació d'escriptori ChatGPT i una subscripció activa Plus, Pro, Team o Enterprise. Actualitza l'aplicació i comprova el teu pla a Configuració → Compte.

**El servidor Cursor apareix en vermell / "Failed to connect"**

Verifica que el teu `~/.cursor/mcp.json` és un JSON vàlid i conté `"type": "http"`. Després ves a **Configuració → Configuració de Cursor → MCP** i fes clic a **Actualitza**. Si l'error persisteix, reinicia completament Cursor — els canvis de configuració no sempre es detecten sense reinici.

**Els servidors MCP de Windsurf no apareixen a la configuració**

Windsurf requereix un reinici complet (no només recarregar la finestra) per detectar canvis a `mcp_config.json`. Fes servir **Fitxer → Surt** i torna a obrir. Si els servidors encara no apareixen, comprova que tens Windsurf **1.4+** a **Ajuda → Sobre Windsurf**.

**L'opció "Afegeix servidor" de Cline no és visible**

El gestor de servidors MCP requereix Cline **2.0** o superior. Obre el panell d'extensions de VS Code, busca Cline i fes clic a **Actualitza** si hi ha una actualització disponible. Després d'actualitzar, recarrega VS Code (`Ctrl+Shift+P` → "Developer: Reload Window").

---

<LinkButton href="https://app.wink.travel" variant="primary" icon="external">Obre Wink Portal</LinkButton>
<LinkButton href="https://github.com/wink-travel" variant="secondary" icon="external">Wink a GitHub</LinkButton>
