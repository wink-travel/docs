---
title: Catàleg d'Esdeveniments Webhook
description: Cada tipus d'esdeveniment webhook a la plataforma Wink, generat a partir del catàleg d'esdeveniments de la plataforma — qui els rep i on es documenta la càrrega útil.
sidebar:
  order: 3.5
---

import webhooks from '../../../../../schemas/webhooks.json';
import WebhookEventTypes from '@/components/webhooks/event-types.astro';

Aquesta pàgina llista cada esdeveniment webhook al qual et pots subscriure a Wink. Per a instruccions de configuració, consulta la [Guia d'Integració Webhook](/ca/developers/webhook-integration) o la [pàgina de gestió de Webhooks](/ca/portal/settings/webhooks).

## Com subscriure's

1. Navega a **Aplicacions > Webhooks** al teu portal.
2. Crea un webhook i selecciona els esdeveniments que vols rebre — o deixa la llista buida per rebre tots els esdeveniments.
3. Wink envia un HTTP POST signat al teu endpoint cada vegada que l'esdeveniment ocorre. Consulta [Integració Webhook](/ca/developers/webhook-integration) per a l'estructura, capçaleres, verificació de signatura i reintents.

## Cada tipus d'esdeveniment

Aquesta taula es genera a partir del catàleg d'esdeveniments de la plataforma en temps de compilació, així que sempre coincideix amb l'API.
Els tipus d'esdeveniments marcats com a entregats enllacen a la seva pàgina de referència a la [API de Webhooks](/ca/api/webhooks/), que
documenta el cos JSON exacte (`WebhookEnvelope` amb un `objecte` específic de l'esdeveniment), les capçaleres `Wink-*` i
la política de reintents. Els tipus d'esdeveniments que no s'entreguen existeixen internament (pagaments, llibre major, sincronització del gestor de canals, …) però actualment no s'envien als subscriptors webhook.

**Destinataris** indica qui rep un esdeveniment: `SUPPLIER` (l'hotel), `AFFILIATE` (un revenedor amb un canal de vendes actiu amb l'hotel), `AGENT` (l'agència de viatges que va fer la reserva) o `SELF` (l'entitat gestora sobre la qual tracta l'esdeveniment). Cada destinatari rep la mateixa forma de càrrega útil; el detall específic per rol està darrere de l'URL `links.self` de la càrrega útil.

<WebhookEventTypes
  types={webhooks['x-wink-event-types']}
  referenceBase="/api/webhooks"
  eventHeader="Esdeveniment"
  descriptionHeader="Descripció"
  deliveredHeader="Entregat"
  recipientsHeader="Destinataris"
  deliveredLabel="entregat"
  notDeliveredLabel="no entregat"
  noRecipientsLabel="—"
/>

:::tip
Comença subscrivint-te a tots els esdeveniments durant el desenvolupament. Un cop entenguis quines càrregues útils necessita el teu sistema, restringeix les teves subscripcions per reduir el soroll. Envia't un esdeveniment de prova des de **Aplicacions > Webhooks** (o `POST …/webhook/{id}/test`) per confirmar el teu endpoint i la verificació de signatura abans d'anar a producció.
:::

## Lectures addicionals

- [Guia d'Integració Webhook](/ca/developers/webhook-integration) — Passos de configuració i millors pràctiques.
- [Webhooks](/ca/portal/settings/webhooks) — Gestiona les teves subscripcions webhook.
- [Aplicacions](/ca/account/applications) — Gestiona les credencials API.
- [Desenvolupa a Wink](/ca/developers/build-on-wink) — Visió general de la plataforma per a desenvolupadors.
