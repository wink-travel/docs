---
title: Servere MCP
description: Conectează agenții AI la platforma Wink prin Model Context Protocol. Serverele MCP autentificate oferă agentului tău acces direct la inventar, rezervări, integrări cu manageri de canale și altele — securizat prin autentificare interactivă OAuth2.
sidebar:
  order: 10
---

import { Steps, LinkButton, Aside, Tabs, TabItem } from '@astrojs/starlight/components';

Wink oferă două servere MCP autentificate. Ambele folosesc **autentificare interactivă OAuth2** (fluxul codului de autorizare PKCE) — agentul tău AI deschide o filă de browser pentru a te autentifica cu contul Wink. Nu sunt necesare secrete client de gestionat.

| Server | URL | Ce face |
|--------|-----|---------|
| **Inventory API** | `https://api.wink.travel/mcp` | Inventar, rezervări, tarife, canale de vânzare, analize și toate operațiunile platformei |
| **Integrations** | `https://integrations.wink.travel/mcp` | Integrări cu manageri de canale — sincronizează proprietăți, tarife și disponibilitate cu OTA-uri |

---

## Cum funcționează autentificarea

Serverele MCP Wink folosesc fluxul **OAuth2 Authorization Code + PKCE** și suportă **Dynamic Client Registration (RFC 7591)**. Iată ce se întâmplă când te conectezi prima dată:

1. Clientul tău AI descoperă serverul de autorizare din metadatele endpoint-ului MCP `/.well-known/oauth-protected-resource`.
2. Clientul se înregistrează singur prin **Dynamic Client Registration** — nu este necesară configurare manuală a aplicației.
3. Clientul deschide o **filă de browser** către `https://iam.wink.travel`. Te autentifici cu contul Wink și aprobi permisiunile solicitate pe ecranul de consimțământ.
4. Wink emite un **token de acces** legat de contul și identitatea ta.
5. Clientul AI atașează tokenul la fiecare cerere MCP. Toate rezultatele uneltelor sunt automat limitate la entitatea ta de administrare.
6. Tokenurile expiră după 3 ore. Clientul AI le reîmprospătează silențios în fundal fără a cere din nou autentificare.

Fiecare apel de unealtă rulează **sub identitatea ta** — aceleași reguli ACL care protejează portalul web se aplică și agentului AI.

---

## Conectează clientul tău AI

<Tabs>
  <TabItem label="Claude Code">

<Steps>
1. Asigură-te că Claude Code este actualizat:
   ```bash
   npm install -g @anthropic-ai/claude-code
   ```

2. Adaugă serverul Wink Inventory MCP:
   ```bash
   claude mcp add wink-inventory https://api.wink.travel/mcp --transport http
   ```

3. Claude Code contactează serverul, descoperă serverul de autorizare și deschide browserul implicit.

4. **Autentifică-te în Wink** și aprobă cererea de permisiuni pe ecranul de consimțământ.

5. Claude Code confirmă conexiunea. Tastează `/mcp` pentru a verifica dacă serverul apare și uneltele s-au încărcat.
</Steps>

Pentru a adăuga și serverul Integrations:
```bash
claude mcp add wink-integrations https://integrations.wink.travel/mcp --transport http
```

:::tip[Distribuie cu echipa ta]
Adaugă `--scope project` la `claude mcp add` și Claude Code va scrie URL-ul serverului în `.mcp.json` la rădăcina repo-ului. Comite acel fișier — fiecare membru al echipei își va finaliza autentificarea în browser prima dată când se conectează.
:::

  </TabItem>
  <TabItem label="Claude Desktop">

<Steps>
1. Editează fișierul de configurare Claude Desktop:
   - **macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`
   - **Windows:** `%APPDATA%\Claude\claude_desktop_config.json`
   - **Linux:** `~/.config/Claude/claude_desktop_config.json`

2. Adaugă intrările serverelor MCP:

   ```json title="claude_desktop_config.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

3. **Închide complet și repornește** Claude Desktop (folosește Quit din bara de meniu — închiderea ferestrei nu este suficientă).

4. Claude Desktop deschide o filă de browser pentru fiecare server nou. Autentifică-te în Wink și aprobă permisiunile.

5. Pictograma de priză din bara de jos arată ambele servere ca fiind conectate.
</Steps>

  </TabItem>
  <TabItem label="OpenAI Codex">

<Steps>
1. Asigură-te că Codex CLI este instalat:
   ```bash
   npm install -g @openai/codex
   ```

2. Deschide (sau creează) fișierul de configurare Codex:
   - **macOS / Linux:** `~/.codex/config.json`
   - **Windows:** `%USERPROFILE%\.codex\config.json`

3. Adaugă intrările serverelor Wink MCP:

   ```json title="~/.codex/config.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

4. Pornește Codex. La prima utilizare, Codex deschide o filă de browser către `https://iam.wink.travel`.

5. **Autentifică-te în Wink** și aprobă permisiunile solicitate.

6. Codex confirmă conexiunea. Rulează `codex` și cere să listeze uneltele disponibile pentru a verifica dacă serverele Wink sunt active.
</Steps>

:::note
Suportul MCP în Codex CLI necesită versiunea **1.0** sau mai recentă. Rulează `codex --version` pentru a verifica și `npm install -g @openai/codex` pentru a face upgrade.
:::

  </TabItem>
  <TabItem label="ChatGPT">

Suportul pentru servere MCP este disponibil în **aplicația desktop ChatGPT** pentru abonații Plus, Pro, Team și Enterprise.

<Steps>
1. Descarcă și instalează [aplicația desktop ChatGPT](https://openai.com/chatgpt/download/) dacă nu ai făcut-o deja.

2. Deschide aplicația și dă clic pe **pictograma profilului tău** din colțul dreapta sus, apoi alege **Settings**.

3. Mergi la **Connectors** (sau **MCP Servers** în funcție de versiunea aplicației) și dă clic pe **Add**.

4. Introdu URL-ul serverului Wink Inventory:
   ```
   https://api.wink.travel/mcp
   ```
   Apoi dă clic pe **Connect**.

5. ChatGPT deschide o filă de browser către `https://iam.wink.travel`. **Autentifică-te în Wink** și aprobă permisiunile.

6. Repetă pașii 4–5 pentru serverul Integrations:
   ```
   https://integrations.wink.travel/mcp
   ```

7. Ambele servere apar în lista ta de Connectors. Începe o conversație nouă — uneltele Wink sunt disponibile automat.
</Steps>

:::note[Limitare aplicație web]
Serverele MCP pot fi configurate doar în **aplicația desktop**. Interfața web ChatGPT la chatgpt.com nu suportă servere MCP personalizate.
:::

  </TabItem>
  <TabItem label="Cursor">

<Steps>
1. Deschide (sau creează) fișierul de configurare Cursor MCP:
   - **macOS / Linux:** `~/.cursor/mcp.json`
   - **Windows:** `%USERPROFILE%\.cursor\mcp.json`

2. Adaugă intrările serverelor Wink MCP:

   ```json title="~/.cursor/mcp.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

3. Deschide Cursor. Mergi la **Settings → Cursor Settings → MCP** și confirmă că ambele servere apar cu indicator verde de stare. Dacă apar roșu, dă clic pe **Refresh**.

4. Cursor deschide o filă de browser către `https://iam.wink.travel`. **Autentifică-te în Wink** și aprobă permisiunile.

5. Deschide o conversație (`Ctrl+L` / `Cmd+L`) și cere lui Cursor să listeze uneltele Wink disponibile pentru a confirma conexiunea.
</Steps>

:::tip[Config la nivel de proiect]
Poți plasa și un fișier `mcp.json` la rădăcina proiectului în loc de `~/.cursor/`. Configurația la nivel de proiect are prioritate și face serverele disponibile pentru fiecare membru al echipei care deschide proiectul în Cursor.
:::

  </TabItem>
  <TabItem label="Windsurf">

<Steps>
1. Deschide (sau creează) fișierul de configurare Windsurf MCP:
   - **macOS / Linux:** `~/.codeium/windsurf/mcp_config.json`
   - **Windows:** `%USERPROFILE%\.codeium\windsurf\mcp_config.json`

2. Adaugă intrările serverelor Wink MCP:

   ```json title="~/.codeium/windsurf/mcp_config.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

3. **Repornește complet Windsurf** (File → Quit, apoi redeschide).

4. Mergi la **Settings → MCP Servers** și verifică dacă ambele servere apar ca fiind conectate. Windsurf deschide o filă de browser către `https://iam.wink.travel` la prima utilizare.

5. **Autentifică-te în Wink** și aprobă permisiunile. Serverele devin verzi în panoul de setări după autentificare.
</Steps>

:::note
Suportul MCP în Windsurf necesită versiunea **1.4** sau mai recentă. Verifică prin **Help → About Windsurf** și actualizează de pe site-ul Windsurf dacă este nevoie.
:::

  </TabItem>
  <TabItem label="Cline (VS Code)">

Cline este o extensie VS Code cu manager MCP integrat. Nu este necesară editarea manuală a JSON-ului.

<Steps>
1. Instalează Cline din VS Code Marketplace:
   - Deschide VS Code și apasă `Ctrl+Shift+X` / `Cmd+Shift+X`
   - Caută **Cline** și dă clic pe **Install**

2. Dă clic pe **pictograma Cline** din bara de activități (sidebar stânga) pentru a deschide panoul Cline.

3. Dă clic pe pictograma **MCP Servers** (pictograma priză) din bara de unelte Cline, apoi alege **Edit MCP Settings**.

4. VS Code deschide `cline_mcp_settings.json`. Adaugă intrările Wink:

   ```json title="cline_mcp_settings.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

5. **Salvează fișierul.** Cline se reîncarcă automat și deschide o filă de browser către `https://iam.wink.travel`.

6. **Autentifică-te în Wink** și aprobă permisiunile. Ambele servere apar ca active în panoul MCP Servers.
</Steps>

:::tip[Alternativ: adaugă prin URL]
În panoul MCP Servers, dă clic pe **Add Server** și lipește direct `https://api.wink.travel/mcp`. Cline completează automat configurația și gestionează autentificarea fără a edita JSON manual.
:::

  </TabItem>
</Tabs>

---

## Referință scope-uri

Când aprobi ecranul de consimțământ vei vedea o defalcare a ceea ce solicită agentul. Wink grupează permisiunile pe arii funcționale:

| Grup scope | Scope-uri | Ce deblochează |
|------------|-----------|----------------|
| **Transport MCP** | `mcp.read` `mcp.write` `mcp.remove` | Necesare pentru conectarea la oricare server MCP |
| **Cont** | `account.read` `account.write` `account.remove` | Entități de administrare, membri echipă, setări, aplicații |
| **Rezervare** | `booking.read` `booking.write` `booking.remove` | Căutare, vizualizare, creare, anulare și rambursare rezervări |
| **Inventar** | `inventory.read` `inventory.write` `inventory.remove` | Proprietăți, camere, tarife, facilități, disponibilitate |
| **Analize** | `analytics.read` | Analize rezervări, rapoarte de venituri, clasamente |
| **Marketing** | `marketing.read` `marketing.write` `marketing.remove` | Postări sociale, campanii, canale de vânzare, promoții |
| **Conținut** | `content.read` `content.write` `content.remove` | Multimedia, URL-uri scurtate, elemente sindicabile, hărți |
| **Plată** | `payment.read` `payment.write` | Registru, plăți, defalcări financiare, retrageri |

Clientul AI solicită doar scope-urile de care are nevoie. Poți refuza permisiuni individuale pe ecranul de consimțământ — uneltele care necesită un scope refuzat vor returna o eroare la invocare.

---

## Depanare

**Fila de browser nu se deschide**

Clientul tău AI poate să nu suporte încă fluxul interactiv PKCE. Verifică dacă folosești o versiune recentă:
- **Claude Code:** `npm install -g @anthropic-ai/claude-code`
- **Codex CLI:** `npm install -g @openai/codex`
- **Claude Desktop / ChatGPT desktop:** actualizează din updaterul integrat al aplicației

**`401 Unauthorized` după autentificare**

Tokenul tău cache poate fi expirat. Elimină și adaugă din nou serverul pentru a declanșa o autentificare nouă:

```bash
# Claude Code
claude mcp remove wink-inventory
claude mcp add wink-inventory https://api.wink.travel/mcp --transport http
```

Pentru Claude Desktop și ChatGPT, elimină intrarea serverului din setări, repornește aplicația și adaugă-l din nou.

**`403 Forbidden` la apelul unei unelte specifice**

Unealta necesită un scope pe care nu l-ai aprobat pe ecranul de consimțământ. Reconectează serverul — ecranul de consimțământ va apărea din nou și poți aproba permisiunea lipsă.

**Serverul apare, dar apelurile uneltelor returnează "account not found"**

Ești autentificat cu un cont Wink care nu are o entitate de administrare. Accesează [Wink Portal](https://app.wink.travel) și asigură-te că contul tău este configurat înainte de a folosi serverul MCP.

**Tokenul expiră în timpul sesiunii**

Tokenurile sunt valabile 3 ore. Claude Code și Claude Desktop reîmprospătează tokenurile silențios fără a întrerupe sesiunea. Dacă vezi erori de autentificare după o perioadă lungă de inactivitate, tastează un mesaj nou — clientul se va reautentifica automat.

**Codex CLI afișează "MCP server unreachable"**

Verifică dacă `~/.codex/config.json` este JSON valid (fără virgule finale) și că `"type": "http"` este setat. Codex necesită transport HTTP pentru servere remote; transportul stdio este doar pentru binare locale.

**Meniul "Connectors" din ChatGPT lipsește**

Suportul MCP necesită versiunea aplicației desktop ChatGPT **1.2024.352** sau mai recentă și un abonament activ Plus, Pro, Team sau Enterprise. Actualizează aplicația și verifică planul în Settings → Account.

**Serverul Cursor apare roșu / "Failed to connect"**

Verifică dacă `~/.cursor/mcp.json` este JSON valid și conține `"type": "http"`. Apoi mergi la **Settings → Cursor Settings → MCP** și dă clic pe **Refresh**. Dacă eroarea persistă, repornește complet Cursor — modificările de configurare nu sunt întotdeauna preluate fără restart.

**Serverele MCP Windsurf nu apar în setări**

Windsurf necesită o repornire completă (nu doar reîncărcarea ferestrei) pentru a prelua modificările din `mcp_config.json`. Folosește **File → Quit** și redeschide aplicația. Dacă serverele încă nu apar, confirmă că folosești Windsurf **1.4+** prin **Help → About Windsurf**.

**Opțiunea "Add Server" din Cline nu este vizibilă**

Managerul MCP necesită Cline **2.0** sau mai recent. Deschide panoul Extensions din VS Code, găsește Cline și dă clic pe **Update** dacă există o actualizare. După actualizare, reîncarcă VS Code (`Ctrl+Shift+P` → "Developer: Reload Window").

---

<LinkButton href="https://app.wink.travel" variant="primary" icon="external">Deschide Wink Portal</LinkButton>
<LinkButton href="https://github.com/wink-travel" variant="secondary" icon="external">Wink pe GitHub</LinkButton>
