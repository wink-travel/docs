---
title: Preact
description: Folosește componentele web Wink în aplicații Preact — același model ca React, cu o amprentă mai mică.
sidebar:
  order: 24
---

import { Aside, LinkCard } from '@astrojs/starlight/components';

API-ul Preact este compatibil cu React. Integrarea urmează același model — apelează `load()` o singură dată la pornirea aplicației, apoi folosește tagurile `<wink-*>` în JSX.

<LinkCard
  title="@wink/elements npm package"
  href="/integrations/npm"
  description="Instalează și încarcă pachetul CDN cu tipuri TypeScript."
/>

## Instalare

```bash
npm install @wink/elements
```

## Încarcă o singură dată la pornirea aplicației

```tsx
// src/app.tsx
import { useEffect } from 'preact/hooks';
import { load } from '@wink/elements';

export function App() {
  useEffect(() => {
    load({ clientId: import.meta.env.VITE_WINK_CLIENT_ID });
  }, []);

  return <YourRoutes />;
}
```

## Folosește în JSX

```tsx
// src/pages/hotels.tsx
export function HotelsPage() {
  return (
    <main>
      <wink-content-loader layout="HOTEL" id="YOUR_LAYOUT_ID" />
      <wink-lookup />
    </main>
  );
}
```

## TypeScript — declară intrinsecurile JSX

Namespace-ul JSX al Preact este separat de cel al React. Extinde `preact/src/jsx.d.ts`:

```ts
// src/wink-elements.d.ts
import type {
  WinkContentLoaderAttributes,
  WinkLookupAttributes,
  WinkSearchButtonAttributes,
  WinkAccountButtonAttributes,
  WinkItineraryButtonAttributes,
  WinkShoppingCartButtonAttributes,
  WinkAppLoaderAttributes,
} from '@wink/elements';

declare module 'preact' {
  namespace JSX {
    interface IntrinsicElements {
      'wink-content-loader': WinkContentLoaderAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-lookup': WinkLookupAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-search-button': WinkSearchButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-account-button': WinkAccountButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-itinerary-button': WinkItineraryButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-shopping-cart-button': WinkShoppingCartButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-app-loader': WinkAppLoaderAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
    }
  }
}
```

## Preact cu `preact/compat`

Dacă faci alias React către `preact/compat`, folosește declarațiile JSX pentru React:

```ts
// src/wink-elements.d.ts
// Folosește declarația JSX React din /integrations/react
// — preact/compat re-exportă namespace-ul JSX al React
```

<Aside type="tip">
Dimensiunea mai mică a pachetului Preact este o alegere bună pentru încorporarea componentelor Wink în magazine online sensibile la performanță. Funcția `load()` din `@wink/elements` adaugă ~500 bytes comprimați — majoritatea greutății vine din pachetul CDN încărcat asincron.
</Aside>

## Lecturi suplimentare

- [Referință Web Components](/ro/integrations/web-components)
- [@wink/elements npm package](/ro/integrations/npm)
- [Integrare React](/ro/integrations/react)
