---
title: MCPサーバー
description: Model Context Protocolを介してAIエージェントをWinkプラットフォームに接続します。認証済みのMCPサーバーは、インタラクティブなOAuth2ログインで保護され、エージェントにインベントリ、予約、チャネルマネージャー統合などへの直接アクセスを提供します。
sidebar:
  order: 10
---

import { Steps, LinkButton, Aside, Tabs, TabItem } from '@astrojs/starlight/components';

Winkは2つの認証済みMCPサーバーを公開しています。どちらも**インタラクティブなOAuth2ログイン**（PKCE認可コードフロー）を使用しており、AIエージェントがブラウザタブを開いてWinkアカウントでサインインします。クライアントシークレットの管理は不要です。

| サーバー | URL | 機能 |
|--------|-----|--------------|
| **Inventory API** | `https://api.wink.travel/mcp` | インベントリ、予約、料金、販売チャネル、分析、プラットフォームのすべての操作 |
| **Integrations** | `https://integrations.wink.travel/mcp` | チャネルマネージャー統合 — OTAsとの物件、料金、空室状況の同期 |

---

## 認証の仕組み

Wink MCPサーバーは**OAuth2認可コード＋PKCE**フローを使用し、**Dynamic Client Registration (RFC 7591)**に対応しています。初回接続時の流れは以下の通りです：

1. AIクライアントはMCPエンドポイントの`/.well-known/oauth-protected-resource`メタデータから認可サーバーを検出します。
2. クライアントは**Dynamic Client Registration**を通じて自己登録します — 手動のアプリケーション設定は不要です。
3. クライアントは`https://iam.wink.travel`の**ブラウザタブ**を開きます。Winkアカウントでサインインし、同意画面で要求された権限を承認します。
4. Winkはアカウントに紐づく**アクセストークン**を発行します。
5. AIクライアントはすべてのMCPリクエストにトークンを添付します。すべてのツール結果は自動的に管理エンティティにスコープされます。
6. トークンは3時間で期限切れになります。AIクライアントはバックグラウンドで静かにトークンを更新し、再度のプロンプトはありません。

すべてのツール呼び出しは**あなたのIDのもとで**実行されます — ウェブポータルを保護するのと同じACLルールがAIエージェントにも適用されます。

---

## AIクライアントの接続方法

<Tabs>
  <TabItem label="Claude Code">

<Steps>
1. Claude Codeが最新であることを確認してください：
   ```bash
   npm install -g @anthropic-ai/claude-code
   ```

2. Wink Inventory MCPサーバーを追加します：
   ```bash
   claude mcp add wink-inventory https://api.wink.travel/mcp --transport http
   ```

3. Claude Codeがサーバーに接続し、認可サーバーを検出してデフォルトブラウザを開きます。

4. **Winkにサインイン**し、同意画面で権限を承認します。

5. Claude Codeが接続を確認します。`/mcp`と入力してサーバーが表示され、ツールが読み込まれていることを確認してください。
</Steps>

Integrationsサーバーも追加する場合：
```bash
claude mcp add wink-integrations https://integrations.wink.travel/mcp --transport http
```

:::tip[チームと共有]
`claude mcp add`に`--scope project`を追加すると、Claude CodeはサーバーURLをリポジトリルートの`.mcp.json`に書き込みます。そのファイルをコミットし、チームメンバーは初回接続時に各自ブラウザでサインインを完了します。
:::

  </TabItem>
  <TabItem label="Claude Desktop">

<Steps>
1. Claude Desktopの設定ファイルを編集します：
   - **macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`
   - **Windows:** `%APPDATA%\Claude\claude_desktop_config.json`
   - **Linux:** `~/.config/Claude/claude_desktop_config.json`

2. MCPサーバーのエントリを追加します：

   ```json title="claude_desktop_config.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

3. Claude Desktopを**完全に終了して再起動**します（メニューバーのQuitを使用してください。ウィンドウを閉じるだけでは不十分です）。

4. Claude Desktopは新しいサーバーごとにブラウザタブを開きます。Winkにサインインし、権限を承認してください。

5. 下部ツールバーのプラグアイコンが両方のサーバーが接続されていることを示します。
</Steps>

  </TabItem>
  <TabItem label="OpenAI Codex">

<Steps>
1. Codex CLIがインストールされていることを確認してください：
   ```bash
   npm install -g @openai/codex
   ```

2. Codexの設定ファイルを開く（または作成）します：
   - **macOS / Linux:** `~/.codex/config.json`
   - **Windows:** `%USERPROFILE%\.codex\config.json`

3. Wink MCPサーバーのエントリを追加します：

   ```json title="~/.codex/config.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

4. Codexを起動します。初回使用時に`https://iam.wink.travel`のブラウザタブが開きます。

5. **Winkにサインイン**し、要求された権限を承認します。

6. Codexが接続を確認します。`codex`を実行し、利用可能なツールをリストアップしてWinkサーバーが有効であることを確認してください。
</Steps>

:::note
Codex CLIのMCPサポートにはバージョン**1.0**以降が必要です。`codex --version`で確認し、`npm install -g @openai/codex`でアップグレードしてください。
:::

  </TabItem>
  <TabItem label="ChatGPT">

MCPサーバーのサポートは、Plus、Pro、Team、Enterpriseのサブスクライバー向けの**ChatGPTデスクトップアプリ**で利用可能です。

<Steps>
1. まだの場合は[ChatGPTデスクトップアプリ](https://openai.com/chatgpt/download/)をダウンロードしてインストールします。

2. アプリを開き、右上の**プロフィールアイコン**をクリックして**設定**を選択します。

3. **Connectors**（またはアプリのバージョンによっては**MCP Servers**）に移動し、**追加**をクリックします。

4. Wink InventoryサーバーのURLを入力します：
   ```
   https://api.wink.travel/mcp
   ```
   そして**接続**をクリックします。

5. ChatGPTが`https://iam.wink.travel`のブラウザタブを開きます。**Winkにサインイン**し、権限を承認します。

6. Integrationsサーバーについても同様に4〜5の手順を繰り返します：
   ```
   https://integrations.wink.travel/mcp
   ```

7. 両方のサーバーがConnectorsリストに表示されます。新しいチャットを開始すると、Winkツールが自動的に利用可能になります。
</Steps>

:::note[ウェブアプリの制限]
MCPサーバーは**デスクトップアプリ**でのみ設定可能です。chatgpt.comのウェブインターフェースはカスタムMCPサーバーをサポートしていません。
:::

  </TabItem>
  <TabItem label="Cursor">

<Steps>
1. CursorのMCP設定ファイルを開く（または作成）します：
   - **macOS / Linux:** `~/.cursor/mcp.json`
   - **Windows:** `%USERPROFILE%\.cursor\mcp.json`

2. Wink MCPサーバーのエントリを追加します：

   ```json title="~/.cursor/mcp.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

3. Cursorを開き、**設定 → Cursor設定 → MCP**に移動して両方のサーバーが緑のステータスで表示されていることを確認します。赤の場合は**更新**をクリックしてください。

4. Cursorが`https://iam.wink.travel`のブラウザタブを開きます。**Winkにサインイン**し、権限を承認します。

5. チャットを開き（`Ctrl+L` / `Cmd+L`）、Cursorに利用可能なWinkツールをリストアップするよう依頼して接続を確認します。
</Steps>

:::tip[プロジェクトスコープの設定]
`~/.cursor/`ではなくプロジェクトのルートに`mcp.json`ファイルを置くこともできます。プロジェクトレベルの設定が優先され、プロジェクトを開くチームメンバー全員がサーバーを利用可能になります。
:::

  </TabItem>
  <TabItem label="Windsurf">

<Steps>
1. WindsurfのMCP設定ファイルを開く（または作成）します：
   - **macOS / Linux:** `~/.codeium/windsurf/mcp_config.json`
   - **Windows:** `%USERPROFILE%\.codeium\windsurf\mcp_config.json`

2. Wink MCPサーバーのエントリを追加します：

   ```json title="~/.codeium/windsurf/mcp_config.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

3. **Windsurfを完全に再起動**します（ファイル → 終了、そして再起動）。

4. **設定 → MCPサーバー**に移動し、両方のサーバーが接続済みであることを確認します。初回使用時にWindsurfは`https://iam.wink.travel`のブラウザタブを開きます。

5. **Winkにサインイン**し、権限を承認します。認証後、設定パネルのサーバーが緑色に変わります。
</Steps>

:::note
WindsurfのMCPサポートにはバージョン**1.4**以降が必要です。**ヘルプ → Windsurfについて**で確認し、必要に応じてWindsurfのウェブサイトからアップデートしてください。
:::

  </TabItem>
  <TabItem label="Cline (VS Code)">

ClineはVS Code拡張機能で、組み込みのMCPサーバーマネージャーを備えています。手動でJSONを編集する必要はありません。

<Steps>
1. VS CodeマーケットプレイスからClineをインストールします：
   - VS Codeを開き、`Ctrl+Shift+X` / `Cmd+Shift+X`を押します
   - **Cline**を検索し、**インストール**をクリック

2. アクティビティバー（左サイドバー）の**Clineアイコン**をクリックしてClineパネルを開きます。

3. Clineツールバーの**MCPサーバーアイコン**（プラグアイコン）をクリックし、**MCP設定を編集**を選択します。

4. VS Codeが`cline_mcp_settings.json`を開きます。Winkのエントリを追加します：

   ```json title="cline_mcp_settings.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

5. **ファイルを保存**します。Clineは自動的にリロードされ、`https://iam.wink.travel`のブラウザタブを開きます。

6. **Winkにサインイン**し、権限を承認します。MCPサーバーパネルに両方のサーバーがアクティブとして表示されます。
</Steps>

:::tip[代替：URLで追加]
MCPサーバーパネルで**サーバーを追加**をクリックし、`https://api.wink.travel/mcp`を直接貼り付けます。Clineが自動的に設定を入力し、JSONを手動で編集せずに認証を処理します。
:::

  </TabItem>
</Tabs>

---

## スコープ

同意画面でエージェントが要求している内容の内訳が表示されます。MCPクライアントはプラットフォームの他の部分と同じ権限語彙を使用しています — 詳細は[認証ページのスコープリファレンス](/ja/developers/authentication#scopes)を参照してください。

MCP特有の点は2つあります：

- **`mcp.read`、`mcp.write`、`mcp.remove`スコープが追加で必要**で、MCPトランスポートを開きます。`mcp.*`スコープを持つクライアントは`/mcp`にアクセスできますが、呼び出す各ツールは依然としてそのエンドポイントのセクションスコープ（例：`booking.read`）を適用します。
- **AIクライアントは必要なスコープのみを要求します。** 同意画面で個別の権限を拒否できます — 拒否されたスコープを必要とするツールは呼び出し時にエラーを返します。

---

## トラブルシューティング

**ブラウザタブが開かない**

AIクライアントがまだインタラクティブなPKCEフローをサポートしていない可能性があります。最新バージョンを使用しているか確認してください：
- **Claude Code:** `npm install -g @anthropic-ai/claude-code`
- **Codex CLI:** `npm install -g @openai/codex`
- **Claude Desktop / ChatGPTデスクトップ:** アプリ内のアップデーターから更新

**サインイン後に`401 Unauthorized`が出る**

キャッシュされたトークンが古い可能性があります。サーバーを削除して再追加し、新しいサインインをトリガーしてください：

```bash
# Claude Code
claude mcp remove wink-inventory
claude mcp add wink-inventory https://api.wink.travel/mcp --transport http
```

Claude DesktopやChatGPTでは設定からサーバーエントリを削除し、アプリを再起動して再度追加してください。

**特定のツール呼び出しで`403 Forbidden`が出る**

ツールが同意画面で承認されなかったスコープを必要としています。サーバーを再接続すると同意画面が再表示され、欠落した権限を承認できます。

**サーバーは表示されるがツール呼び出しで「アカウントが見つかりません」となる**

管理エンティティのないWinkアカウントでサインインしています。使用前に[Winkポータル](https://app.wink.travel)でアカウント設定を確認してください。

**セッション中にトークンが期限切れになる**

トークンは3時間有効です。Claude CodeとClaude Desktopはトークンを静かに更新し、セッションを中断しません。長時間アイドル後に認証エラーが出た場合は新しいメッセージを入力すると自動的に再認証されます。

**Codex CLIで「MCPサーバーに接続できません」と表示される**

`~/.codex/config.json`が有効なJSON（末尾のカンマなし）であることと、`"type": "http"`が設定されていることを確認してください。CodexはリモートサーバーにHTTPトランスポートを要求し、stdioトランスポートはローカルバイナリ用です。

**ChatGPTの「Connectors」メニューが見当たらない**

MCPサポートにはChatGPTデスクトップアプリのバージョン**1.2024.352**以降と、Plus、Pro、Team、Enterpriseの有効なサブスクリプションが必要です。アプリを更新し、設定 → アカウントでプランを確認してください。

**Cursorサーバーが赤表示 / 「接続に失敗しました」**

`~/.cursor/mcp.json`が有効なJSONで`"type": "http"`を含んでいることを確認してください。次に**設定 → Cursor設定 → MCP**で**更新**をクリックします。エラーが続く場合はCursorを完全に再起動してください — 設定変更は再起動なしでは反映されないことがあります。

**WindsurfのMCPサーバーが設定に表示されない**

Windsurfは`mcp_config.json`の変更を反映するために完全な再起動（ウィンドウのリロードではなく）が必要です。**ファイル → 終了**で終了し、再起動してください。サーバーが表示されない場合は、**ヘルプ → Windsurfについて**でバージョンが**1.4以上**であることを確認してください。

**Clineで「サーバーを追加」オプションが見当たらない**

MCPサーバーマネージャーにはClineのバージョン**2.0**以降が必要です。VS Codeの拡張機能パネルでClineを見つけ、更新があれば**更新**をクリックしてください。更新後、VS Codeをリロードします（`Ctrl+Shift+P` → 「Developer: Reload Window」）。

---

<LinkButton href="https://app.wink.travel" variant="primary" icon="external">Winkポータルを開く</LinkButton>
<LinkButton href="https://github.com/wink-travel" variant="secondary" icon="external">GitHubのWink</LinkButton>
