---
title: Angular
description: Používajte Wink webové komponenty v aplikáciách Angular 14+ s CUSTOM_ELEMENTS_SCHEMA a @wink/elements.
sidebar:
  order: 17
---

import { Steps, Aside, LinkCard } from '@astrojs/starlight/components';

Wink komponenty sú samy o sebe Angular Elements — prirodzene sa integrujú do Angular aplikácií. Jediná potrebná konfigurácia je povedať Angular kompilátoru, aby povolil neznáme názvy elementov.

<LinkCard
  title="@wink/elements npm package"
  href="/integrations/npm"
  description="Nainštalujte a načítajte CDN balík s typmi pre TypeScript."
/>

## Inštalácia

```bash
npm install @wink/elements
```

## Bootstrap — načítajte CDN raz

Zavolajte `load()` pred `bootstrapApplication` (alebo v rámci `APP_INITIALIZER`):

```ts
// src/main.ts
import { bootstrapApplication } from '@angular/platform-browser';
import { AppComponent } from './portal/app.component';
import { load } from '@wink/elements';

load({ clientId: import.meta.env['NG_APP_WINK_CLIENT_ID'] });

bootstrapApplication(AppComponent).catch(console.error);
```

## Povoliť vlastné názvy elementov

Pridajte `CUSTOM_ELEMENTS_SCHEMA` ku každému samostatnému komponentu (alebo modulu), ktorý vykresľuje tagy `<wink-*>`:

```ts
// src/portal/hotels/hotels.component.ts
import { Component, CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';

@Component({
  selector: 'app-hotels',
  standalone: true,
  schemas: [CUSTOM_ELEMENTS_SCHEMA],
  template: `
    <wink-content-loader layout="HOTEL" [id]="layoutId" />
    <wink-lookup />
  `,
})
export class HotelsComponent {
  layoutId = 'YOUR_LAYOUT_ID';
}
```

<Aside type="tip" title="NgModule aplikácie">
Pre aplikácie založené na NgModule pridajte `CUSTOM_ELEMENTS_SCHEMA` do poľa `schemas` dekorátora `@NgModule`.
</Aside>

## Väzba atribútov

Angular syntax šablóny pracuje priamo s reťazcovými atribútmi vlastných elementov:

```html
<!-- Statické -->
<wink-content-loader layout="HOTEL" id="abc123" />

<!-- Dynamická väzba -->
<wink-content-loader [attr.layout]="layout" [attr.id]="layoutId" />
```

Použite väzbu `[attr.name]` keď je hodnota dynamická — bežná Angular väzba `[property]` cieli na DOM vlastnosti, nie na HTML atribúty, na ktorých vlastné elementy závisia.

## Premenné prostredia

Pre projekty `@angular/cli` 16+ použite `environment.ts` alebo prefix `NG_APP_*` s `@ngx-env/builder`:

```ts
// src/environments/environment.ts
export const environment = {
  winkClientId: 'YOUR_CLIENT_ID',
};
```

```ts
// src/main.ts
import { environment } from './environments/environment';
load({ clientId: environment.winkClientId });
```

## Typy pre TypeScript

Balík `@wink/elements` exportuje typované rozhrania pre všetky atribúty komponentov:

```ts
import type { WinkContentLoaderAttributes, WinkLayout } from '@wink/elements';

const attrs: WinkContentLoaderAttributes = {
  layout: 'HOTEL' as WinkLayout,
  id: 'abc123',
};
```

## Ďalšie čítanie

- [Referencie Web Components](/sk/integrations/web-components)
- [@wink/elements npm package](/sk/integrations/npm)
