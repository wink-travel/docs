---
title: Catalogue des événements Webhook
description: Tous les types d'événements webhook sur la plateforme Wink, générés à partir du catalogue d'événements de la plateforme — qui sont livrés, à qui, et où la charge utile est documentée.
sidebar:
  order: 3.5
---

import webhooks from '../../../../schemas/webhooks.json';
import WebhookEventTypes from '@/components/webhooks/event-types.astro';

Cette page liste tous les événements webhook auxquels vous pouvez vous abonner sur Wink. Pour les instructions de configuration, consultez le [Guide d'intégration Webhook](/fr/developers/webhook-integration) ou la [page de gestion des Webhooks](/fr/portal/settings/webhooks).

## Comment s'abonner

1. Allez dans **Applications > Webhooks** dans votre portail.
2. Créez un webhook et sélectionnez les événements que vous souhaitez recevoir — ou laissez la liste vide pour recevoir tous les événements.
3. Wink envoie un POST HTTP signé à votre point de terminaison chaque fois que l'événement se produit. Voir [Intégration Webhook](/fr/developers/webhook-integration) pour l'enveloppe, les en-têtes, la vérification de la signature et les tentatives de renvoi.

## Tous les types d'événements

Ce tableau est généré à partir du catalogue d'événements de la plateforme au moment de la compilation, il correspond donc toujours à l'API.
Les types d'événements marqués comme livrés renvoient à leur page de référence dans l'[API Webhooks](/fr/api/webhooks/), qui
documente le corps JSON exact (`WebhookEnvelope` avec un `object` spécifique à l'événement), les en-têtes `Wink-*` et
la politique de nouvelle tentative. Les types d'événements non livrés existent en interne (paiements, grand livre, synchronisation du channel-manager, …) mais ne sont pas actuellement envoyés aux abonnés webhook.

**Destinataires** indique qui reçoit un événement : `SUPPLIER` (l'hôtel), `AFFILIATE` (un revendeur avec un canal de vente actif vers l'hôtel), `AGENT` (l'agence de voyage ayant effectué la réservation) ou `SELF` (l'entité gestionnaire concernée par l'événement). Chaque destinataire reçoit la même forme de charge utile ; les détails spécifiques au rôle se trouvent derrière l'URL `links.self` de la charge utile.

<WebhookEventTypes
  types={webhooks['x-wink-event-types']}
  referenceBase="/api/webhooks"
  eventHeader="Événement"
  descriptionHeader="Description"
  deliveredHeader="Livré"
  recipientsHeader="Destinataires"
  deliveredLabel="livré"
  notDeliveredLabel="non livré"
  noRecipientsLabel="—"
/>

:::tip
Commencez par vous abonner à tous les événements pendant le développement. Une fois que vous comprenez quelles charges utiles votre système nécessite, réduisez vos abonnements pour diminuer le bruit. Envoyez-vous un événement test depuis **Applications > Webhooks** (ou `POST …/webhook/{id}/test`) pour confirmer votre point de terminaison et la vérification de la signature avant la mise en production.
:::

## Pour aller plus loin

- [Guide d'intégration Webhook](/fr/developers/webhook-integration) — Étapes de configuration et bonnes pratiques.
- [Webhooks](/fr/portal/settings/webhooks) — Gérez vos abonnements webhook.
- [Applications](/fr/account/applications) — Gérez vos identifiants API.
- [Développer sur Wink](/fr/developers/build-on-wink) — Vue d'ensemble de la plateforme pour les développeurs.
