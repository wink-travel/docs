---
title: Webhook-integratie
description: Deze gids laat ontwikkelaars zien hoe ze webhooks kunnen instellen om realtime meldingen te ontvangen voor boekingen, betalingen en andere gebeurtenissen.
sidebar:
  order: 3
---

import { Steps, LinkButton } from '@astrojs/starlight/components';

Webhooks laten je systemen realtime meldingen ontvangen wanneer er gebeurtenissen plaatsvinden op je Wink-account — nieuwe boekingen, annuleringen, betalingsupdates en meer. Deze gids begeleidt je bij de setup en best practices.

## Doelgroep

Deze gids is bedoeld voor ontwikkelaars die Wink integreren met externe systemen zoals property management systemen (PMS), channel managers, CRM’s of aangepaste dashboards.

## Hoe webhooks werken

1. Je registreert een webhook-URL op Wink.
2. Wanneer er een gebeurtenis plaatsvindt (bijv. een nieuwe boeking), stuurt Wink een HTTP POST naar jouw URL.
3. Je server verwerkt de payload en reageert met een `200 OK`.

## Een webhook instellen

<Steps>
1. Log in op je account (Extranet, Studio of TripPay — allemaal ondersteunen webhooks).
2. Ga naar `Applications` en vervolgens `Webhooks`. Zie [Webhooks](/nl/portal/settings/webhooks).
3. Klik op `Create webhook`.
4. Voer een **naam** in (bijv. "PMS Booking Sync").
5. Voer je **webhook-URL** in — het HTTPS-eindpunt op je server.
6. **Selecteer gebeurtenissen** — Kies specifieke gebeurtenissen om op te abonneren, of laat leeg om alle gebeurtenissen te ontvangen.
7. Zet **Enabled** aan.
8. Klik op `Save`.
</Steps>

## Soorten gebeurtenissen

Wink ondersteunt **64+ webhook-gebeurtenistypen** in categorieën zoals accountbeheer, boekingscontracten, betalingen, channel manager-updates en beheer van entiteiten. Veelvoorkomende gebeurtenissen zijn:

| Categorie | Voorbeelden |
|---|---|
| **Boeking** | `booking.create`, `booking.cancelled` |
| **Boekingscontract** | `booking.contract.created`, `booking.contract.processing`, `booking.contract.cancelled` |
| **Betaling** | `internal.payment.succeeded`, `internal.payment.failed` |
| **Terugbetalingen** | `booking.contract.refund.partial`, `booking.contract.refund.full` |
| **Channel manager** | `channel-manager.update.rate`, `channel-manager.update.availability` |
| **Account** | `account.create`, `account.status.updated` |

Voor een volledige lijst van elk gebeurtenistype met beschrijvingen, zie de **Webhook Events Catalog**.

<LinkButton href="/nl/developers/webhook-events">
  Bekijk alle 64+ gebeurtenistypen
</LinkButton>

:::tip
Begin tijdens de ontwikkeling met het abonneren op alle gebeurtenissen. Zodra je de payloads begrijpt, beperk je je tot alleen de gebeurtenissen die je systeem nodig heeft.
:::

## Best practices

- **Gebruik HTTPS** — Wink stuurt payloads alleen naar HTTPS-eindpunten.
- **Reageer snel** — Geef zo snel mogelijk een `200 OK` terug zodra je de payload ontvangt. Voer zware verwerking asynchroon uit.
- **Idempotentie** — Je handler moet idempotent zijn. Wink kan de levering opnieuw proberen als het geen `200`-antwoord ontvangt.
- **Valideer de bron** — Verifieer dat binnenkomende verzoeken van Wink afkomstig zijn voordat je ze verwerkt.
- **Logging** — Log elke webhook-payload die je ontvangt. Dit maakt het debuggen van integratieproblemen veel eenvoudiger.

## Pauzeren en verwijderen

Je kunt een webhook **uitschakelen** zonder deze te verwijderen. Dit pauzeert de levering zodat je kunt troubleshooten zonder je configuratie te verliezen. Wanneer je klaar bent, zet je hem weer aan.

Het verwijderen van een webhook verwijdert deze permanent. Elke integratie die op die webhook vertrouwt, ontvangt dan geen meldingen meer.

## Verdere lectuur

- [Webhook Events Catalog](/nl/developers/webhook-events) — Volledige lijst van alle 64+ gebeurtenistypen.
- [Webhooks](/nl/portal/settings/webhooks) — Volledige referentie voor webhookbeheer.
- [Applications](/nl/portal/settings/applications) — Beheer je API-gegevens.
- [Developers > APIs](/nl/api) — Volledige API-documentatie.
