---
title: SolidJS
description: Använd Wink web components i SolidJS-applikationer — inbyggt stöd för custom elements med onMount.
sidebar:
  order: 25
---

import { Aside, LinkCard } from '@astrojs/starlight/components';

SolidJS har inbyggt stöd för custom elements — det skickar attribut direkt till DOM utan ett virtuellt DOM-lager. Ingen extra kompilatorinställning krävs.

<LinkCard
  title="@wink/elements npm package"
  href="/integrations/npm"
  description="Installera och ladda CDN-bunten med TypeScript-typer."
/>

## Installera

```bash
npm install @wink/elements
```

## Ladda en gång vid appstart

```tsx
// src/App.tsx
import { onMount } from 'solid-js';
import { load } from '@wink/elements';

export default function App() {
  onMount(() => {
    load({ clientId: import.meta.env.VITE_WINK_CLIENT_ID });
  });

  return <YourRoutes />;
}
```

`onMount` körs endast i webbläsaren efter den initiala renderingen — det är SolidJS motsvarighet till Reacts `useEffect(() => {}, [])`.

## Använd i JSX

```tsx
// src/pages/Hotels.tsx
import { createSignal } from 'solid-js';

export default function HotelsPage() {
  const [layoutId] = createSignal('YOUR_LAYOUT_ID');

  return (
    <main>
      <wink-content-loader layout="HOTEL" id={layoutId()} />
      <wink-lookup />
    </main>
  );
}
```

## TypeScript — deklarera JSX intrinsics

SolidJS använder sitt eget JSX-namespace. Utöka `solid-js` JSX-typer:

```ts
// src/wink-elements.d.ts
import type {
  WinkContentLoaderAttributes,
  WinkLookupAttributes,
  WinkSearchButtonAttributes,
  WinkAccountButtonAttributes,
  WinkItineraryButtonAttributes,
  WinkShoppingCartButtonAttributes,
  WinkAppLoaderAttributes,
} from '@wink/elements';

declare module 'solid-js' {
  namespace JSX {
    interface IntrinsicElements {
      'wink-content-loader': WinkContentLoaderAttributes & JSX.DOMAttributes<HTMLElement>;
      'wink-lookup': WinkLookupAttributes & JSX.DOMAttributes<HTMLElement>;
      'wink-search-button': WinkSearchButtonAttributes & JSX.DOMAttributes<HTMLElement>;
      'wink-account-button': WinkAccountButtonAttributes & JSX.DOMAttributes<HTMLElement>;
      'wink-itinerary-button': WinkItineraryButtonAttributes & JSX.DOMAttributes<HTMLElement>;
      'wink-shopping-cart-button': WinkShoppingCartButtonAttributes & JSX.DOMAttributes<HTMLElement>;
      'wink-app-loader': WinkAppLoaderAttributes & JSX.DOMAttributes<HTMLElement>;
    }
  }
}
```

## SolidStart (SSR)

För SolidStart-applikationer med server-side rendering, skydda `load()`-anropet med `isServer`:

```tsx
// src/app.tsx
import { onMount } from 'solid-js';
import { isServer } from 'solid-js/web';
import { load } from '@wink/elements';

export default function App() {
  if (!isServer) {
    onMount(() => {
      load({ clientId: import.meta.env.VITE_WINK_CLIENT_ID });
    });
  }

  return <YourRoutes />;
}
```

<Aside type="tip">
SolidJS:s finmaskiga reaktivitet innebär att attributuppdateringar flödar direkt till DOM — ingen batchning eller sammanslagningskostnad. Wink-komponenter får attributändringar omedelbart när dina signalvärden uppdateras.
</Aside>

## Vidare läsning

- [Web Components referens](/sv/integrations/web-components)
- [@wink/elements npm package](/sv/integrations/npm)
