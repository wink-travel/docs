---
title: MCP-servrar
description: Anslut AI-agenter till Wink-plattformen via Model Context Protocol. Autentiserade MCP-servrar ger din agent direktåtkomst till inventarier, bokningar, kanalhanterarintegrationer och mer — säkrat med interaktiv OAuth2-inloggning.
sidebar:
  order: 10
---

import { Steps, LinkButton, Aside, Tabs, TabItem } from '@astrojs/starlight/components';

Wink tillhandahåller två autentiserade MCP-servrar. Båda använder **interaktiv OAuth2-inloggning** (PKCE-auktoriseringskodflöde) — din AI-agent öppnar en webbläsarflik så att du kan logga in med ditt Wink-konto. Inga klienthemligheter att hantera.

| Server | URL | Vad den gör |
|--------|-----|-------------|
| **Inventory API** | `https://api.wink.travel/mcp` | Inventarier, bokningar, priser, försäljningskanaler, analys och alla plattformsoperationer |
| **Integrations** | `https://integrations.wink.travel/mcp` | Kanalhanterarintegrationer — synkronisera fastigheter, priser och tillgänglighet med OTA:er |

---

## Hur autentisering fungerar

Wink MCP-servrar använder **OAuth2 Authorization Code + PKCE**-flödet och stödjer **Dynamic Client Registration (RFC 7591)**. Så här går det till när du ansluter första gången:

1. Din AI-klient upptäcker auktoriseringsservern från MCP-endpointens `/.well-known/oauth-protected-resource` metadata.
2. Klienten registrerar sig själv via **Dynamic Client Registration** — ingen manuell applikationsinställning krävs.
3. Klienten öppnar en **webbläsarflik** till `https://iam.wink.travel`. Du loggar in med ditt Wink-konto och godkänner de begärda behörigheterna på samtyckesskärmen.
4. Wink utfärdar en **åtkomsttoken** som är kopplad till ditt konto och din identitet.
5. AI-klienten bifogar token till varje MCP-förfrågan. Alla verktygsresultat är automatiskt begränsade till din hanterande enhet.
6. Token går ut efter 3 timmar. Din AI-klient uppdaterar tyst i bakgrunden utan att be om inloggning igen.

Varje verktygsanrop körs **under din identitet** — samma ACL-regler som skyddar webbportalen gäller för AI-agenten.

---

## Anslut din AI-klient

<Tabs>
  <TabItem label="Claude Code">

<Steps>
1. Se till att Claude Code är uppdaterad:
   ```bash
   npm install -g @anthropic-ai/claude-code
   ```

2. Lägg till Wink Inventory MCP-servern:
   ```bash
   claude mcp add wink-inventory https://api.wink.travel/mcp --transport http
   ```

3. Claude Code kontaktar servern, upptäcker auktoriseringsservern och öppnar din standardwebbläsare.

4. **Logga in på Wink** och godkänn behörighetsförfrågan på samtyckesskärmen.

5. Claude Code bekräftar anslutningen. Skriv `/mcp` för att verifiera att servern visas och att dess verktyg har laddats.
</Steps>

För att även lägga till Integrations-servern:
```bash
claude mcp add wink-integrations https://integrations.wink.travel/mcp --transport http
```

:::tip[Dela med ditt team]
Lägg till `--scope project` till `claude mcp add` så skriver Claude Code server-URL:en till `.mcp.json` i repo-roten. Committa den filen — varje teammedlem slutför sin egen webbläsarinloggning första gången de ansluter.
:::

  </TabItem>
  <TabItem label="Claude Desktop">

<Steps>
1. Redigera Claude Desktop-konfigurationsfilen:
   - **macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`
   - **Windows:** `%APPDATA%\Claude\claude_desktop_config.json`
   - **Linux:** `~/.config/Claude/claude_desktop_config.json`

2. Lägg till MCP-serverposter:

   ```json title="claude_desktop_config.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

3. **Avsluta helt och starta om** Claude Desktop (använd Quit från menyraden — att bara stänga fönstret räcker inte).

4. Claude Desktop öppnar en webbläsarflik för varje ny server. Logga in på Wink och godkänn behörigheter.

5. Pluggikonen i bottenverktygsfältet visar båda servrarna som anslutna.
</Steps>

  </TabItem>
  <TabItem label="OpenAI Codex">

<Steps>
1. Se till att Codex CLI är installerad:
   ```bash
   npm install -g @openai/codex
   ```

2. Öppna (eller skapa) Codex-konfigurationsfilen:
   - **macOS / Linux:** `~/.codex/config.json`
   - **Windows:** `%USERPROFILE%\.codex\config.json`

3. Lägg till Wink MCP-serverposter:

   ```json title="~/.codex/config.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

4. Starta Codex. Vid första användning öppnar Codex en webbläsarflik till `https://iam.wink.travel`.

5. **Logga in på Wink** och godkänn de begärda behörigheterna.

6. Codex bekräftar anslutningen. Kör `codex` och be den lista tillgängliga verktyg för att verifiera att Wink-servrarna är aktiva.
</Steps>

:::note
Codex CLI:s MCP-stöd kräver version **1.0** eller senare. Kör `codex --version` för att kontrollera och `npm install -g @openai/codex` för att uppgradera.
:::

  </TabItem>
  <TabItem label="ChatGPT">

MCP-serverstöd finns i **ChatGPT desktop app** för Plus, Pro, Team och Enterprise-prenumeranter.

<Steps>
1. Ladda ner och installera [ChatGPT desktop app](https://openai.com/chatgpt/download/) om du inte redan gjort det.

2. Öppna appen och klicka på **din profilikon** uppe till höger, välj sedan **Settings**.

3. Gå till **Connectors** (eller **MCP Servers** beroende på appversion) och klicka på **Add**.

4. Ange Wink Inventory-serverns URL:
   ```
   https://api.wink.travel/mcp
   ```
   Klicka sedan på **Connect**.

5. ChatGPT öppnar en webbläsarflik till `https://iam.wink.travel`. **Logga in på Wink** och godkänn behörigheter.

6. Upprepa steg 4–5 för Integrations-servern:
   ```
   https://integrations.wink.travel/mcp
   ```

7. Båda servrarna visas i din Connectors-lista. Starta en ny chatt — Wink-verktyg är tillgängliga automatiskt.
</Steps>

:::note[Begränsning i webbappen]
MCP-servrar kan endast konfigureras i **desktop-appen**. ChatGPT:s webbgränssnitt på chatgpt.com stödjer inte anpassade MCP-servrar.
:::

  </TabItem>
  <TabItem label="Cursor">

<Steps>
1. Öppna (eller skapa) Cursor MCP-konfigurationsfilen:
   - **macOS / Linux:** `~/.cursor/mcp.json`
   - **Windows:** `%USERPROFILE%\.cursor\mcp.json`

2. Lägg till Wink MCP-serverposter:

   ```json title="~/.cursor/mcp.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

3. Öppna Cursor. Gå till **Settings → Cursor Settings → MCP** och bekräfta att båda servrarna visas med grön statusindikator. Om de visas röda, klicka på **Refresh**.

4. Cursor öppnar en webbläsarflik till `https://iam.wink.travel`. **Logga in på Wink** och godkänn behörigheter.

5. Öppna en chatt (`Ctrl+L` / `Cmd+L`) och be Cursor lista tillgängliga Wink-verktyg för att bekräfta anslutningen.
</Steps>

:::tip[Projektbegränsad konfiguration]
Du kan också placera en `mcp.json`-fil i projektets rot istället för i `~/.cursor/`. Projektkonfiguration prioriteras och gör servrarna tillgängliga för varje teammedlem som öppnar projektet i Cursor.
:::

  </TabItem>
  <TabItem label="Windsurf">

<Steps>
1. Öppna (eller skapa) Windsurf MCP-konfigurationsfilen:
   - **macOS / Linux:** `~/.codeium/windsurf/mcp_config.json`
   - **Windows:** `%USERPROFILE%\.codeium\windsurf\mcp_config.json`

2. Lägg till Wink MCP-serverposter:

   ```json title="~/.codeium/windsurf/mcp_config.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

3. **Starta om Windsurf helt** (Arkiv → Avsluta, sedan öppna igen).

4. Gå till **Settings → MCP Servers** och verifiera att båda servrar visas som anslutna. Windsurf öppnar en webbläsarflik till `https://iam.wink.travel` vid första användning.

5. **Logga in på Wink** och godkänn behörigheter. Servrarna blir gröna i inställningspanelen när autentiseringen är klar.
</Steps>

:::note
Windsurf MCP-stöd kräver version **1.4** eller senare. Kontrollera via **Hjälp → Om Windsurf** och uppdatera från Windsurf-webbplatsen vid behov.
:::

  </TabItem>
  <TabItem label="Cline (VS Code)">

Cline är en VS Code-tillägg med inbyggd MCP-serverhanterare. Ingen manuell JSON-redigering krävs.

<Steps>
1. Installera Cline från VS Code Marketplace:
   - Öppna VS Code och tryck `Ctrl+Shift+X` / `Cmd+Shift+X`
   - Sök efter **Cline** och klicka på **Install**

2. Klicka på **Cline-ikonen** i aktivitetsfältet (vänster sidopanel) för att öppna Cline-panelen.

3. Klicka på **MCP Servers-ikonen** (pluggikonen) i Cline-verktygsfältet, välj sedan **Edit MCP Settings**.

4. VS Code öppnar `cline_mcp_settings.json`. Lägg till Wink-posterna:

   ```json title="cline_mcp_settings.json"
   {
     "mcpServers": {
       "wink-inventory": {
         "type": "http",
         "url": "https://api.wink.travel/mcp"
       },
       "wink-integrations": {
         "type": "http",
         "url": "https://integrations.wink.travel/mcp"
       }
     }
   }
   ```

5. **Spara filen.** Cline laddar om automatiskt och öppnar en webbläsarflik till `https://iam.wink.travel`.

6. **Logga in på Wink** och godkänn behörigheter. Båda servrar visas som aktiva i MCP Servers-panelen.
</Steps>

:::tip[Alternativ: lägg till via URL]
I MCP Servers-panelen, klicka på **Add Server** och klistra in `https://api.wink.travel/mcp` direkt. Cline fyller automatiskt i konfigurationen och hanterar autentisering utan manuell JSON-redigering.
:::

  </TabItem>
</Tabs>

---

## Scope-referens

När du godkänner samtyckesskärmen ser du en uppdelning av vad agenten begär. Wink grupperar behörigheter efter funktionellt område:

| Scope-grupp | Scopes | Vad de låser upp |
|-------------|--------|-----------------|
| **MCP transport** | `mcp.read` `mcp.write` `mcp.remove` | Krävs för att ansluta till någon av MCP-servrarna |
| **Account** | `account.read` `account.write` `account.remove` | Hantering av enheter, teammedlemmar, inställningar, applikationer |
| **Booking** | `booking.read` `booking.write` `booking.remove` | Söka, visa, skapa, avboka och återbetala bokningar |
| **Inventory** | `inventory.read` `inventory.write` `inventory.remove` | Fastigheter, rum, priser, bekvämligheter, tillgänglighet |
| **Analytics** | `analytics.read` | Bokningsanalys, intäktsrapporter, topplistor |
| **Marketing** | `marketing.read` `marketing.write` `marketing.remove` | Sociala inlägg, kampanjer, försäljningskanaler, erbjudanden |
| **Content** | `content.read` `content.write` `content.remove` | Multimedia, förkortade URL:er, syndikerbara objekt, kartor |
| **Payment** | `payment.read` `payment.write` | Bokföring, utbetalningar, ekonomiska sammanställningar, uttag |

AI-klienten begär endast de scopes den behöver. Du kan neka enskilda behörigheter på samtyckesskärmen — verktyg som kräver en nekad scope returnerar ett fel vid anrop.

---

## Felsökning

**Webbläsarflik öppnas inte**

Din AI-klient kanske inte stöder det interaktiva PKCE-flödet än. Kontrollera att du kör en aktuell version:
- **Claude Code:** `npm install -g @anthropic-ai/claude-code`
- **Codex CLI:** `npm install -g @openai/codex`
- **Claude Desktop / ChatGPT desktop:** uppdatera via appens inbyggda uppdaterare

**`401 Unauthorized` efter inloggning**

Din cachade token kan vara föråldrad. Ta bort och lägg till servern igen för att trigga en ny inloggning:

```bash
# Claude Code
claude mcp remove wink-inventory
claude mcp add wink-inventory https://api.wink.travel/mcp --transport http
```

För Claude Desktop och ChatGPT, ta bort serverposten i inställningarna, starta om appen och lägg till den igen.

**`403 Forbidden` vid anrop av specifikt verktyg**

Verktyget kräver en scope du inte godkände på samtyckesskärmen. Anslut servern igen — samtyckesskärmen visas på nytt och du kan godkänna den saknade behörigheten.

**Servern visas men verktygsanrop returnerar "account not found"**

Du är inloggad på ett Wink-konto utan hanterande enhet. Gå till [Wink Portal](https://app.wink.travel) och säkerställ att ditt konto är korrekt inställt innan du använder MCP-servern.

**Token går ut mitt i sessionen**

Token är giltiga i 3 timmar. Claude Code och Claude Desktop uppdaterar token tyst utan att avbryta din session. Om du får autentiseringsfel efter lång inaktivitet, skriv ett nytt meddelande — klienten autentiserar automatiskt om.

**Codex CLI visar "MCP server unreachable"**

Kontrollera att din `~/.codex/config.json` är giltig JSON (inga avslutande kommatecken) och att `"type": "http"` är satt. Codex kräver HTTP-transport för fjärrservrar; stdio-transport är endast för lokala binärer.

**ChatGPT saknar "Connectors"-menyn**

MCP-stöd kräver ChatGPT desktop app version **1.2024.352** eller senare och en aktiv Plus-, Pro-, Team- eller Enterprise-prenumeration. Uppdatera appen och kontrollera din plan under Settings → Account.

**Cursor-server visar rött / "Failed to connect"**

Verifiera att din `~/.cursor/mcp.json` är giltig JSON och innehåller `"type": "http"`. Gå sedan till **Settings → Cursor Settings → MCP** och klicka på **Refresh**. Om felet kvarstår, starta om Cursor helt — konfigurationsändringar plockas inte alltid upp utan omstart.

**Windsurf MCP-servrar visas inte i inställningar**

Windsurf kräver full omstart (inte bara fönsterladdning) för att plocka upp ändringar i `mcp_config.json`. Använd **Arkiv → Avsluta** och starta om. Om servrar fortfarande inte visas, kontrollera att du har Windsurf **1.4+** via **Hjälp → Om Windsurf**.

**Cline saknar "Add Server"-alternativ**

MCP-serverhanteraren kräver Cline **2.0** eller senare. Öppna VS Code Extensions-panelen, hitta Cline och klicka på **Update** om en uppdatering finns. Efter uppdatering, ladda om VS Code (`Ctrl+Shift+P` → "Developer: Reload Window").

---

<LinkButton href="https://app.wink.travel" variant="primary" icon="external">Öppna Wink Portal</LinkButton>
<LinkButton href="https://github.com/wink-travel" variant="secondary" icon="external">Wink på GitHub</LinkButton>
