---
title: Svelte / SvelteKit
description: Χρησιμοποιήστε τα web components του Wink σε εφαρμογές Svelte και SvelteKit με onMount και ελέγχους browser.
sidebar:
  order: 20
---

import { Aside, LinkCard } from '@astrojs/starlight/components';

Το Svelte περνάει τα attributes απευθείας στο DOM χωρίς επίπεδο virtual DOM, καθιστώντας τα custom elements φυσική επιλογή. Για το SvelteKit (SSR), φορτώστε το CDN μέσα στο `onMount` ή προστατέψτε με τον έλεγχο `browser`.

<LinkCard
  title="@wink/elements npm package"
  href="/integrations/npm"
  description="Εγκαταστήστε και φορτώστε το CDN bundle με τύπους TypeScript."
/>

## Εγκατάσταση

```bash
npm install @wink/elements
```

## SvelteKit — root layout

```ts
// src/routes/+layout.ts
import { browser } from '$app/environment';
import { load as loadWink } from '@wink/elements';

export function load() {
  if (browser) {
    loadWink({ clientId: import.meta.env.PUBLIC_WINK_CLIENT_ID });
  }
}
```

Εναλλακτικά, φορτώστε μέσα στο `onMount` στο `+layout.svelte`:

```svelte
<!-- src/routes/+layout.svelte -->
<script>
  import { onMount } from 'svelte';
  import { load } from '@wink/elements';

  onMount(() => {
    load({ clientId: import.meta.env.PUBLIC_WINK_CLIENT_ID });
  });
</script>

<slot />
```

## Χρήση σε components

Το Svelte περνάει όλα τα άγνωστα attributes απευθείας στο DOM, οπότε δεν απαιτείται επιπλέον ρύθμιση:

```svelte
<script>
  export let layoutId = 'YOUR_LAYOUT_ID';
</script>

<main>
  <wink-content-loader layout="HOTEL" id={layoutId} />
  <wink-lookup />
</main>
```

## Svelte 5 (runes syntax)

```svelte
<script lang="ts">
  import { onMount } from 'svelte';
  import { load } from '@wink/elements';

  let { layoutId }: { layoutId: string } = $props();

  onMount(() => {
    load({ clientId: import.meta.env.PUBLIC_WINK_CLIENT_ID });
  });
</script>

<wink-content-loader layout="HOTEL" id={layoutId} />
```

## Μεταβλητές περιβάλλοντος

Το SvelteKit εκθέτει μόνο τις μεταβλητές που ξεκινούν με `PUBLIC_` στον browser:

```
# .env
PUBLIC_WINK_CLIENT_ID=your-client-id
```

<Aside type="caution" title="SSR rendering">
Μην αποδώσετε ποτέ στοιχεία `<wink-*>` κατά το server-side rendering — τα custom elements καταχωρούνται στο registry `customElements` του browser που δεν υπάρχει στον server. Περιβάλλετε με `{#if browser}` ή καλέστε `load()` μόνο μέσα στο `onMount`.
</Aside>

```svelte
<script>
  import { browser } from '$app/environment';
</script>

{#if browser}
  <wink-content-loader layout="HOTEL" id="YOUR_LAYOUT_ID" />
{/if}
```

## Καθαρό Svelte (Vite, χωρίς SvelteKit)

```svelte
<!-- src/App.svelte -->
<script>
  import { onMount } from 'svelte';
  import { load } from '@wink/elements';

  onMount(() => {
    load({ clientId: import.meta.env.VITE_WINK_CLIENT_ID });
  });
</script>

<wink-content-loader layout="HOTEL" id="YOUR_LAYOUT_ID" />
```

## Περισσότερη ανάγνωση

- [Αναφορά Web Components](/el/integrations/web-components)
- [@wink/elements npm package](/el/integrations/npm)
