---
title: Webhooks
description: Εγγραφείτε σε γεγονότα Wink και TripPay.
sidebar:
  order: 11
---

import { Steps } from '@astrojs/starlight/components';
import CloudinaryImage from '/src/components/cloudinary-image.astro';

Τα Webhooks είναι ένας τρόπος για να λαμβάνετε και να αντιδράτε σε πραγματικό χρόνο σε γεγονότα από το Wink για τον λογαριασμό σας.

<Steps>
 1. Επιλέξτε τον λογαριασμό με τον οποίο θέλετε να εργαστείτε από τα [Accounts](https://my.wink.travel).
 2. Κάντε κλικ στο `Actions` στο κάτω μέρος της κάρτας του λογαριασμού και επιλέξτε `Manage account`.
 3. Κάντε κλικ στην καρτέλα `Webhooks` στην επόμενη σελίδα.
 4. Αυτή η ενότητα θα εμφανίσει όλα τα webhooks σας.
 5. Επιλέξτε να επεξεργαστείτε ένα υπάρχον ή να δημιουργήσετε ένα νέο.
 </Steps>

## Παράδειγμα

<Steps>
1. Γίνεται μια κράτηση στο Wink.
1. Εκδίδεται το γεγονός Wink `booking.created`.
2. Το σύστημά σας ακούει ενεργά αυτό το γεγονός.
3. Το σύστημά σας παρακολουθεί το δικό του σύνολο αναλύσεων και χρησιμοποιεί αυτό το γεγονός για να ενημερώσει τη βάση δεδομένων χρονικών σειρών του.
</Steps>

<figure>
<CloudinaryImage
      width="350"
      height="215"
      resize="scale"
      quality="best"
      alt="Δείγμα webhook"
      src="academy/webhook"
      errorImage="noimage_opaque_nyrtl0.png"
    />
    <figcaption>*Δείγμα καταχώρησης webhook*</figcaption>
</figure>

## Δημιουργία webhook

:::caution
Πρέπει να δημιουργήσετε έναν συνεργατικό λογαριασμό στο Wink ή έναν λογαριασμό στο TripPay πριν δημιουργήσετε ένα webhook, καθώς θα συνδέσετε το webhook σας με συγκεκριμένο λογαριασμό.
:::

Ακολουθούν τα βήματα για τη δημιουργία ενός webhook:

<Steps>
1. Από τη σελίδα webhooks, κάντε κλικ στο κουμπί `Create new webhook`.
2. ***Όνομα*** Δώστε ένα όνομα στο webhook σας. *π.χ. Wink Stalker*
3. ***Ενεργοποίηση*** Αφήστε ως έχει για να είναι ενεργοποιημένο.
4. **Όνομα οντότητας** Συνδέστε το webhook σας με έναν από τους υπάρχοντες λογαριασμούς σας. *π.χ. Cool Account*
5. **Γεγονότα** Ακούστε τα γεγονότα που σας ενδιαφέρουν. *π.χ. `booking.created`*
6. **URL Webhook** Εισάγετε το URL όπου θα ακούτε τα γεγονότα από εμάς. *π.χ. `https://my.system.com/webhook/wink`*
7. Κάντε κλικ στο κουμπί `Save` για να συνεχίσετε.
</Steps>

Ανακατευθύνεστε πίσω στη λίστα των webhooks σας και το νέο webhook είναι έτοιμο προς χρήση.

## Ενημέρωση webhook

Θα χρειαστεί να ενημερώσετε την εφαρμογή σας αν θέλετε να:

- Απενεργοποιήσετε το webhook σας.
- Ακούσετε περισσότερα ή λιγότερα γεγονότα.
- Αλλάξετε το URL ακρόασης του webhook.

Για να ενημερώσετε ένα υπάρχον webhook, κάντε τα εξής:

<Steps>
1. Κάντε κλικ στο σύνδεσμο `Actions` για το webhook που θέλετε να ενημερώσετε.
2. Κάντε κλικ στο κουμπί `Update` κάτω από το Actions.
3. Κάντε τις αλλαγές σας στο webhook.
4. Κάντε κλικ στο κουμπί `Save` για να συνεχίσετε.
</Steps>

Ανακατευθύνεστε πίσω στη λίστα των webhooks σας.

## Αφαίρεση webhook

Αν δεν χρειάζεστε πλέον το webhook, μπορείτε να το αφαιρέσετε.

Για να αφαιρέσετε ένα webhook, κάντε τα εξής:

<Steps>
1. Κάντε κλικ στο σύνδεσμο `Actions` για το webhook που θέλετε να αφαιρέσετε.
2. Κάντε κλικ στο κουμπί `Remove` κάτω από το Actions.
3. Κάντε κλικ στο κουμπί `OK` για να επιβεβαιώσετε τη διαγραφή.
</Steps>

Ανακατευθύνεστε πίσω στη λίστα των webhooks σας.
