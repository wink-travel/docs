---
title: MCP poslužitelji
description: Povežite AI agente s Wink platformom putem Model Context Protocola. Wink objavljuje svoje alate kao fokusirane, zadatkovno oblikovane krajnje točke tako da vaš agent učitava samo ono što vaš posao zahtijeva — osigurano interaktivnim OAuth2 prijavom.
sidebar:
  order: 10
---

import { Steps, LinkButton, Aside, Tabs, TabItem } from '@astrojs/starlight/components';

Winkovi MCP alati objavljeni su kao **skupovi alata** — fokusirane krajnje točke, svaka pokriva jedan zadatak. Povezujete svoj AI klijent s jednom ili dvije koje odgovaraju vašem poslu, a ne sa svim.

Svaka krajnja točka koristi **interaktivnu OAuth2 prijavu** (PKCE autorizacijski kodni tok): vaš AI agent otvara karticu preglednika, prijavljujete se sa svojim Wink računom i to je to. Nema potrebe za upravljanjem klijentskim tajnama.

```bash title="Integrator channel managera povezuje se na jednu krajnju točku"
claude mcp add wink-channel-manager https://integrations.wink.travel/mcp/channel-manager --transport http
```

```bash title="Hotelijer koji upravlja vlastitim cijenama povezuje se na drugu"
claude mcp add wink-rates https://api.wink.travel/mcp/extranet/rates --transport http
```

---

## Zašto su alati podijeljeni po krajnjim točkama

Wink izlaže oko 500 alata. Davanje svih njih agentu odjednom tehnički je moguće — ali loša ideja.

**Definicije alata troše kontekst.** Svaki alat koji MCP poslužitelj oglašava učitava se u kontekstni prozor modela prije nego što obavi bilo kakav posao, zajedno s njegovim opisom i potpunom shemom parametara. Petsto takvih alata troši velik dio budžeta koji vaš stvarni zadatak treba, pri svakom zahtjevu.

**Kvaliteta izbora opada kako lista raste.** Agent koji bira između 500 sličnih alata donosi lošije odluke nego onaj koji bira između 40. Ako integrirate channel manager, otprilike dvadeset alata je relevantno za vas; ostalih 480 je buka koja otežava pronalazak pravog alata.

**Posao je stvarno razdvojiv.** Netko tko sinkronizira dostupnost iz PMS-a i netko tko uređuje affiliate odredišne stranice gotovo da ne dijele alate. Podjela po zadatku znači da svaka veza nosi vokabular jednog zadatka.

<Aside type="caution" title="Skupovi alata nisu dozvole">
Odabir uskog skupa alata **ne** ograničava što vaš račun može raditi, a odabir širokog ne daje ništa dodatno. Svaki alat provodi iste dozvole računa kao i web portal, pod vašim prijavljenim identitetom. Ako vaš račun ne može otkazati rezervaciju u Wink Portalu, agent također ne može — bez obzira na to na koju je krajnju točku povezan.

Birajte skupove alata za **fokus**, a ne za sigurnost.
</Aside>

---

## Direktorij krajnjih točaka

Dva domaćina služe skupove alata. Broj alata je približan i raste s vremenom.

### `https://api.wink.travel` — platforma

| Krajnja točka | Alati | Što pokriva |
|---|---:|---|
| `/mcp/extranet/facilities` | ~64 | Rezervabilni i na licu mjesta inventar: sobe za goste i tipovi soba, spa centri, restorani, sobe za sastanke, aktivnosti, atrakcije, mjesta, ostale prodajne stavke |
| `/mcp/extranet/property` | ~62 | Zapis o objektu i njegovi komercijalni uvjeti: profil i sadržaj, dodaci, politike otkazivanja, pogodnosti, obavijesti |
| `/mcp/settings` | ~60 | Administracija računa: profil i brendiranje upravljačkog subjekta, upravitelji i njihova prava, prilagodbe, webhooks, postavke obavijesti |
| `/mcp/studio/embeds` | ~48 | Objavljivanje kuriranog inventara: widgeti, mreže i rangirane mreže, kartice, karte, duboke poveznice na inventar i stranice dobavljača |
| `/mcp/extranet/rates` | ~44 | Cijene i dostupnost: razdoblja i kvote inventara, planovi cijena, glavne cijene, posebne i promotivne cijene |
| `/mcp/extranet/operations` | ~39 | Upravljanje aktivnim objektom: rezervacije i njihov životni ciklus, recenzije gostiju i odgovori, kalendarski feedovi, zakazani zadaci |
| `/mcp/extranet/distribution` | ~37 | Gdje se inventar prodaje: prodajni kanali i modifikatori cijena, zahtjevi za affiliate i dobavljačke odnose, registracija, prikupljanje leadova |
| `/mcp/studio/curation` | ~35 | Izgradnja kolekcija za prodaju: statične i dinamičke liste, rangirane liste, spremljene pretrage, agregati objekata |
| `/mcp/link-manager` | ~28 | Sindicirane kolekcije poveznica, njihov dizajn, kategorije i oznake, plus skraćene URL-ove |
| `/mcp/reference` | ~24 | Podaci za čitanje: zemlje, valute i tečajevi, jezici, vremenske zone, geografske pretrage, IP geolokacija |
| `/mcp/analytics` | ~18 | Analitika rezervacija i prihoda: pregledi, linijski grafikoni, ljestvice, performanse affiliatea, metričke dijeljenja |
| `/mcp/payment` | ~18 | Financijske operacije: knjiga računa, isplate i procjene naknada, agregati sredstava za rezervacije |
| `/mcp/booking-engine` | ~17 | Put kupovine za putnika: pretraga i pronalazak, košarica i naplata, liste želja, prilagodba kampanja |
| `/mcp/social` | ~17 | Povezani društveni računi, objavljivanje i zakazivanje objava, čitanje uvida u objave |
| `/mcp/travel-agent` | ~8 | Rezervacije koje agent obavlja u ime putnika i izvještaji agenata |

### `https://integrations.wink.travel` — integracije channel managera

Jedna krajnja točka, jer ovaj domaćin postoji za jedan zadatak.

| Krajnja točka | Alati | Što pokriva |
|---|---:|---|
| `/mcp/channel-manager` | ~9 | Povezivanje i verifikacija integracije channel managera, PMS-a ili CRS-a |

<Aside type="note" title="Integracija channel managera vs. upravljanje vlastitim cijenama">
To su različiti poslovi za različite ljude i ne dijele istu krajnju točku.

**Prodavač channel managera, PMS-a ili CRS-a** integrira se jednom putem `integrations.wink.travel/mcp/channel-manager`, i od tada razmjenjuje podatke o objektima, cijenama, dostupnosti i rezervacijama preko te integracije. Ta jedna krajnja točka je cijela površina.

**Hotelijer koji ručno mijenja vlastite cijene** koristi extranet skupove alata na `api.wink.travel` — `/mcp/extranet/rates` i slične. Oni nisu dio integracije channel managera i integrator nema razloga povezivati se na njih.

Administracija računa također je na `api.wink.travel`, na `/mcp/settings`. Na domaćinu za integracije nema `/mcp/settings`.
</Aside>

Svaka krajnja točka također nosi alat `ping`, tako da možete potvrditi da je veza aktivna prije nego što zatražite stvarni posao.

### Agentic plaćanja

Za kompletan tijek rezervacije putnika, povežite **Wink Booking Engine** na `https://api.wink.travel/mcp/booking-engine` i kompatibilni **novčanik za plaćanje**. Dodavanje Wink MCP otvara prijavu u pregledniku i odabir dozvola; vaš klijent zatim upravlja autentikacijom.

Booking Engine uključuje pronalazak destinacije, pretragu hotela i soba, `agentic_booking_quote`, `agentic_booking_pay`, `agentic_booking_book`, dohvat rezervacija i račune, kada su agentic rezervacija i plaćanje omogućeni za to okruženje. Pogledajte [Agentic Payments](/hr/developers/agentic-payments/) za postavljanje veze, dozvole koje treba odabrati i korake od pretrage do potvrde.

---

## Koje skupove alata trebam?

| Ako ste… | Povežite se na |
|---|---|
| **Integrirate channel manager, PMS ili CRS** | `integrations.wink.travel/mcp/channel-manager` — samo ovu |
| **Upravljate sadržajem objekta** | `/mcp/extranet/property` + `/mcp/extranet/facilities` |
| **Upravljate sobama i tipovima soba** | `/mcp/extranet/facilities` |
| **Upravljate cijenama i dostupnošću** | `/mcp/extranet/rates` |
| **Vodite dnevne rezervacije** | `/mcp/extranet/operations` |
| **Širite distribuciju** | `/mcp/extranet/distribution` + `/mcp/analytics` |
| **Gradite affiliate stranice** | `/mcp/studio/curation` + `/mcp/studio/embeds` |
| **Vodite link-in-bio stranice** | `/mcp/link-manager` + `/mcp/social` |
| **Rezervirate u ime putnika** | `/mcp/travel-agent` |
| **Usaglašavate novac** | `/mcp/payment` |
| **Rezervirate sobu s AI agentom** | `/mcp/booking-engine` + kompatibilni novčanik za plaćanje — pogledajte [Agentic Payments](/hr/developers/agentic-payments/) |
| **Gradite iskustvo rezervacije** | `/mcp/booking-engine`; opcionalno `/mcp/reference` za dodatne pretrage |

**Booking Engine uključuje osnovni tijek rezervacije.** Omogućuje otkrivanje računa i prilagodbi, pronalazak destinacije, dostupnost hotela i detalje o cijenama soba, košarice, naplatu i upravljanje rezervacijama putnika. Agentic alati za rezervaciju i plaćanje također su uključeni kada su omogućeni za to okruženje.

Za pretragu hotela u gradu, koristite `destination_lookup_search_suggestions` za razrješenje destinacije, zatim `inventory_search_city` za pronalazak dostupnih hotela i cijena. Oba alata su u `/mcp/booking-engine`, pa ovaj tijek ne zahtijeva vezu s Reference.

Dodajte `/mcp/reference` kada trebate pomoćne pretrage poput zemalja, valuta i tečajeva, podržanih jezika, vremenskih zona, šire geografske pretrage, OTA klasifikacija ili javnog znanja o objektima. To su opcionalni dodaci tijeku rezervacije.

Počnite s jednim. Dodajte drugi kad vam zatreba nešto što prvi ne može — svaka veza je neovisna i dodavanje jedne ne utječe na druge.

<Aside type="tip" title="Još uvijek želite sve?">
`https://api.wink.travel/mcp` i `https://integrations.wink.travel/mcp` ostaju dostupni i služe **svaki** alat na svom domaćinu. Korisni su za istraživanje što postoji, ali ih ne preporučujemo za svakodnevni rad: lista alata je dovoljno velika da zasjeni vaš zadatak i pogorša izbor alata agenta. Preferirajte gore navedene skupove alata.
</Aside>

---

## Kako funkcionira autentikacija

Wink MCP poslužitelji koriste **OAuth2 Authorization Code + PKCE** tok i podržavaju **Dynamic Client Registration (RFC 7591)**. Evo što se događa kad se prvi put povežete:

1. Vaš AI klijent otkriva autorizacijski poslužitelj iz MCP krajnje točke `/.well-known/oauth-protected-resource` metapodataka.
2. Klijent se samoregistrira putem **Dynamic Client Registration** — nije potrebna ručna konfiguracija aplikacije.
3. Klijent otvara **karticu preglednika** na `https://iam.wink.travel`. Prijavljujete se sa svojim Wink računom i odobravate tražene dozvole na zaslonu za suglasnost.
4. Wink izdaje **access token** vezan za vaš račun i identitet.
5. AI klijent prilaže token svakom MCP zahtjevu. Svi rezultati alata automatski su ograničeni na vaš upravljački subjekt.
6. Tokeni istječu nakon 3 sata. Vaš AI klijent tiho osvježava token u pozadini bez ponovnog traženja prijave.

Svaki poziv alata radi **pod vašim identitetom** — ista pravila ACL-a koja štite web portal vrijede i za AI agenta.

---

## Povežite svoj AI klijent

<Tabs>
  <TabItem label="Claude Code">

<Steps>
1. Provjerite je li Claude Code ažuriran:
   ```bash
   npm install -g @anthropic-ai/claude-code
   ```

2. Odaberite skup alata iz [direktorija gore](#endpoint-directory) i dodajte ga. Na primjer, cijene i dostupnost:
   ```bash
   claude mcp add wink-rates https://api.wink.travel/mcp/extranet/rates --transport http
   ```

3. Claude Code kontaktira poslužitelj, otkriva autorizacijski poslužitelj i otvara vaš zadani preglednik.

4. **Prijavite se u Wink** i odobrite zahtjev za dozvolu na zaslonu za suglasnost.

5. Claude Code potvrđuje vezu. Upisom `/mcp` provjerite pojavljuje li se poslužitelj i jesu li njegovi alati učitani.
</Steps>

Dodajte onoliko skupova alata koliko vaš posao zahtijeva — svaki je zaseban unos poslužitelja s vlastitim imenom:
```bash
claude mcp add wink-operations https://api.wink.travel/mcp/extranet/operations --transport http
claude mcp add wink-analytics  https://api.wink.travel/mcp/analytics           --transport http
```

Prijavljujete se samo jednom. Nakon prve odobrene prijave u pregledniku, Claude Code ponovno koristi vašu Wink sesiju za dodatne Wink krajnje točke.

:::tip[Podijelite s timom]
Dodajte `--scope project` uz `claude mcp add` i Claude Code zapisuje URL poslužitelja u `.mcp.json` u korijenu repozitorija. Komitirajte tu datoteku — svaki član tima dobiva iste skupove alata, a svaki prvi put kad se poveže dovršava vlastitu prijavu u pregledniku.
:::

  </TabItem>
  <TabItem label="Claude Desktop">

<Steps>
1. Uredite konfiguracijsku datoteku Claude Desktopa:
   - **macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`
   - **Windows:** `%APPDATA%\Claude\claude_desktop_config.json`
   - **Linux:** `~/.config/Claude/claude_desktop_config.json`

2. Dodajte unose MCP poslužitelja:

   ```json title="claude_desktop_config.json"
   {
     "mcpServers": {
       "wink-extranet-rates": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/rates"
       },
       "wink-extranet-operations": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/operations"
       }
     }
   }
   ```

3. **Potpuno izađite i ponovno pokrenite** Claude Desktop (koristite Quit iz izbornika — zatvaranje prozora nije dovoljno).

4. Claude Desktop otvara karticu preglednika za svaki novi poslužitelj. Prijavite se u Wink i odobrite dozvole.

5. Ikona utikača u donjoj alatnoj traci pokazuje oba poslužitelja kao povezana.
</Steps>

  </TabItem>
  <TabItem label="OpenAI Codex">

<Steps>
1. Provjerite je li Codex CLI instaliran:
   ```bash
   npm install -g @openai/codex
   ```

2. Otvorite (ili kreirajte) konfiguracijsku datoteku Codexa:
   - **macOS / Linux:** `~/.codex/config.json`
   - **Windows:** `%USERPROFILE%\.codex\config.json`

3. Dodajte Wink MCP unose:

   ```json title="~/.codex/config.json"
   {
     "mcpServers": {
       "wink-extranet-rates": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/rates"
       },
       "wink-extranet-operations": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/operations"
       }
     }
   }
   ```

4. Pokrenite Codex. Prvi put Codex otvara karticu preglednika na `https://iam.wink.travel`.

5. **Prijavite se u Wink** i odobrite tražene dozvole.

6. Codex potvrđuje vezu. Pokrenite `codex` i zatražite popis dostupnih alata da provjerite jesu li Wink poslužitelji aktivni.
</Steps>

:::note
Podrška MCP-a u Codex CLI zahtijeva verziju **1.0** ili noviju. Pokrenite `codex --version` za provjeru i `npm install -g @openai/codex` za nadogradnju.
:::

  </TabItem>
  <TabItem label="ChatGPT">

Podrška za MCP poslužitelje dostupna je u **ChatGPT desktop aplikaciji** za Plus, Pro, Team i Enterprise pretplatnike.

<Steps>
1. Preuzmite i instalirajte [ChatGPT desktop aplikaciju](https://openai.com/chatgpt/download/) ako je već nemate.

2. Otvorite aplikaciju i kliknite **ikonu svog profila** u gornjem desnom kutu, zatim odaberite **Settings**.

3. Idite na **Connectors** (ili **MCP Servers** ovisno o verziji aplikacije) i kliknite **Add**.

4. Unesite URL skupa alata iz [direktorija gore](#endpoint-directory), na primjer:
   ```
   https://api.wink.travel/mcp/extranet/rates
   ```
   Zatim kliknite **Connect**.

5. ChatGPT otvara karticu preglednika na `https://iam.wink.travel`. **Prijavite se u Wink** i odobrite dozvole.

6. Ponovite korake 4–5 za bilo koji drugi skup alata koji vam treba, na primjer:
   ```
   https://api.wink.travel/mcp/extranet/operations
   ```

7. Svaki skup alata pojavljuje se na vašem popisu Connectors. Započnite novi chat — Wink alati su automatski dostupni.
</Steps>

:::note[Ograničenje web aplikacije]
MCP poslužitelji se mogu konfigurirati samo u **desktop aplikaciji**. Web sučelje ChatGPT-a na chatgpt.com ne podržava prilagođene MCP poslužitelje.
:::

  </TabItem>
  <TabItem label="Cursor">

<Steps>
1. Otvorite (ili kreirajte) Cursor MCP konfiguracijsku datoteku:
   - **macOS / Linux:** `~/.cursor/mcp.json`
   - **Windows:** `%USERPROFILE%\.cursor\mcp.json`

2. Dodajte Wink MCP unose:

   ```json title="~/.cursor/mcp.json"
   {
     "mcpServers": {
       "wink-extranet-rates": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/rates"
       },
       "wink-extranet-operations": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/operations"
       }
     }
   }
   ```

3. Otvorite Cursor. Idite na **Settings → Cursor Settings → MCP** i provjerite pojavljuju li se oba poslužitelja sa zelenim indikatorom statusa. Ako su crveni, kliknite **Refresh**.

4. Cursor otvara karticu preglednika na `https://iam.wink.travel`. **Prijavite se u Wink** i odobrite dozvole.

5. Otvorite chat (`Ctrl+L` / `Cmd+L`) i zatražite od Cursora da navede dostupne Wink alate za potvrdu veze.
</Steps>

:::tip[Konfiguracija ograničena na projekt]
Također možete staviti `mcp.json` datoteku u korijen svog projekta umjesto u `~/.cursor/`. Konfiguracija na razini projekta ima prednost i čini poslužitelje dostupnima svakom članu tima koji otvori projekt u Cursoru.
:::

  </TabItem>
  <TabItem label="Windsurf">

<Steps>
1. Otvorite (ili kreirajte) Windsurf MCP konfiguracijsku datoteku:
   - **macOS / Linux:** `~/.codeium/windsurf/mcp_config.json`
   - **Windows:** `%USERPROFILE%\.codeium\windsurf\mcp_config.json`

2. Dodajte Wink MCP unose:

   ```json title="~/.codeium/windsurf/mcp_config.json"
   {
     "mcpServers": {
       "wink-extranet-rates": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/rates"
       },
       "wink-extranet-operations": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/operations"
       }
     }
   }
   ```

3. **Potpuno ponovno pokrenite Windsurf** (File → Quit, zatim ponovno otvorite).

4. Idite na **Settings → MCP Servers** i provjerite prikazuju li se oba poslužitelja kao povezana. Windsurf otvara karticu preglednika na `https://iam.wink.travel` pri prvom korištenju.

5. **Prijavite se u Wink** i odobrite dozvole. Poslužitelji postaju zeleni u postavkama nakon autentikacije.
</Steps>

:::note
Podrška za Windsurf MCP zahtijeva verziju **1.4** ili noviju. Provjerite putem **Help → About Windsurf** i po potrebi ažurirajte s Windsurf web stranice.
:::

  </TabItem>
  <TabItem label="Cline (VS Code)">

Cline je VS Code ekstenzija s ugrađenim MCP upraviteljem poslužitelja. Nije potrebno ručno uređivanje JSON-a.

<Steps>
1. Instalirajte Cline iz VS Code Marketplace:
   - Otvorite VS Code i pritisnite `Ctrl+Shift+X` / `Cmd+Shift+X`
   - Potražite **Cline** i kliknite **Install**

2. Kliknite **ikonu Cline** u Activity Baru (lijevi bočni izbornik) za otvaranje Cline panela.

3. Kliknite ikonu **MCP Servers** (ikona utikača) u Cline alatnoj traci, zatim odaberite **Edit MCP Settings**.

4. VS Code otvara `cline_mcp_settings.json`. Dodajte Wink unose:

   ```json title="cline_mcp_settings.json"
   {
     "mcpServers": {
       "wink-extranet-rates": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/rates"
       },
       "wink-extranet-operations": {
         "type": "http",
         "url": "https://api.wink.travel/mcp/extranet/operations"
       }
     }
   }
   ```

5. **Spremite datoteku.** Cline se automatski ponovno učitava i otvara karticu preglednika na `https://iam.wink.travel`.

6. **Prijavite se u Wink** i odobrite dozvole. Oba poslužitelja pojavljuju se kao aktivna u MCP Servers panelu.
</Steps>

:::tip[Alternativa: dodajte putem URL-a]
U MCP Servers panelu kliknite **Add Server** i zalijepite URL skupa alata poput `https://api.wink.travel/mcp/extranet/rates` izravno. Cline automatski popunjava konfiguraciju i upravlja autentikacijom bez ručnog uređivanja JSON-a.
:::

  </TabItem>
</Tabs>

---

## Dozvole (Scopes)

Kad odobrite zaslon za suglasnost, vidjet ćete razradu što agent traži. MCP
klijenti koriste isti vokabular dozvola kao i ostatak platforme — pogledajte cjeloviti
[referentni popis dozvola na stranici Autentikacije](/hr/developers/authentication#scopes).

Dva su specifična za MCP:

- **`mcp.read`, `mcp.write` i `mcp.remove` dozvole su dodatno potrebne** za otvaranje MCP
  transporta. Klijent koji ima `mcp.*` dozvolu može pristupiti bilo kojoj Wink MCP krajnjoj točki, ali svaki
  alat koji pozove i dalje provodi dozvole sekcije krajnje točke koju obuhvaća (npr. `booking.read`).
- **AI klijent traži samo dozvole koje mu trebaju.** Možete odbiti pojedinačne dozvole na
  zaslonu za suglasnost — alati koji zahtijevaju odbijenu dozvolu vratit će grešku pri pozivu.

**Skupovi alata ne mijenjaju ništa od ovoga.** Krajnja točka na koju se povežete odlučuje koji su alati *navedeni*,
a ne što im je *dozvoljeno*. Povezivanje na `/mcp/payment` ne daje nikakvu financijsku dozvolu koju već niste imali, a povezivanje na `/mcp/reference` ne sprječava alat da provodi vlastite dozvole. Autorizacija se odlučuje po pozivu alata, na temelju vašeg tokena i prava vašeg računa.

---

## Rješavanje problema

**Kartica preglednika se ne otvara**

Vaš AI klijent možda još ne podržava interaktivni PKCE tok. Provjerite koristite li najnoviju verziju:
- **Claude Code:** `npm install -g @anthropic-ai/claude-code`
- **Codex CLI:** `npm install -g @openai/codex`
- **Claude Desktop / ChatGPT desktop:** ažurirajte putem ugrađenog updatera aplikacije

**`401 Unauthorized` nakon prijave**

Vaš spremljeni token može biti zastario. Uklonite i ponovno dodajte poslužitelj da pokrenete novu prijavu:

```bash
# Claude Code
claude mcp remove wink-rates
claude mcp add wink-rates https://api.wink.travel/mcp/extranet/rates --transport http
```

Za Claude Desktop i ChatGPT, uklonite unos poslužitelja iz postavki, ponovno pokrenite aplikaciju i dodajte ga ponovno.

**`403 Forbidden` pri pozivu određenog alata**

Alat zahtijeva dozvolu koju niste odobrili na zaslonu za suglasnost ili dozvolu koju vaš račun nema. Ponovno povežite poslužitelj — zaslon za suglasnost će se ponovno pojaviti i možete odobriti nedostajuću dozvolu. Ako i dalje ne uspijeva, alat treba dozvolu računa koju nemate; povezivanje na drugi skup alata neće pomoći jer skupovi alata ne dodjeljuju dozvole.

**Alat koji trebam nije na popisu**

Vjerojatno ste povezani na skup alata koji ne pokriva taj zadatak. Provjerite [direktorij krajnjih točaka](#endpoint-directory) i dodajte skup alata koji ga pokriva — veze su neovisne, pa dodavanje jednog ne mijenja druge. Ako niste sigurni koji ga ima, privremeno se povežite na `https://api.wink.travel/mcp`, pronađite alat, zatim provjerite koji skup alata ga navodi.

**`404 Not Found` na krajnjoj točki skupa alata**

Pažljivo provjerite putanju — ugniježđeni skupovi alata imaju dva segmenta (`/mcp/extranet/rates`, ne `/mcp/extranet-rates`), a ravni imaju jedan (`/mcp/link-manager`). Napomena: *neautentificirani* zahtjev na bilo koji put ispod `/mcp` vraća `401`, ne `404`, pa sam `401` ne govori postoji li krajnja točka.

**Povezao sam se na `api.wink.travel/mcp/channel-manager` i ništa nisam dobio**

Ta krajnja točka ne postoji. Integracija channel managera se služi samo s `https://integrations.wink.travel/mcp/channel-manager`. Ako također trebate ručno čitati ili pisati vlastite cijene objekta, to je zaseban zadatak na zasebnoj krajnjoj točki — vidi `/mcp/extranet/rates`.

**Poslužitelj se pojavljuje, ali pozivi alata vraćaju "račun nije pronađen"**

Prijavljeni ste u Wink račun koji nema upravljački subjekt. Idite na [Wink Portal](https://app.wink.travel) i provjerite je li vaš račun postavljen prije korištenja MCP poslužitelja.

**Token istječe tijekom sesije**

Tokeni vrijede 3 sata. Claude Code i Claude Desktop tiho osvježavaju tokene bez prekida sesije. Ako vidite greške autentikacije nakon dužeg mirovanja, pošaljite novu poruku — klijent će se automatski ponovno autentificirati.

**Codex CLI prikazuje "MCP server unreachable"**

Provjerite je li vaš `~/.codex/config.json` valjani JSON (bez zareza na kraju) i da je `"type": "http"` postavljeno. Codex zahtijeva HTTP transport za udaljene poslužitelje; stdio transport je samo za lokalne binarne datoteke.

**ChatGPT nema izbornik "Connectors"**

Podrška za MCP zahtijeva verziju ChatGPT desktop aplikacije **1.2024.352** ili noviju i aktivnu Plus, Pro, Team ili Enterprise pretplatu. Ažurirajte aplikaciju i provjerite svoj plan u Settings → Account.

**Cursor poslužitelj pokazuje crveno / "Failed to connect"**

Provjerite je li vaš `~/.cursor/mcp.json` valjani JSON i sadrži `"type": "http"`. Zatim idite na **Settings → Cursor Settings → MCP** i kliknite **Refresh**. Ako greška ostaje, potpuno ponovno pokrenite Cursor — promjene konfiguracije se ne preuzimaju uvijek bez ponovnog pokretanja.

**Windsurf MCP poslužitelji se ne prikazuju u postavkama**

Windsurf zahtijeva potpuno ponovno pokretanje (ne samo ponovno učitavanje prozora) da preuzme promjene u `mcp_config.json`. Koristite **File → Quit** i ponovno pokrenite. Ako poslužitelji i dalje ne postoje, provjerite jeste li na Windsurf verziji **1.4+** putem **Help → About Windsurf**.

**Cline nema opciju "Add Server"**

Upravitelj MCP poslužitelja zahtijeva Cline verziju **2.0** ili noviju. Otvorite VS Code Extensions panel, pronađite Cline i kliknite **Update** ako je dostupna nadogradnja. Nakon nadogradnje, ponovno učitajte VS Code (`Ctrl+Shift+P` → "Developer: Reload Window").

---

<LinkButton href="https://app.wink.travel" variant="primary" icon="external">Otvori Wink Portal</LinkButton>
<LinkButton href="https://github.com/wink-travel" variant="secondary" icon="external">Wink na GitHubu</LinkButton>
