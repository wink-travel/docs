---
title: Preact
description: Utilize os componentes web Wink em aplicações Preact — mesmo padrão do React com uma pegada mais leve.
sidebar:
  order: 24
---

import { Aside, LinkCard } from '@astrojs/starlight/components';

A API do Preact é compatível com React. A integração segue o mesmo padrão — chame `load()` uma vez no arranque da aplicação, depois use as tags `<wink-*>` no JSX.

<LinkCard
  title="@wink/elements npm package"
  href="/integrations/npm"
  description="Instale e carregue o bundle CDN com tipos TypeScript."
/>

## Instalar

```bash
npm install @wink/elements
```

## Carregar uma vez no arranque da aplicação

```tsx
// src/app.tsx
import { useEffect } from 'preact/hooks';
import { load } from '@wink/elements';

export function App() {
  useEffect(() => {
    load({ clientId: import.meta.env.VITE_WINK_CLIENT_ID });
  }, []);

  return <YourRoutes />;
}
```

## Usar no JSX

```tsx
// src/pages/hotels.tsx
export function HotelsPage() {
  return (
    <main>
      <wink-content-loader layout="HOTEL" id="YOUR_LAYOUT_ID" />
      <wink-lookup />
    </main>
  );
}
```

## TypeScript — declarar intrínsecos JSX

O namespace JSX do Preact é separado do React. Aumente `preact/src/jsx.d.ts`:

```ts
// src/wink-elements.d.ts
import type {
  WinkContentLoaderAttributes,
  WinkLookupAttributes,
  WinkSearchButtonAttributes,
  WinkAccountButtonAttributes,
  WinkItineraryButtonAttributes,
  WinkShoppingCartButtonAttributes,
  WinkAppLoaderAttributes,
} from '@wink/elements';

declare module 'preact' {
  namespace JSX {
    interface IntrinsicElements {
      'wink-content-loader': WinkContentLoaderAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-lookup': WinkLookupAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-search-button': WinkSearchButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-account-button': WinkAccountButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-itinerary-button': WinkItineraryButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-shopping-cart-button': WinkShoppingCartButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-app-loader': WinkAppLoaderAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
    }
  }
}
```

## Preact com `preact/compat`

Se estiver a fazer alias do React para `preact/compat`, use as declarações JSX do React em vez disso:

```ts
// src/wink-elements.d.ts
// Use a declaração JSX do React de /integrations/react
// — preact/compat reexporta o namespace JSX do React
```

<Aside type="tip">
O tamanho mais reduzido do bundle do Preact é uma boa combinação para incorporar componentes Wink em lojas sensíveis à performance. A função `load()` do `@wink/elements` adiciona ~500 bytes comprimidos — a maior parte do peso vem do bundle CDN carregado de forma assíncrona.
</Aside>

## Leitura adicional

- [Referência de Web Components](/pt-PT/integrations/web-components)
- [@wink/elements npm package](/pt-PT/integrations/npm)
- [Integração React](/pt-PT/integrations/react)
