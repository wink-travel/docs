---
title: Joomla
description: Incorpore o inventário de viagens Wink no seu website Joomla 4/5 usando a extensão Wink.
sidebar:
  order: 12
---

import { Steps, LinkCard } from '@astrojs/starlight/components';

A **Extensão Wink para Joomla** é um pacote que instala um plugin de sistema (carrega scripts CDN), um plugin de conteúdo (processa tags shortcode em artigos) e um componente (página de definições administrativas).

<LinkCard
  title="wink-travel/wink-joomla-extension no GitHub"
  href="https://github.com/wink-travel/wink-joomla-extension"
  description="Código fonte, guia de configuração para desenvolvedores e instruções de contribuição."
/>

## Requisitos

- Joomla 4 ou 5
- PHP 8.1+

## Instalar

<Steps>
1. Descarregue o mais recente `pkg_wink-x.x.x.zip` da [página de lançamentos do GitHub](https://github.com/wink-travel/wink-joomla-extension/releases).
2. No Joomla Admin, vá a **Extensões → Gerir → Instalar**.
3. Carregue o ficheiro zip e clique em **Carregar & Instalar**.
4. Ambos os plugins são ativados automaticamente. Vá a **Componentes → Definições Wink**.
5. Introduza o seu **Client ID** e **Client Secret** e clique em **Guardar**.
   Vá a [Aplicações](/pt-PT/portal/settings/applications) para obter as suas credenciais.
</Steps>

## Incorporar componentes em artigos

Use tags no estilo shortcode em qualquer parte do conteúdo do artigo:

```
{wink-content layout="HOTEL" id="YOUR_LAYOUT_ID"}

{wink-lookup}

{wink-search}

{wink-account}

{wink-itinerary}

{wink-cart}
```

O plugin de conteúdo substitui estas tags pelo HTML correto `<wink-*>` no momento da renderização. O plugin de sistema carrega automaticamente os scripts CDN e injeta `<wink-app-loader>` no rodapé da página.

## Shortcodes disponíveis

| Shortcode | Saída | Descrição |
|---|---|---|
| `{wink-content layout="..." id="..."}` | `<wink-content-loader>` | Cartões de hotel, grelhas, mapas |
| `{wink-lookup}` | `<wink-lookup>` | Barra de pesquisa de destinos |
| `{wink-search}` | `<wink-search-button>` | Botão de seleção de itinerário |
| `{wink-account}` | `<wink-account-button>` | Iniciar sessão / conta de utilizador |
| `{wink-itinerary}` | `<wink-itinerary-button>` | Botão de estado do itinerário |
| `{wink-cart}` | `<wink-shopping-cart-button>` | Botão do carrinho |

## Leitura adicional

- [Referência de Web Components](/pt-PT/integrations/web-components)
- [Obtenha o seu Client ID](/pt-PT/portal/settings/applications)
- [Extensão Wink para Joomla — código fonte & documentação para desenvolvedores](https://github.com/wink-travel/wink-joomla-extension)
