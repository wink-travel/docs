---
title: React
description: Usa los web components de Wink en cualquier aplicación React con soporte completo de TypeScript vía @wink/elements.
sidebar:
  order: 15
---

import { Aside, LinkCard } from '@astrojs/starlight/components';

React 19+ tiene soporte nativo para custom elements. React 18 y versiones anteriores solo pasan atributos como strings — usa el patrón de helper tipado a continuación para mantener compatibilidad entre versiones.

<LinkCard
  title="@wink/elements npm package"
  href="/integrations/npm"
  description="Instalá y cargá el bundle CDN con tipos de TypeScript."
/>

## Instalación

```bash
npm install @wink/elements
```

## Cargar una vez en la raíz de la app

Llamá a `load()` en tu componente de nivel superior. Es idempotente — seguro llamarlo varias veces.

```tsx
// src/App.tsx
import { useEffect } from 'react';
import { load } from '@wink/elements';

export default function App() {
  useEffect(() => {
    load({ clientId: import.meta.env.VITE_WINK_CLIENT_ID });
  }, []);

  return <YourRoutes />;
}
```

## Usar componentes en JSX

```tsx
export default function HotelPage() {
  return (
    <main>
      <wink-content-loader layout="HOTEL" id="YOUR_LAYOUT_ID" />
      <wink-lookup />
    </main>
  );
}
```

## TypeScript — declarar intrínsecos JSX

React no reconoce por defecto los nombres de custom elements. Agregá un archivo de declaración una vez para evitar errores de tipo:

```ts
// src/wink-elements.d.ts
import type {
  WinkContentLoaderAttributes,
  WinkLookupAttributes,
  WinkSearchButtonAttributes,
  WinkAccountButtonAttributes,
  WinkItineraryButtonAttributes,
  WinkShoppingCartButtonAttributes,
  WinkAppLoaderAttributes,
} from '@wink/elements';

declare module 'react' {
  namespace JSX {
    interface IntrinsicElements {
      'wink-content-loader': WinkContentLoaderAttributes & React.HTMLAttributes<HTMLElement>;
      'wink-lookup': WinkLookupAttributes & React.HTMLAttributes<HTMLElement>;
      'wink-search-button': WinkSearchButtonAttributes & React.HTMLAttributes<HTMLElement>;
      'wink-account-button': WinkAccountButtonAttributes & React.HTMLAttributes<HTMLElement>;
      'wink-itinerary-button': WinkItineraryButtonAttributes & React.HTMLAttributes<HTMLElement>;
      'wink-shopping-cart-button': WinkShoppingCartButtonAttributes & React.HTMLAttributes<HTMLElement>;
      'wink-app-loader': WinkAppLoaderAttributes & React.HTMLAttributes<HTMLElement>;
    }
  }
}
```

<Aside type="caution" title="Limitación de atributos en React 18">
React 18 y versiones anteriores serializan todas las props JSX a strings antes de pasarlas al DOM. Las props booleanas y objetos deben convertirse manualmente. React 19 pasa props no-string directamente a custom elements — no se necesita workaround.
</Aside>

## Pasar atributos tipados (compatible con React 18)

```tsx
import type { WinkContentLoaderAttributes } from '@wink/elements';

const attrs: WinkContentLoaderAttributes = {
  layout: 'HOTEL',
  id: 'abc123',
  sort: 'POPULARITY',
};

// Spread funciona en React 19; para React 18 todos los valores son strings de todos modos
export default function HotelCard() {
  return <wink-content-loader layout={attrs.layout} id={attrs.id} sort={attrs.sort} />;
}
```

## Lectura adicional

- [Referencia de Web Components](/es-AR/integrations/web-components)
- [@wink/elements npm package](/es-AR/integrations/npm)
- [Integración con Next.js](/es-AR/integrations/nextjs)
