---
title: Webhook Events Catalog
description: Every webhook event type on the Wink platform, generated from the platform's event catalog — which are delivered, to whom, and where the payload is documented.
sidebar:
  order: 3.5
---

import webhooks from '../../../../schemas/webhooks.json';
import WebhookEventTypes from '@/components/webhooks/event-types.astro';

This page lists every webhook event you can subscribe to on Wink. For setup instructions, see the [Webhook Integration Guide](/developers/webhook-integration) or the [Webhooks management page](/portal/settings/webhooks).

## How to subscribe

1. Navigate to **Applications > Webhooks** in your portal.
2. Create a webhook and select the events you want to receive — or leave the list empty to receive every event.
3. Wink sends a signed HTTP POST to your endpoint each time the event occurs. See [Webhook Integration](/developers/webhook-integration) for the envelope, headers, signature verification and retries.

## Every event type

This table is generated from the platform's event catalog at build time, so it always matches the API.
Event types marked as delivered link to their reference page in the [Webhooks API](/api/webhooks/), which
documents the exact JSON body (`WebhookEnvelope` with an event-specific `object`), the `Wink-*` headers and
the retry policy. Event types that are not delivered exist internally (payments, ledger, channel-manager
sync, …) but are not currently sent to webhook subscribers.

**Recipients** says who receives an event: `SUPPLIER` (the hotel), `AFFILIATE` (a reseller with an active
sales channel to the hotel), `AGENT` (the travel agency that made the booking) or `SELF` (the managing entity
the event is about). Every recipient receives the same payload shape; role-specific detail is behind the
payload's `links.self` URL.

<WebhookEventTypes
  types={webhooks['x-wink-event-types']}
  referenceBase="/api/webhooks"
  eventHeader="Event"
  descriptionHeader="Description"
  deliveredHeader="Delivered"
  recipientsHeader="Recipients"
  deliveredLabel="delivered"
  notDeliveredLabel="not delivered"
  noRecipientsLabel="—"
/>

:::tip
Start by subscribing to all events during development. Once you understand which payloads your system needs, narrow your subscriptions to reduce noise. Send yourself a test event from **Applications > Webhooks** (or `POST …/webhook/{id}/test`) to confirm your endpoint and signature check before going live.
:::

## Further reading

- [Webhook Integration Guide](/developers/webhook-integration) — Setup steps and best practices.
- [Webhooks](/portal/settings/webhooks) — Manage your webhook subscriptions.
- [Applications](/account/applications) — Manage API credentials.
- [Build on Wink](/developers/build-on-wink) — Platform overview for developers.
