---
title: Svelte / SvelteKit
description: Używaj komponentów webowych Wink w aplikacjach Svelte i SvelteKit z onMount i zabezpieczeniami dla przeglądarki.
sidebar:
  order: 20
---

import { Aside, LinkCard } from '@astrojs/starlight/components';

Svelte przekazuje atrybuty bezpośrednio do DOM bez warstwy wirtualnego DOM, co sprawia, że elementy niestandardowe są naturalnym wyborem. Dla SvelteKit (SSR) załaduj CDN wewnątrz `onMount` lub zabezpiecz za pomocą sprawdzenia `browser`.

<LinkCard
  title="@wink/elements npm package"
  href="/integrations/npm"
  description="Zainstaluj i załaduj pakiet CDN z typami TypeScript."
/>

## Instalacja

```bash
npm install @wink/elements
```

## SvelteKit — główny layout

```ts
// src/routes/+layout.ts
import { browser } from '$app/environment';
import { load as loadWink } from '@wink/elements';

export function load() {
  if (browser) {
    loadWink({ clientId: import.meta.env.PUBLIC_WINK_CLIENT_ID });
  }
}
```

Alternatywnie, załaduj wewnątrz `onMount` w `+layout.svelte`:

```svelte
<!-- src/routes/+layout.svelte -->
<script>
  import { onMount } from 'svelte';
  import { load } from '@wink/elements';

  onMount(() => {
    load({ clientId: import.meta.env.PUBLIC_WINK_CLIENT_ID });
  });
</script>

<slot />
```

## Użycie w komponentach

Svelte przekazuje wszystkie nieznane atrybuty bezpośrednio do DOM, więc nie jest potrzebna dodatkowa konfiguracja:

```svelte
<script>
  export let layoutId = 'YOUR_LAYOUT_ID';
</script>

<main>
  <wink-content-loader layout="HOTEL" id={layoutId} />
  <wink-lookup />
</main>
```

## Svelte 5 (składnia runes)

```svelte
<script lang="ts">
  import { onMount } from 'svelte';
  import { load } from '@wink/elements';

  let { layoutId }: { layoutId: string } = $props();

  onMount(() => {
    load({ clientId: import.meta.env.PUBLIC_WINK_CLIENT_ID });
  });
</script>

<wink-content-loader layout="HOTEL" id={layoutId} />
```

## Zmienne środowiskowe

SvelteKit udostępnia w przeglądarce tylko zmienne z prefiksem `PUBLIC_`:

```
# .env
PUBLIC_WINK_CLIENT_ID=your-client-id
```

<Aside type="caution" title="Renderowanie SSR">
Nigdy nie renderuj elementów `<wink-*>` podczas renderowania po stronie serwera — elementy niestandardowe są rejestrowane w rejestrze `customElements` przeglądarki, który nie istnieje na serwerze. Owiń w `{#if browser}` lub wywołuj `load()` tylko wewnątrz `onMount`.
</Aside>

```svelte
<script>
  import { browser } from '$app/environment';
</script>

{#if browser}
  <wink-content-loader layout="HOTEL" id="YOUR_LAYOUT_ID" />
{/if}
```

## Czysty Svelte (Vite, bez SvelteKit)

```svelte
<!-- src/App.svelte -->
<script>
  import { onMount } from 'svelte';
  import { load } from '@wink/elements';

  onMount(() => {
    load({ clientId: import.meta.env.VITE_WINK_CLIENT_ID });
  });
</script>

<wink-content-loader layout="HOTEL" id="YOUR_LAYOUT_ID" />
```

## Dalsza lektura

- [Referencje Web Components](/pl/integrations/web-components)
- [@wink/elements npm package](/pl/integrations/npm)
