---
title: Preact
description: Gunakan komponen web Wink dalam aplikasi Preact — corak yang sama seperti React dengan jejak yang lebih kecil.
sidebar:
  order: 24
---

import { Aside, LinkCard } from '@astrojs/starlight/components';

API Preact serasi dengan React. Integrasi mengikuti corak yang sama — panggil `load()` sekali sahaja semasa permulaan aplikasi, kemudian gunakan tag `<wink-*>` dalam JSX.

<LinkCard
  title="@wink/elements npm package"
  href="/integrations/npm"
  description="Pasang dan muatkan bundel CDN dengan jenis TypeScript."
/>

## Pasang

```bash
npm install @wink/elements
```

## Muat sekali semasa permulaan aplikasi

```tsx
// src/app.tsx
import { useEffect } from 'preact/hooks';
import { load } from '@wink/elements';

export function App() {
  useEffect(() => {
    load({ clientId: import.meta.env.VITE_WINK_CLIENT_ID });
  }, []);

  return <YourRoutes />;
}
```

## Gunakan dalam JSX

```tsx
// src/pages/hotels.tsx
export function HotelsPage() {
  return (
    <main>
      <wink-content-loader layout="HOTEL" id="YOUR_LAYOUT_ID" />
      <wink-lookup />
    </main>
  );
}
```

## TypeScript — isytiharkan JSX intrinsics

Namespace JSX Preact berasingan daripada React. Tambah `preact/src/jsx.d.ts`:

```ts
// src/wink-elements.d.ts
import type {
  WinkContentLoaderAttributes,
  WinkLookupAttributes,
  WinkSearchButtonAttributes,
  WinkAccountButtonAttributes,
  WinkItineraryButtonAttributes,
  WinkShoppingCartButtonAttributes,
  WinkAppLoaderAttributes,
} from '@wink/elements';

declare module 'preact' {
  namespace JSX {
    interface IntrinsicElements {
      'wink-content-loader': WinkContentLoaderAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-lookup': WinkLookupAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-search-button': WinkSearchButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-account-button': WinkAccountButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-itinerary-button': WinkItineraryButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-shopping-cart-button': WinkShoppingCartButtonAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
      'wink-app-loader': WinkAppLoaderAttributes & preact.JSX.HTMLAttributes<HTMLElement>;
    }
  }
}
```

## Preact dengan `preact/compat`

Jika anda menggunakan alias React kepada `preact/compat`, gunakan deklarasi JSX React sebaliknya:

```ts
// src/wink-elements.d.ts
// Gunakan deklarasi JSX React dari /integrations/react
// — preact/compat mengeksport semula namespace JSX React
```

<Aside type="tip">
Saiz bundel Preact yang lebih kecil sangat sesuai untuk menyematkan komponen Wink dalam kedai yang sensitif kepada prestasi. Fungsi `load()` dari `@wink/elements` menambah ~500 bait gzipped — sebahagian besar berat datang dari bundel CDN yang dimuatkan secara tak segerak.
</Aside>

## Bacaan lanjut

- [Rujukan Web Components](/ms/integrations/web-components)
- [@wink/elements npm package](/ms/integrations/npm)
- [Integrasi React](/ms/integrations/react)
