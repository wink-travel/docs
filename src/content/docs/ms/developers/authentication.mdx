---
title: Pengesahan
description: Pelajari cara mengesahkan menggunakan pelayan kebenaran OAuth2 Wink.
sidebar:
  order: 2
---

:::tip
Pelayan pengesahan Wink IAM menyokong titik akhir OAuth2 yang biasa:

- /oauth2/authorize
- /oauth2/token
- /oauth2/introspect
- /oauth2/revoke
- /oauth2/userinfo
:::

Sebuah aplikasi menyediakan anda dengan `Client ID` dan `Secret Key` yang anda perlukan untuk mencipta sesi OAuth2 yang disahkan yang boleh anda gunakan untuk berkomunikasi dengan API Wink dan TripPay.

Berikut adalah langkah-langkah untuk mencipta sesi yang disahkan.

**Langkah 1**. Dapatkan token akses di persekitaran staging atau produksi kami:

Anda kemungkinan besar akan menggunakan perpustakaan OAuth2 yang kukuh untuk bahasa anda, yang akan melakukan semua kerja berat untuk anda.
Contoh kami akan menunjukkan penggunaan paling asas dari baris perintah menggunakan `curl`.

### Staging

```bash

curl -X POST https://staging-iam.wink.travel/oauth2/token \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "grant_type=client_credentials" \
  -d "client_id=<YOUR_CLIENT_ID>" \
  -d "client_secret=<YOUR_SECRET_KEY>"

```

### Produksi

```bash

curl -X POST https://iam.wink.travel/oauth2/token \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "grant_type=client_credentials" \
  -d "client_id=<YOUR_CLIENT_ID>" \
  -d "client_secret=<YOUR_SECRET_KEY>"

```

Ini akan mengembalikan token akses, bersama data respons OAuth2 yang lain:

```json
{
  "access_token": "abc123"
}
```

Apabila anda membuat panggilan ke mana-mana titik akhir kami, sertakan yang berikut dalam header:

- `Wink-Version` = `2.0` Terkini - *Lihat dokumentasi API kami untuk versi lain yang tersedia.*
- `Authentication` = `Bearer: ${access_token}` *Masukkan token akses anda.*

:::tip
Anda boleh menguji semua API kami tanpa menulis satu baris kod pun sebelum bermula.

Dapatkan 'Client ID' dan 'Secret Key' dari salah satu aplikasi anda dan navigasi ke mana-mana [API](/ms/developers/apis) kami.
:::
