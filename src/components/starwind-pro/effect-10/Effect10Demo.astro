---
/**
 * Rising Particles Effect
 *
 * A beautiful rising particle animation that creates an ambient background effect.
 * Particles float upward with gentle horizontal drift, perfect for hero sections
 * or decorative backgrounds.
 *
 * Usage:
 * 1. Use the starwind cli to add this component to your project `npx starwind@latest add @starwind-pro/effect-10`
 * 2. Import the `Effect10` component and wrap your content
 *
 * Props:
 * - particleCount: number (default: 50) - Number of particles
 * - minSize: number (default: 2) - Minimum particle size in pixels
 * - maxSize: number (default: 6) - Maximum particle size in pixels
 * - minDuration: number (default: 8) - Minimum rise duration in seconds
 * - maxDuration: number (default: 20) - Maximum rise duration in seconds
 * - particleColor: string (default: "currentColor") - Particle color
 * - minOpacity: number (default: 0.1) - Minimum particle opacity
 * - maxOpacity: number (default: 0.6) - Maximum particle opacity
 * - class: string
 */
import RisingParticlesEffect from "./Effect10.astro";
---

<RisingParticlesEffect
  id="effect-10"
  class="@container w-full py-48"
  particleCount={50}
  minSize={2}
  maxSize={5}
  minDuration={10}
  maxDuration={18}
  particleColor="var(--color-foreground)"
  minOpacity={0.15}
  maxOpacity={0.5}
>
  <div class="mx-auto mb-8 max-w-2xl text-center">
    <h2 class="font-heading text-3xl font-semibold tracking-tight @xl:text-4xl">
      Rising Particles Effect
    </h2>
    <p class="text-muted-foreground mt-3">
      An ambient particle animation that creates a magical, floating atmosphere. Perfect for hero
      sections and decorative backgrounds.
    </p>
  </div>

  <p class="text-muted-foreground mt-4 text-center text-sm">
    Customize particle count, size, speed, color, and opacity
  </p>
</RisingParticlesEffect>
