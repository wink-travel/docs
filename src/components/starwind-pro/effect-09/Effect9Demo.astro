---
/**
 * Image Compare Slider Effect
 *
 * A before/after image comparison slider that allows users to drag a handle
 * to reveal more of one image while hiding the other.
 *
 * Usage:
 * 1. Use the starwind cli to add this component to your project `npx starwind@latest add @starwind-pro/effect-09`
 * 2. Import the `Effect9` component and provide before/after image sources
 *
 * Props:
 * - beforeSrc: string (required) - URL for the "before" image
 * - afterSrc: string (required) - URL for the "after" image
 * - beforeAlt: string (default: "Before") - Alt text for before image
 * - afterAlt: string (default: "After") - Alt text for after image
 * - initialPosition: number (default: 50) - Initial slider position (0-100)
 * - class: string
 */
import ImageCompareEffect from "./Effect9.astro";
---

<section id="effect-09" class="@container mx-auto w-full max-w-7xl px-4 py-24">
  <div class="mx-auto max-w-3xl">
    <div class="mx-auto mb-8 max-w-2xl text-center">
      <h2 class="font-heading text-3xl font-semibold tracking-tight @xl:text-4xl">
        Image Compare Slider
      </h2>
      <p class="text-muted-foreground mt-3">
        Drag the slider to compare before and after images. Perfect for showcasing transformations,
        edits, or design changes.
      </p>
    </div>

    <ImageCompareEffect
      class="rounded-lg"
      beforeSrc="https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=800&h=600&fit=crop&sat=-100"
      afterSrc="https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=800&h=600&fit=crop"
      beforeAlt="Grayscale mountain landscape"
      afterAlt="Color mountain landscape"
      initialPosition={50}
    />

    <p class="text-muted-foreground mt-4 text-center text-sm">
      Before: Grayscale • After: Full Color
    </p>
  </div>
</section>
